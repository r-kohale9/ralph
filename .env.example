# ─── CLIProxyAPI ──────────────────────────────────────────────────────────────
PROXY_URL=http://localhost:8317
PROXY_KEY=ralph-pipeline-key-change-this

# ─── Model routing (override to reroute any step) ────────────────────────────
RALPH_GEN_MODEL=claude-opus-4-6          # Step 1: HTML generation
RALPH_TEST_MODEL=gemini-3.1-pro-preview          # Step 2: Test generation
RALPH_FIX_MODEL=claude-sonnet-4-6        # Step 3: Fix iterations
RALPH_REVIEW_MODEL=gemini-3.1-pro-preview  # Step 4: Review
RALPH_FALLBACK_MODEL=gpt-4.1             # Overflow when primary returns 429
RALPH_TRIAGE_MODEL=gpt-4.1-mini
RALPH_GLOBAL_FIX_MODEL=claude-opus-4-6
RALPH_LEARNINGS_MODEL=gpt-4.1-mini

# ─── Pipeline settings ───────────────────────────────────────────────────────
RALPH_AUTO_RETRY=0                   # Set to 1 to auto-requeue builds that score 0/total tests
RALPH_CONCURRENCY=1                  # Increase to 2-3 for parallel builds (ensure enough RAM for concurrent Playwright)
RALPH_MODEL_CONCURRENCY=4            # Max concurrent LLM requests per model (prevents quota storms with parallel builds)
RALPH_MAX_ITERATIONS=5
RALPH_MAX_GLOBAL_FIX_ITERATIONS=2
RALPH_BULK_THRESHOLD=5
RALPH_LLM_TIMEOUT=300
RALPH_TEST_TIMEOUT=120

# ─── Infrastructure ──────────────────────────────────────────────────────────
PORT=3000
REDIS_URL=redis://localhost:6379
RALPH_REPO_DIR=/srv/ralph/repo
RALPH_DB_PATH=/srv/ralph/data/builds.db
RALPH_CPU_GATE=85
RALPH_RAM_GATE_MB=512

# ─── Integrations ────────────────────────────────────────────────────────────
GITHUB_WEBHOOK_SECRET=your-github-webhook-secret
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/T.../B.../...
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token
SLACK_SIGNING_SECRET=your-slack-signing-secret
SLACK_CHANNEL_ID=C0123456789
# Slack user ID to tag on build start (e.g. U0242GULG48). Optional — set per-user in .env or in
# the MCP server env block in Claude config to tag the person who triggered the build.
RALPH_SLACK_USER_ID=

# ─── GCP Cloud Storage ──────────────────────────────────────────────────────
RALPH_GCP_BUCKET=your-gcp-bucket-name
RALPH_GCP_PROJECT=your-gcp-project-id

# ─── Observability ────────────────────────────────────────────────────────────
# Google Cloud Logging (leave empty for structured stdout logging)
GOOGLE_CLOUD_PROJECT=
RALPH_LOG_NAME=ralph-pipeline
LOG_LEVEL=info

# Sentry error monitoring (leave empty to disable)
SENTRY_DSN=
NODE_ENV=production
RALPH_VERSION=1.0.0
