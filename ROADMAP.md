# Ralph Pipeline — Roadmap

**Last updated:** March 23, 2026 (Session: CT9 transition-slot visibility ban + C1 timeout fix + RULE-003 transitionScreen try/catch (dc03155, 4e8fca8, 2026-03-23); PART-028 stripped-CSS T1 check + FIX-001 preserve-CSS rule + ARIA-001 feedback accessibility (dc03155); Slack parent message live updates at step boundaries (9101650); Cross-slot feed links — UI/UX findings route to R&D + test quality + education (4f96009); Cron 7 Slot Watchdog — idle slot detection every 5 min (c57447d); per-game docs consolidated into warehouse/templates/ (7935d5d); R&D slot ACTIVE: post-GEN-116/117/118 first-attempt approval rate measurement (2/5 data points); Education slot ACTIVE: which-ratio #560 re-queued after EACCES infra fix)
**Status legend:** done | in-progress | planned | blocked

---

## P0 — Deployment Blockers

| Item | Status | File(s) | Notes |
|------|--------|---------|-------|
| HMAC-SHA256 webhook verification | done | server.js:35-70 | Length-safe `timingSafeEqual` |
| BigInt serialization crash | done | db.js:48 | `Number(result.lastInsertRowid)` |
| Async Express error handling | done | server.js:122,174 | try/catch in all async routes |
| LLM infinite recursion on 429 | done | llm.js:19-50 | Max 5 retries with exponential backoff |
| Metrics NaN corruption guard | done | metrics.js:observeHistogram | Rejects null/undefined/NaN |
| Health endpoint crash (Redis down) | done | server.js:243-247 | `getJobCounts` inside try/catch |
| Server reference for graceful shutdown | done | server.js:261 | `const server = app.listen(...)` |
| Refuse to start without webhook secret | done | server.js:23-26 | `process.exit(1)` in production mode |
| Dockerfile for Node.js app | done | Dockerfile | Multi-stage: builder (native addons) → runtime (node:20-slim) |
| .dockerignore | done | .dockerignore | Excludes node_modules, .git, data, .env, test/ |
| CI/CD pipeline (lint + test) | done | .github/workflows/ci.yml | `node --check`, `bash -n ralph.sh`, `npm test` |
| CI/CD deploy workflow | done | .github/workflows/deploy.yml | SSH deploy, manual trigger, requires CI pass |

## P1 — Testing & Validation

| Item | Status | File(s) | Notes |
|------|--------|---------|-------|
| Unit tests (101 cases) | done | test/*.test.js | db, metrics, slack, llm, logger, sentry, server, worker, validate-static |
| ralph.sh smoke tests (31→45 cases) | done | test/ralph-sh.test.js | Syntax, structure, extract_html, extract_tests, validate_spec, config defaults, report format, E6/E8/E9/E10/T6/T2/E5 |
| E2E integration test | done | test/e2e.test.js | Full build lifecycle, HMAC signatures, concurrent builds, failure paths |
| T1 validator: warnings → errors | done | lib/validate-static.js | answer handler, gameState, star thresholds, 480px — all now errors |
| T1 validator: fix pattern matching | done | lib/validate-static.js | `id="gameContent"` regex; star thresholds use `/0\.8\b/`, `/80\s*%/` |
| Contract tests (CLIProxyAPI) | done | test/proxy-contract.test.js | Validates request format, Claude/OpenAI response parsing, 429 retry, error handling |
| T2 contract validation layer | done | lib/validate-contract.js | Validates gameState shape, postMessage contracts, scoring/init contracts |
| Load/stress tests | done | test/load.test.js | 47-template bulk creates, rapid cycles, 100+ record performance, metrics throughput |

**Test count: 338 tests, 52 suites, 0 failures**

## P2 — Spec Compliance & Feature Completeness

| Item | Status | File(s) | Notes |
|------|--------|---------|-------|
| `all: true` bulk rebuild API | done | server.js:184-210 | Discovers templates from filesystem, schedules overnight |
| Slack format: show call count | done | lib/slack.js:29-33 | `"N calls"` instead of `gen=model` |
| Column name: `duration_s` alias | done | lib/db.js:93 | `SELECT total_time_s AS duration_s` |
| `commit.removed` handling | done | server.js:82 | Deleted spec files now trigger builds |
| T6 inputSchema.json generation | done | ralph.sh (Step 5) | LLM-generated JSON Schema with fallback; runs post-approval |
| E5 event schema validation | done | ralph.sh, test prompt | Test generation prompt includes postMessage payload schema validation |

## P3 — DevOps & Operations

| Item | Status | File(s) | Notes |
|------|--------|---------|-------|
| systemd service units | done | systemd/*.service | Server + worker |
| Docker Compose (Redis + proxy) | done | docker-compose.yml | |
| .env.example | done | .env.example | 39 env vars documented |
| Dockerfile (app) | done | Dockerfile | Multi-stage with non-root user, healthcheck, volume for SQLite |
| CI/CD: lint + test | done | .github/workflows/ci.yml | JS syntax check, bash syntax check, npm test |
| CI/CD: deploy | done | .github/workflows/deploy.yml | SSH deploy with secrets, manual trigger |
| Prometheus alert rules | done | monitoring/alerts.yml | 6 rules: failure rate, stuck builds, backlog, rate limits, worker down, high duration |
| Grafana dashboard | done | monitoring/grafana-dashboard.json | 5 rows: overview, duration, LLM, queue, per-game table |
| Deployment runbook | done | docs/deployment.md | First deploy, updates, troubleshooting (6 scenarios) |
| Log rotation | done | systemd/ralph-logrotate.conf | Daily rotation, 14 days retention, compress |
| Nginx reverse proxy config | done | nginx.conf | TLS, rate limiting, security headers, metrics restricted to internal IPs |
| KillMode=control-group: prevent orphaned Claude processes | done | systemd/ralph-worker.service | `KillMode=control-group` ensures all child processes (Claude subprocesses) are killed on worker stop/restart — prevents zombie LLM calls consuming API quota. Commit bd871ab. |
| Fix git pull branch: c_code → main | done | worker.js or deploy scripts | Auto-pull was targeting wrong branch `c_code`; fixed to `main`. Commit a8dc2d7. |

## P4 — Code Quality & Architecture

| Item | Status | File(s) | Notes |
|------|--------|---------|-------|
| Sentry v8 API alignment | done | lib/sentry.js:93-115 | Wrapper normalizes v8 Span to v7-compatible interface |
| Optional dependencies | done | package.json | `@google-cloud/logging` and `@sentry/node` moved to `optionalDependencies` |
| CLAUDE.md project context | done | CLAUDE.md | Architecture, commands, key files, code style, known constraints |
| llm.js status: kept for E3 | done | lib/llm.js | Documented as E3 migration target; used by contract/proxy tests |
| Express 5 migration | done | server.js, package.json | Express 5 handles async rejections natively; removed try/catch wrappers |
| ESLint + Prettier | done | .eslintrc.js, .prettierrc.json | Rules aligned with code style; devDependencies added |

## P8 — Build Reliability (from failure-analysis.md, 2026-03-20)

> Source: `docs/failure-analysis.md` — 223 builds analyzed. Warehouse gate + BullMQ stall fix address 63% of production failures.

| Item | Status | Files | Notes |
|------|--------|-------|-------|
| **Warehouse hygiene gate** | **done (2026-03-20)** | worker.js, lib/pipeline.js | Pre-build: deletes stale warehouse HTML for non-approved games before calling `runPipeline()`. Prevents generation bypass — root cause of 54% of production failures. Shipped in commit 8202a79. |
| **BullMQ stall prevention** | **done (2026-03-20)** | worker.js | Wired `onProgress` → `job.updateProgress()` every ~2 min (KillMode=control-group + heartbeat). BullMQ renews job lock on each call, preventing the 15 stall failures. Shipped in commit cc36e6c. |
| **Stale warehouse auto-delete: relax isInitFailure guard** | **done (2026-03-20)** | lib/pipeline.js | `isInitFailure()` refactored to exported function; ANY-match + passed===0; 14 regex patterns; 10 new unit tests; 399 tests pass; deployed 2026-03-20 (commit 5e7eaa0). |
| **Redis AOF persistence (Task #44)** | **done (2026-03-20)** | docker-compose.yml | `--appendonly yes` in Redis service command prevents BullMQ queue loss on restart. Verified on live server: `ralph-redis-1` container running with AOF active; `appendonlydir/` with `*.incr.aof` confirmed. See Lesson 45 in docs/lessons-learned.md. |
| **Step 1d: Page load smoke check** | **done (2026-03-20)** | lib/pipeline-utils.js, lib/pipeline.js | `runPageSmokeDiagnostic()` + `classifySmokeErrors()` added. Catches CDN 404s, `waitForPackages` timeout, init errors before test-gen runs. One regen attempt on fatal errors; throws immediately if still broken. 11 new tests; 428 total pass. See Lesson 46 in docs/lessons-learned.md. |
| **Global fix 0/0 regression: page broken after global fix** | **done (2026-03-21, commit 25ca9e6)** | lib/pipeline-fix-loop.js | Pattern observed in count-and-tap #393, #397 and word-pairs #395 global fix loop. Fix: (1) track pre-global-fix HTML and which batches were passing before each iteration; (2) detect when a previously-passing batch regresses to 0/0 after global fix; (3) roll back to pre-fix HTML and log a warning; (4) capture browser console errors via `capturePageConsoleErrors()` and inject as `brokenPageErrorContext` into next global fix prompt. 550 tests pass. Measure: count-and-tap #440 is the first build that will exercise this guard. |
| **BullMQ lock duration 90min → 5h + stalledInterval 30s → 5min** | **done (2026-03-21, commit 17b384d)** | worker.js | Root cause: `updateProgress()` does NOT renew BullMQ lock (incorrect prior comments); lock renewal is solely by LockManager timer. 90min lockDuration was insufficient for 5-iteration builds (5×25-30min = 125-150min total). stalledInterval=30s caused premature stall detection on any transient Redis blip → double-execution race ("Missing key for job N. moveToFinished"). Fix: lockDuration 90min→5h, lockRenewTime 20min→60min, stalledInterval default→5min. 550 tests pass. Prevents double-execution and premature stall detection on long builds. |

---

## P5 — Scalability & Intelligence (from spec E1-E10)

| Item | Status | Notes |
|------|--------|-------|
| E1 parallel batch runner | done | BullMQ concurrency=2 + rate limiter handles this |
| E2 smart retry escalation | done | ralph.sh, diagnosis mode on iteration 3+ |
| E3 migrate CLI to API | done | lib/pipeline.js, worker.js: dual-mode (bash/Node.js); opt-in via RALPH_USE_NODE_PIPELINE=1 |
| E4 warehouse-aware context | **done (2026-03-20)** | `getGameLearnings()` in db.js; prior game learnings injected into gen + fix prompts at Step 1 and Step 3; `formatGameLearningsBlock()` in pipeline.js; 447 tests pass; commit 316f32a |
| E11 parallel build generation | done | lib/llm.js per-model semaphore (RALPH_MODEL_CONCURRENCY); RALPH_CONCURRENCY already wired in worker.js |
| E12 parallel test generation | done | lib/pipeline.js | Promise.all() across 5 categories in Step 2b; saves 60-100s per build |
| E13 model routing (triage/global/learnings) | done | lib/pipeline.js | TRIAGE_MODEL (gpt-4.1-mini), GLOBAL_FIX_MODEL (claude-opus-4-6), LEARNINGS_MODEL (gpt-4.1-mini) |
| E14 hardware resource gate | done | lib/metrics.js, worker.js | lib/metrics.js system gauges (CPU/RAM/disk); worker.js gate before job start (RALPH_CPU_GATE, RALPH_RAM_GATE_MB); docs/scale-config.md |
| E15 distributed worker support | done | worker.js, docker-compose.scale.yml | RALPH_WORKER_ID, worker_id in builds table, docker-compose.scale.yml, docs/distributed.md |
| E6 caching / incremental runs | done | ralph.sh: check_cache/update_cache with sha256sum; gated by RALPH_ENABLE_CACHE=1 |
| E7 failure pattern database | done | lib/db.js, worker.js, server.js: failure_patterns table, categorization, /api/failure-patterns endpoint |
| E8 diff-based fix prompts | done | ralph.sh: sends only `<script>` section for HTML >20KB on iteration 2+ |
| E9 spec validation against warehouse | done | ralph.sh: validate_spec_against_warehouse checks part references exist |
| E10 deployment step | done | ralph.sh: versioned artifact dirs, latest symlink, manifest.json; gated by RALPH_DEPLOY_ENABLED=1 |

## P6 — Test Generation Quality

**Active slot state:**
| Field | Value |
|-------|-------|
| Current task | CR-016+CR-017 (agent a77b1111c7c88ce9b active, 2026-03-23): GEN-RESTART-RESET minimum reset set clarification ("For any field set during setupGame()/startGame(), reset it in restartGame()") + adjustment-strategy rendering fix (enforce visible initial phase container). After agent confirms commit, next from backlog: transitionScreen.show() object-API rule (GEN-TRANSITION-API-001 — NEVER string mode 'victory'/'gameover'; always object {buttons,title,subtitle,icons}) sourced from which-ratio P0 blank end screen. |
| Status | 1012/1012 tests pass (988 + 15 new TE-RES-001 tests + 9 others). GF fix impact: ~70% of game-flow failures addressed. Awaiting next build to measure lift from 63% baseline. |
| Waiting on | next approved build to confirm game-flow rate improvement |
| Blocked by | none |

| Item | Status | File(s) | Notes |
|------|--------|---------|-------|
| Categorized spec files (per category) | done | lib/pipeline.js | game-flow, mechanics, level-progression, edge-cases, contract — each with own fix loop |
| Pipeline-controlled boilerplate | done | lib/pipeline.js | LLM generates only test.describe() body; pipeline prepends sharedBoilerplate |
| Per-batch progressive fix loop | done | lib/pipeline.js | RALPH_CATEGORY_BATCH_SIZE=1 sequential; each batch up to MAX_ITERATIONS |
| Test case coverage enforcement | done | lib/pipeline.js | Prompt enforces exact N test() calls matching Step 2a output |
| DOM snapshot for test generation context | done | lib/pipeline.js | Headless Playwright captures actual element IDs/classes from running game; injected into test-gen prompts |
| Per-category pass rate tracking | done | lib/pipeline.js, ralph-report.json | category_results in report; identifies which category consistently fails |
| **TE-ANALYTICS-001: adjustment-strategy toBeVisible() failures** | **DIAGNOSED — 2026-03-23** | **Verdict: TEST BUG (historical, now resolved — game approved on build #385).** Root causes were two distinct test-gen bugs: (1) Early builds (#376–#378, failure_patterns IDs 3–14): test-gen generated `toBeVisible()` assertions on game-play elements WITHOUT first calling `startGame()` — violated R1/phase-gated visibility. (2) Build #381: `#mathai-transition-slot button` used as locator in mechanics/level-progression/edge-cases/contract tests — all timed out because slot is empty after startGame() — violated GF9/R2. Build #378 HTML was structurally correct (CDN packages loaded, `transitionScreen.show()` called, `data-phase=start_screen` synced properly). This is a pure test-gen bug, not an HTML bug. **Cross-game pattern confirmed:** Same `#mathai-transition-slot button` not-toBeVisible() failure is ACTIVE in recent failed builds: find-triangle-side #547, name-the-sides #553–556, keep-track #483, associations #513-pre — all contract.spec.js. The fix rules GF5/GF9/R2/R2/CT3 already exist in prompts.js; failure pattern persists because generated contract tests still use direct `#mathai-transition-slot button` selector instead of `skipToEnd()`. **Proposed fix:** Add a T1-style pre-test-gen check or strengthen GF9/CT3 with a BANNED-SELECTOR list. Route to Gen Quality as GEN-TE-001: add explicit banned-selector enforcement in buildTestGenPrompt() for `#mathai-transition-slot button` in contract/game-flow tests. |
| **TE-UNKNOWN-001: Residual unknown failure patterns** | **FIXED — 2026-03-23** | 28 rows with category='unknown' found (28 total across 10 games). 17 of 28 re-categorized using current categorizeFailure() logic: 6 → state (page.evaluate TypeError), 7 → rendering (toHaveText/toContainText/toContainText/expect(received)), 1 → infra (.spec.js module not found), 3 → interaction (life loss, correct answer, retry). 11 rows remain unknown — all have literal pattern='unknown' (noise rows from pre-GEN-ANALYTICS-001 extraction bug; no diagnostic value, cannot be re-categorized). DB updated directly on prod 2026-03-23. | Medium |
| **TE-CR-001: ARIA-001 regex multi-attribute ordering test** | **DONE (commit ad6686e)** | Code Review 2026-03-23: added tests in test/validate-static.test.js confirming ARIA-001 regex is order-independent — fires on `<div data-phase="results" id="feedback">` (data-phase first, no aria-live), does NOT fire when `aria-live` is present regardless of attribute order. Regex `[^>]+` before `(?:id|class)` skips any preceding attributes — confirmed order-independent. | Low |
| **TE-CR-002: categorizeFailure() tests cover new branches** | **DONE (commit b6551a5)** | Code Review 2026-03-23 (334b4c3): worker.test.js uses a local copy of the old 12-branch function — none of the 10 new branches (toHaveText, page.evaluate, net::err, locator., adjustment, .spec.js etc.) have test coverage. Rewrite tests to import `categorizeFailure` from worker.js directly and add cases for each new branch. | Medium |
| **TE-CR-003: ARIA-001 validator should warn on aria-live="assertive" for non-error-alert feedback** | **DONE (commit 61cbfc6)** | Code Review 2026-03-23 (d64f2f2): validate-static.js W6 block added ARIA-002 check — any element with `aria-live="assertive"` and no `role="alert"` emits a W-level warning. Tests in validate-static.test.js (describe "ARIA-002: aria-live=assertive without role=alert warning") cover: assertive+role=alert → no warning, assertive without role=alert → ARIA-002 warning, polite → no warning. All 138 tests pass. | Medium |
| Human-run Playwright traces as gold standard | planned | — | Record --trace from a correct human test run; use trace viewer output as ground truth for test generation |
| **Add unit tests for updateThreadOpener: 'running'/'APPROVED'/'FAILED' status branches** | **DONE — pre-existing (commit e905fd7)** | test/slack.test.js | 9 tests at slack.test.js line 465 cover: running/APPROVED/REJECTED/FAILED status emojis + labels, channel fallback, URL resolution, error handling, currentStep propagation. Confirmed 2026-03-23. |
| **Add unit test: PART-028 empty/whitespace-only `<style></style>` block not caught** | **DONE — 2026-03-23 (commit 6af64e9)** | Logic was already fixed in 6eea578 (`if (stripped.length === 0)` — no second predicate). Whitespace-only test `<style>   </style>` was missing — added in 6af64e9. 989/989 tests pass. |
| **Add assertion: results screen must have position:fixed or z-index>0** | **DONE (TE-RES-001, 2026-03-23, commit 121ac01)** | lib/pipeline-test-gen.js, test/game-flow.test.js | Source: UI/UX audit name-the-sides (UI-NS-004 test gap). Added `checkResultsScreenViewport()` helper to shared Playwright boilerplate (pipeline-test-gen.js). Asserts position:fixed OR coversViewport (top<=0 && height>=innerHeight). Fails with diagnostic message showing position, rectTop, height, coversViewport. Also added test/game-flow.test.js (15 tests) validating assertion logic: pass/fail/skip cases, coversViewport math, error message format. 1012/1012 tests pass. Deployed to server. |
| **Add assertion: page must have zero console PAGEERRORs during gameplay** | **done (2026-03-23, commit 1b783f1)** | lib/pipeline-test-gen.js | Source: UI/UX audit name-the-sides (UI-NS-007 test gap). `progressBar.update(-9)` fires PAGEERROR every round — tests pass while browser logs errors. Added `let pageErrors = [];` at module level in sharedBoilerplate; `page.on('pageerror')` now pushes to array; `test.afterEach()` throws if any errors collected. Deployed to server. |
| **Add assertion: data-lives guards for non-lives games** | **pending** | lib/prompts.js test gen | Source: UI/UX audit word-pairs #529 (UI-WP-005). `data-lives` is hardcoded to 0 for non-lives games. Pipeline test assertions on data-lives will fail or pass incorrectly. Test gen must check `gameState.totalLives === 0` and skip lives assertions for those games. |
| **Lives games must sync data-lives attribute on DOM element — getLives() reads DOM not gameState** | **pending** | lib/prompts.js gen prompt | Source: UI/UX audit addition-mcq-lives spec (F7). Games with totalLives > 0 that only track gameState.lives but never write data-lives to a DOM element cause getLives() to return undefined — all mechanics assertions on life decrements fail silently. Gen prompt must require: syncDOMState() sets setAttribute('data-lives', gameState.lives) on a visible DOM element. Complements the non-lives guard above. |
| **Add assertion: results screen covers full viewport** | **pending** | lib/prompts.js test gen | Source: UI/UX audits — 6 confirmed instances of results screen not being position:fixed (quadratic-formula, soh-cah-toa, right-triangle-area, word-pairs, name-the-sides, find-triangle-side). Add Playwright assertion: at data-phase='results', results-screen must have offsetTop=0 and height >= window.innerHeight, OR position:fixed. |
| **Add assertion: canvas max-width on 480px viewport** | **pending** | lib/prompts.js test gen | Source: UI/UX audit right-triangle-area #543 (UI-RTA-002). Canvas hardcoded at 500px causes overflow on 375-480px screens. Add Playwright viewport resize to 480px + assert canvas.offsetWidth <= 480. |
| **GEN-TRANSITION-API-001: transitionScreen.show() MUST use object API — NEVER string mode** | **pending (TE handoff 2026-03-23)** | lib/prompts.js gen prompt | Source: which-ratio P0 — blank end screen caused by `transitionScreen.show('victory', ...)` (string mode). CDN only supports object API: `transitionScreen.show({buttons, title, subtitle, icons})`. Add explicit gen rule: BANNED — `transitionScreen.show('victory')`, `transitionScreen.show('gameover')`, any string first arg. REQUIRED — always use object form. Add T1 static check to catch string-mode calls before test gen. |
| Smoke-regen: recurring "missing #gameContent" after regen (disappearing-numbers, associations, kakuro) | **done (2026-03-21, commit 2666e36)** | lib/pipeline.js, lib/prompts.js | Root cause: LLM omitted `slots` wrapper in ScreenLayout.inject() call — options passed directly are silently ignored, #gameContent never created. Fix: CDN_CONSTRAINTS_BLOCK updated with exact CORRECT/WRONG example; smoke-regen prompt now includes correct call snippet; gen prompt Rule 2 uses exact call. See Lesson 69. |
| `data-testid` attributes in gen prompt | done | lib/pipeline.js | Rule 15: LLM adds data-testid to all interactive/observable elements; test gen uses them as primary selectors |
| Force-regenerate missing test categories | done | lib/pipeline.js | Per-category check: only missing/empty categories regenerate; existing valid specs kept |
| Review rejection → targeted fix loop | done | lib/pipeline.js | REJECTED triggers up to 2 targeted HTML fix iterations using rejection reason, then re-reviews |
| Autonomous spec → APPROVED pipeline | done | lib/pipeline.js, worker.js | FAIL/REJECT → triage → targeted fix → retest → re-review loop, fully autonomous |
| Generic pipeline (all game types) | done | lib/pipeline.js, validate-static.js, validate-contract.js | Test gen uses DOM snapshot for selectors; boilerplate is game-agnostic; CDN contract patterns accepted |
| Full error output in fix prompts | done | lib/pipeline.js collectFailures() | Full error message (600 char limit) vs first line only; fix LLM sees actual vs. expected values |
| Deterministic pre-triage | done | lib/pipeline.js | Skip triage LLM for __ralph undefined, visibilityState, pointer-events patterns — saves 30-40% of triage calls |
| E8 script-only fix (iteration 2+) | done | lib/pipeline.js | Sends only <script> sections for large HTML on iteration 2+; merges fix back into full HTML |
| Architecture C: global cross-batch fix loop | done | lib/pipeline.js Step 3c | Global fix after per-batch loops; collects all failing batches for cross-category root cause |
| Contract auto-fix at Step 1b | done | lib/pipeline.js | lib/pipeline.js: contract errors trigger FIX_MODEL call before test loop |
| Category results in review prompt | done | lib/pipeline.js | lib/pipeline.js: game-flow 0% + overall <70% fails before review; category scorecard in review prompt |
| Review prompt consolidation | done | lib/pipeline.js | REVIEW_SHARED_GUIDANCE const shared by early-review, re-review, and final review; eliminates drift |
| Spec-derived fallbackContent | done | lib/pipeline.js | extractSpecRounds() parses spec markdown tables/lists when DOM snapshot rounds are empty; skip-conditions added for PART-xxx metadata rows (build 232 / lesson 44) |
| Slack log restructure (Block Kit) | done | worker.js, lib/slack.js | Block Kit templates with dividers, → Next: narration, pipeline-architecture.md |
| Step 0 spec pre-validation | done | lib/pipeline.js | validateSpec() checks: short spec, missing heading, missing mechanics/scoring/CDN sections; hard fail on errors, warnings to Slack |
| Build auto-retry (RALPH_AUTO_RETRY) | done | worker.js, lib/db.js | Requeues builds scoring 0/total once; retry_count in DB prevents chains; gated by env var |
| Iteration HTML URL audit trail | done | lib/db.js, worker.js | iteration_html_urls column tracks all fix snapshots per build; count shown in Slack summary |
| LLM call cost tracking (per-build USD estimate) | done | lib/llm.js, lib/pipeline.js, lib/slack.js, lib/db.js | Token accumulation per model; MODEL_COSTS map; total_cost_usd in DB + Slack |
| Truncated HTML detection + generation retry | done | lib/pipeline.js, lib/validate-static.js | isHtmlTruncated() checks </html>/script tags; retries up to 3x; T1 now errors on missing </html> |
| Auto-delete stale warehouse HTML on 0% game-flow iter 1 | done | lib/pipeline.js | Detects init-failure pattern + warehouse HTML source → deletes + regenerates; prevents 3-batch waste |
| CDN URL constraint + auto-fix in generation | done | lib/pipeline.js | Rule 18 in gen prompt; post-gen cleanup replaces cdn.mathai.ai → cdn.homeworkapp.ai |
| FeedbackManager.init() purge from 20 specs (Lesson 67) | done (2026-03-21, commit c2b6a36) | warehouse/templates/*/spec.md | `FeedbackManager.init()` bullets in 20 spec DOMContentLoaded examples replaced with explicit DO-NOT-CALL comment. Root cause of associations 3x smoke-check failure and 48+ CDN games generating blocking audio popup. |
| CDN classification fix: ScreenLayout.inject() games (Lesson ~) | done (2026-03-21, commit 54dd4b7) | lib/pipeline-test-gen.js | `hasTransitionSlot` / `hasSlot` extended to detect `ScreenLayout.inject`, CDN package URL, and `TransitionScreenComponent` — catches CDN games whose slot is injected at runtime, not present in raw HTML. count-and-tap was misclassified as Non-CDN causing all game-flow tests to timeout. |
| Conditional beforeEach post-processing | done | lib/pipeline.js | Post-processing respects hasTransitionSlot; ${transitionSlotId} hallucination cleanup |
| Orphaned build auto-cleanup at worker startup | done | worker.js, lib/db.js | cleanupOrphanedBuilds() marks running builds failed at startup; getRunningBuilds() in db.js |
| waitForPackages T1 static check + gen/fix constraints | done | lib/validate-static.js, lib/pipeline.js | T1 validates 10000ms timeout + throw; rules 19/20/21 in gen prompt; all fix prompts updated |
| sound.register() T1 static check | done | lib/validate-static.js | T1 errors on FeedbackManager + sound.register() usage; gen/fix/global prompts forbid it |
| **FeedbackManager.sound.playDynamicFeedback ban (PART-011-SOUND / GEN-113B)** | **done (2026-03-22, commit 26fcfb6)** | lib/validate-static.js, test/validate-static.test.js | T1 check for wrong FeedbackManager.sound namespace. Root cause of count-and-tap build #471 round deadlock. 791 tests pass. |
| CDN window exposure T1 static check + gen rules | done | lib/validate-static.js, lib/pipeline.js | T1 checks window.endGame assignment when DOMContentLoaded present; rule 21 in gen prompt |
| endGame double-call guard in gen prompts | done | lib/pipeline.js | Rule 20: if(gameState.gameEnded)return pattern; added to all fix prompts |
| Static re-validation after Step 1b fix | done | lib/pipeline.js | Re-runs runStaticValidation() after LLM fix; logs remaining errors; emits partial event if not fully fixed |
| window.gameState shape in DOM snapshot | done | lib/pipeline.js | captureGameDomSnapshot() captures actual property names/types from window.gameState; injected into test-gen so LLM uses real data shapes not guesses |
| Spec-derived test generation hints | done | lib/pipeline.js | extractTestGenerationHints() detects multi-cell/timed-flash/learn-recall/sequential-step patterns from spec; injects targeted warnings into test-gen prompt |
| Size-drop continue-to-iter-2 on truncation | done | lib/pipeline.js | When iter 1 full-HTML fix returns near-empty (>90% shrink), continue to iter 2 (E8 script-only) instead of breaking fix loop |
| Behavioral transcript (Step 2.5b) | done | lib/pipeline.js | captureBehavioralTranscript() fires game_init, observes correct/wrong interactions, captures postMessage — injected into test-gen prompt as ground truth |
| T1: ScreenLayout.inject() presence check | done | lib/validate-static.js | Errors if ScreenLayout referenced but .inject() never called — CDN slot never renders otherwise |
| T1: initSentry() order check | done | lib/validate-static.js | Errors if initSentry() appears before waitForPackages() — throws before SDK loads → ScreenLayout blocked |
| T1: Debug functions on window check | **done — REMOVED (2026-03-21, commit d827777)** | lib/validate-static.js | T1 error check for window.debugGame removed — check was wrong. All approved games have window.debugGame; review model does NOT reject it. PART-012 requires debug functions on window. T1 now only warns if debug fns are missing from window. |
| Multi-game scale validation | in-progress | warehouse/templates/ | 47 games queued; 1 APPROVED (match-the-cards), visual-memory + 4 more in queue with latest fixes |
| Reduce review rejection rate: debug-window contradiction — final fix | **done (2026-03-21, commit d827777)** | lib/prompts.js, lib/validate-static.js | Final resolution: all approved games (simon-says, doubles, connect) have window.debugGame; review model does NOT reject it. Commits cc8eb56 + 4e0bf96 went the wrong direction (said NEVER assign to window). d827777 corrected: T1 debugWindow error check removed; CDN_CONSTRAINTS_BLOCK flipped to MUST assign to window; gen prompt PART-012 rule corrected. Eliminates T1 false-failures on correct HTML. |
| Reduce review rejection rate: remaining patterns | **done (2026-03-21)** | lib/prompts.js, lib/validate-static.js | 3 false-rejection patterns fixed: (1) RULE-006 added — endGame() is correct termination regardless of phase string; (2) RULE-007 added — isActive/gameEnded guard equivalence, T1 check warns when isActive missing from gameState init; (3) RULE-008 added — TransitionScreen.show() await is optional at review; T1 check 5h warns on unawaited show() calls; Rules 25+26 added to gen prompt. 4 new tests; 554 total pass. Goal: eliminate these 3 classes from review rejections. |
| HTML generation token truncation on large specs (Lesson 53) | **done (2026-03-20)** | lib/pipeline.js | All 4 HTML generation call sites updated to `{ maxTokens: 32000 }`. Root cause: large specs (bubbles-pairs 64KB, interactive-chat 59KB) exceeded 16K output token default, truncating mid-script. Commit a8392bc. |
| RALPH_LLM_TIMEOUT config drift → worker stall (Lesson 54) | **done (2026-03-20)** | /opt/ralph/.env (server config) | Production had RALPH_LLM_TIMEOUT=1200 (4× documented default). Static-fix calls hung 20+ min, stalling 40+ queued builds (futoshiki build 296 stalled 23 min). Fixed by setting RALPH_LLM_TIMEOUT=300 in production .env. AbortController in llm.js is correctly wired — pure config issue. |
| `logger is not defined` crash on pipeline init | **done (2026-03-20)** | lib/pipeline.js | `runPageSmokeDiagnostic()` was passed `logger` (undeclared) instead of `log` (local alias). Fixed commit 14e22e3. Affects free-the-key, crazy-maze, connect, colour-coding-tool — all requeued. |
| VisibilityTracker template wrong in CDN_CONSTRAINTS_BLOCK | **done (2026-03-20)** | lib/prompts.js | Replaced DOM-element API with correct options-object pattern: onInactive/onResume + `{ fromVisibilityTracker: true }` + popupProps. Propagates to buildFixPrompt/buildGlobalFixPrompt/buildTargetedFixPrompt. Commit 827f44d. |
| popup-backdrop teardown: Rule 24 + CDN constraint (Lesson 58) | **done (2026-03-20, commit 7428526)** | lib/prompts.js | VisibilityTracker's #popup-backdrop stays as full-screen overlay after popup dismissal — intercepts all pointer events, causing click timeouts at iter 2 (builds 306, 310). Fix: Rule 24 in buildGenerationPrompt requires explicit `bd.style.display='none'; bd.style.pointerEvents='none'` in onResume and restartGame(). POPUP-BACKDROP TEARDOWN constraint added to CDN_CONSTRAINTS_BLOCK for fix prompts. Lesson 58 documented. |
| Passing-test-protection regex fix in fix-loop prompt builder | **done (2026-03-20, commit 275d14f)** | lib/pipeline-fix-loop.js | `passingContext` and `priorBatchContext` were silently broken: regex `^\}` in multiline mode matched only top-level `}` but Playwright test blocks have indented `  });` — so `passingTestBodies` was always empty and the "MUST keep passing" LLM constraint was a no-op. Fix: drop body extraction entirely; switch to name-only lists (`"- test name\n"` per passing test; `"batchLabel: name1, name2"` per prior batch). Regression context now actually works — prevents class of iter-3 regressions (4 passing → 0 after fix). |
| extractGameFeatures() unit tests (non-standard lifecycle) | **done (2026-03-20, commit 14d5d7f)** | test/pipeline-utils-game-features.test.js | 34 unit tests covering unlimited-lives, timer scoring, multi-level, accuracy scoring, single-round, and learn/recall two-phase variants. Supports R&D measurement of non-standard lifecycle test gen. 584 total tests pass. |
| Page console error capture on 0/0 batch failures | **done (2026-03-21, commit f6dc634)** | lib/pipeline-fix-loop.js | `capturePageConsoleErrors(htmlPath)` headless Chromium captures JS errors from broken page (0/0 tests ran). `lastBrokenPageErrors` state injected into next fix prompt as `brokenPageErrorContext`. Deployed to server (worker restart activates). Expected: reduce 0/0 → "gave up after X iterations" build failures by giving LLM a signal when page crashes. |
| 'start_screen' vs 'start' phase-name normalization in test gen | **done (2026-03-21, commit 32785d3)** | lib/prompts.js | `extractPhaseNamesFromGame()` now normalizes phase names through the same mapping table as `syncDOMState()` before injecting into GF1 block (game_over→gameover, game_complete→results, start_screen→start, game_init→start, game_playing→playing). Added CRITICAL warning to GF1 prompt block with explicit raw→canonical mapping table. 550 tests pass. Expected: eliminate phase-name mismatch timeouts in game-flow (root cause of 2 non-first-attempt approvals). |
| Sentry.Integrations.CaptureConsole constructor error in initSentry() | **done (2026-03-21, commit 14b15d1)** | lib/prompts.js, lib/validate-static.js | face-memory build failed at Step 1d: `TypeError: Sentry.Integrations.CaptureConsole is not a constructor`. Fixed: initSentry() template in CDN_CONSTRAINTS_BLOCK updated to remove the deprecated `Sentry.Integrations.CaptureConsole` usage; T1 static check added to flag `Sentry.Integrations.CaptureConsole` in generated HTML. Prevents Step 1d smoke-check failures for all CDN games that call initSentry(). |
| count-and-tap recurring null error_message failures (new pattern) | **done (2026-03-22, commit c48fc15)** | lib/db.js, server.js | Root cause: `failBuild()` stored NULL when called with null/undefined/empty errorMessage (SQLite accepted the NULL). Also: `queue.add()` after `createBuild()` had no try/catch — Redis failure left orphaned `queued` DB record forever. Fixes: (1) `failBuild()` now normalizes null/undefined/empty to sentinel string `'build failed: no error message recorded'`; (2) `queue.add()` wrapped in try/catch that calls `db.cancelBuild()` on enqueue failure; (3) 3 new db.test.js cases. 609 tests pass. Success: zero null error_message failures going forward. |
| **TimerComponent is not defined — CDN component hallucination** | **done (2026-03-21, commit 48e9992, Lesson 87)** | lib/prompts.js, lib/validate-static.js | Server DB shows builds 420 (truth-tellers-liars) and 422 (visual-memory) both failed Step 1d with `ReferenceError: TimerComponent is not defined`. LLM generates HTML that references `TimerComponent` — not in the standard CDN package bundle. Fix: (1) determine if TimerComponent is a real CDN component or a hallucination; (2) if hallucination, ban it in gen prompt CDN_CONSTRAINTS_BLOCK and add T1 error check for `TimerComponent` references in HTML; (3) add `TimerComponent is not defined` to classifySmokeErrors() fatal patterns so smoke-regen prompt explains the fix; (4) if real component, add correct CDN script URL to allowed set. Success criterion: zero `TimerComponent is not defined` Step 1d failures in next 5 CDN builds. |
| **PART-017=YES: spec-provided audio/sticker URLs use wrong domain** | **done (2026-03-21, commit 3002003, Lesson 89)** | lib/prompts.js | CDN_CONSTRAINTS_BLOCK domain rule "EVERY URL must use storage.googleapis.com" was too broad — LLM applied it to spec-provided cdn.mathai.ai audio/sticker asset URLs in PART-017=YES games, causing early-review rejection. colour-coding-tool #441 failed for this reason. Fixed: domain rule scoped to CDN package script tags only; explicit exception for spec-provided asset URLs. 550 tests pass. |
| **ScreenLayout.inject() silent empty #gameContent — guard added** | **done (2026-03-21, commit db6f8a4, Lesson 88)** | lib/prompts.js | CDN games where ScreenLayout.inject() ran but created empty #gameContent at runtime produced blank page with no error. Added mandatory post-inject guard in CDN_CONSTRAINTS_BLOCK + gen prompt template: `if (!document.getElementById('gameContent')) { throw new Error('ScreenLayout.inject() did not create #gameContent'); }` — converts silent failures into explicit errors caught by window.__initError. Enables smoke-regen to diagnose the root cause. 550 tests pass. |
| **Persistent CDN 404 after smoke-regen — wrong URL path not domain** | **done (2026-03-21, commit d06ad43)** | lib/pipeline.js, lib/prompts.js | Root cause (different from hypothesis): LLMs used `unpkg.com/@mathai/*` URLs (packages don't exist on unpkg — 404) and `cdn.homeworkapp.ai/cdn/components/web/*` (403) — both invisible to old `checkCdnScriptUrls()`. Fix: (1) `fixCdnPathsInFile()` removes bad URL patterns and injects canonical three-package block (storage.googleapis.com/test-dynamic-assets); (2) `checkCdnScriptUrls()` now HEAD-checks `unpkg.com/@mathai/` URLs; (3) smoke-regen prompt has explicit KNOWN VALID/INVALID CDN URL list. 7 new tests; 606 total pass. Deployed without restart. Builds 505 (memory-flip) and 506 (two-player-race) queued to verify. |

---

## R&D

> One task always active. Target: highest-leverage improvement for reliability / availability / power / scalability.

**Active slot state:**
| Field | Value |
|-------|-------|
| Current task | CR-016 + CR-017 RESOLVED — GEN-TIMER-ONLY and GEN-HIDE-SHOW corrected in both CDN_CONSTRAINTS_BLOCK and buildGenerationPrompt(). 997/997 tests pass. Deployed 2026-03-23. |
| Status | 997/997 tests pass. CR-016 DONE (GEN-TIMER-ONLY WRONG example fixed). CR-017 DONE (GEN-HIDE-SHOW soh-cah-toa #544 citation retracted). |
| Waiting on | none |
| Blocked by | none |

| Task | Status | Hypothesis | Expected Impact |
|------|--------|-----------|-----------------|
| **CR-016: Fix GEN-TIMER-ONLY WRONG example** | **DONE — 2026-03-23** | WRONG example showed timer.start() AFTER renderRound() — not the race condition. Fixed to timer.start() BEFORE renderRound() in both CDN_CONSTRAINTS_BLOCK and rule 44. RIGHT updated to show currentQuestion set before timer.start(). 997 tests pass. Deployed. | Medium: LLM previously saw a non-buggy WRONG pattern — risk of ignoring the rule |
| **CR-017: Retract GEN-HIDE-SHOW soh-cah-toa #544 citation** | **DONE — 2026-03-23** | soh-cah-toa #544 hide() uses querySelector(selector) (not element-expected) — no TypeError occurred. Rule is valid ONLY for element-expected helpers (el.style.display / el.classList.add). Updated WHY clause and source in both CDN_CONSTRAINTS_BLOCK and rule 41. 997 tests pass. Deployed. | High: factually wrong evidence undermines trust in the rule |
| **GEN-CR-001: Tighten GEN-120 — default aria-live to polite** | **DONE — 2026-03-23** | Code Review 2026-03-23: GEN-120 now mandates `aria-live="polite"` as the only default. `assertive` demoted to parenthetical sub-note. Commit d64f2f2. 827 tests pass. Deployed. | n/a |
| **GEN-CR-002: Narrow `expect\(` branch in categorizeFailure()** | **DONE — 2026-03-23 (commit d60ccc9)** | Code Review 2026-03-23 (334b4c3): Narrowed to `expect\(received\)` only (Playwright's own assertion diff format). `categorizeFailure('Expected round counter to increment')` → `interaction` (not rendering) — false-positive eliminated. Verified in worker.test.js line 600–607. Note: short-form Playwright diff (`Expected: N, Received: N`) still falls to unknown (CR-009 in code review log — separate backlog item). | Low |
| **GEN-CR-003: Define "error alert" precisely in GEN-120 assertive sub-note** | **DONE — 2026-03-23 (commit d60ccc9)** | Code Review 2026-03-23 (d64f2f2): Sub-note now reads "assertive is ONLY correct for a div with `role='alert'` AND id or class containing 'error' — NEVER use assertive for correct/incorrect answer feedback." Both CDN_CONSTRAINTS_BLOCK (~L122) and buildGenerationPrompt rule 30 (~L553) updated in sync. Note: AND is more restrictive than WCAG (OR would be correct) — CR-008 in code review log tracks this as a follow-up. | Low |
| **GEN-CR-004: Add GEN-MCQ-PHASE + GEN-MCQ-TIMER to buildCliGenPrompt** | **DONE — pre-existing (commit ae3c2ad)** | Already implemented in ae3c2ad. Rules 33 (GEN-MCQ-PHASE) and 34 (GEN-MCQ-TIMER) at prompts.js lines 814 and 823 in buildCliGenPrompt(). Confirmed via agent inspection 2026-03-23. | Medium: prevents MCQ game-flow failures on CLI-generated games |
| **GEN-CR-005: generateSpec() — warn/guard when templateSpecId is null** | **DONE — 2026-03-23 (commit 3508ef4)** | lib/session-planner.js: WARN logged when templateSpecId is null/empty; throws Error when dryRun=false — prevents silent LLM hallucination. dryRun=true returns skipped result for safe introspection. 988/988 tests pass. Deployed. | Medium: prevents silent LLM spec hallucination on no-template games |
| **GEN-CR-006: Fix planSession() bloomTarget default — 6 → 3** | **DONE — 2026-03-23 (commit 268b27b)** | lib/session-planner.js line 302: default changed from 6 to 3 + clarifying comment added. parseGoal() clamps to max 3; direct callers now get consistent 3-game Apply-level session. 988/988 tests pass. Deployed. | Medium: session length consistency |
| **CR-018 [MED]: Standardize touch-target measurement method in M16** | **DONE — 2026-03-23 (commit 49fa3f1)** | Standardized all three M16 selectors to `getComputedStyle(el).minHeight`. Policy: GEN-UX-002 mandates the CSS `min-height` property — a button achieving 44px via padding only has not followed the rule. `.choice-btn` changed from `getBoundingClientRect().height` → `getComputedStyle(el).minHeight`. `.option-btn` changed from `getBoundingClientRect().height` → `getComputedStyle(el).minHeight`. `.answer-btn` already correct. 988/988 tests pass. Deployed. | Medium: eliminates false-pass for padding-only sizing |
| **CR-008: GEN-CR-003 assertive condition AND → OR** | **DONE — 2026-03-23 (commit 188bbaa)** | Fixed in prompts.js CDN_CONSTRAINTS_BLOCK + buildGenerationPrompt() rule 30: "role=alert OR id/class containing 'error'". Deployed 2026-03-23. | Medium |
| **CR-009: Short-form Playwright diff undetected in categorizeFailure()** | **DONE — 2026-03-23 (commit 188bbaa)** | Added second branch to categorize-failure.js: `/expected:\s*\d+.*received:\s*\d+/i` → 'rendering'. Deployed 2026-03-23. | Medium |
| **CR-012: GEN-PHASE-001 phase table function alias missing for non-MCQ** | **DONE — 2026-03-23 (commit 188bbaa)** | Added parenthetical to prompts.js phase table: "renderRound() or equivalent (renderGrid, loadPuzzle, initBoard, etc.)". Deployed 2026-03-23. | Low-Medium |
| **CR-013: GEN-WINDOW-EXPOSE missing from buildCliGenPrompt()** | **DONE — 2026-03-23 (commit 188bbaa)** | Added GEN-WINDOW-EXPOSE compact rule to buildCliGenPrompt() after GEN-MCQ-TIMER. Deployed 2026-03-23. | Medium — CRITICAL CLI path |
| **GEN-ANALYTICS-001: Reduce 'unknown' failure pattern bucket** | **DONE — 2026-03-23** | Added 10 new regex branches to `categorizeFailure()` in worker.js: Playwright assertion failures (toHaveText/toContainText/toBeVisible) → rendering; page.evaluate errors → state; .spec.js missing → infra; navigation/crash → infra; interaction/completion short-name patterns; generic error: prefix fallback. Also fixed /i flag redundancy (desc is already lowercased). All 827 tests pass. Deployed commit 334b4c3. Historical "unknown" rows (25) are pre-filter artifacts — new occurrences now correctly categorized. | Largest single failure sink covered. Every new failure → named category for targeted fix prompts and per-category pass rate tracking. |
| **CODE-001: resolveFailurePattern() resolve rate is 0% across all categories** | **DONE — 2026-03-23 (commit c688f0e)** | resolveFailurePattern() existed in lib/db.js but was never called on the approval path. Added call in worker.js: after APPROVED, fetch all open failure_patterns for the game via getFailurePatterns() and call resolveFailurePattern() for each. 827/827 tests pass. Deployed. | Pattern DB no longer grows unboundedly after approval — cross-build pattern injection stays signal-dense. |
| **MCP server audit — identify useful servers for learning pipeline** | **pending — priority** | Review https://github.com/punkpeye/awesome-mcp-servers and curate a doc of MCP servers relevant to: (1) education / curriculum content retrieval, (2) pipeline ops (DB, Redis, GCP), (3) web research for Gen Quality / Test Engineering. Context7 already installed. Goal: 2-3 additional MCP servers that give agents better tools than WebFetch alone for their domain. Output: `docs/mcp-servers.md` with install instructions + use cases per slot. | Agents with domain-specific MCP tools (e.g. a math knowledge graph, a real-time Playwright docs server) make better decisions faster. Compounding leverage across all 7 slots. |
| **Ship results screen position:fixed rule** | **DONE — 2026-03-23 (GEN-UX-001)** | 7 confirmed instances: name-the-sides, find-triangle-side, quadratic-formula #546, soh-cah-toa #544, right-triangle-area #543, word-pairs #529, real-world-problem #564. GEN-MOBILE-RESULTS shipped: CDN_CONSTRAINTS_BLOCK line 120 + buildGenerationPrompt() rule 31 (lines 529–534) in lib/prompts.js. Rule: `position:fixed; top:0; left:0; width:100%; height:100%; z-index≥100` on all end-screen overlays. | Eliminates results-screen off-screen bug that appears in every approved game; affects UX of every game completion. |
| **Post-strip zero-test guard** | **DONE — 2026-03-23 (fc4e1d2)** | P1 fix: after banned-selector strip in pipeline-test-gen.js, check if any `test()` calls remain — if not, delete the file for regen. Exports `hasRunnableTestCalls()`. 5 new tests. Deployed. | Prevents silent zero-test categories after GEN-TE-001 strip. |
| **Ship ARIA-001 as hard gen rule — inline feedback panels** | **DONE — 2026-03-23 (commit 3508ef4)** | lib/prompts.js: CDN_CONSTRAINTS_BLOCK (line ~122) and buildGenerationPrompt rule 30 (line ~548) now include explicit ARIA-001 INLINE PANELS structured block listing: #faded-feedback, #practice-feedback, #feedback-area, #feedback-message, #answer-feedback, #result-feedback, #skip-note, #hint-text, .feedback-message, plus catch-all "any div whose id/class contains substring 'feedback'". Also added `#faded-feedback` WRONG example. Addresses 7+ confirmed UI/UX audit instances. 988/988 tests pass. Deployed. | Screen reader users get zero feedback on answer selection in every approved game. |
| **Ship 44px min-height rule on all interactive buttons** | **DONE — 2026-03-23 (GEN-UX-002)** | 7 confirmed instances: which-ratio, name-the-sides, count-and-tap, find-triangle-side, quadratic-formula, soh-cah-toa, real-world-problem #564. GEN-TOUCH-TARGET shipped: CDN_CONSTRAINTS_BLOCK line 121 + buildGenerationPrompt() rule 32 (lines 536–539) in lib/prompts.js. Rule: `min-height:44px; min-width:44px` on all interactive buttons/options. | Affects touch usability on every mobile game. |
| **Canvas responsive sizing rule** | **DONE — 2026-03-23 (commit 9b5b7fe)** | GEN-CANVAS-001: Added to CDN_CONSTRAINTS_BLOCK + buildGenerationPrompt() rule 39. Canvas elements MUST include `style="max-width: 100%; height: auto;"`. Ban: hardcoded `width="500"` with no responsive CSS. Source: UI/UX audit right-triangle-area #543. 988/988 tests pass. Deployed. | Prevents horizontal scroll on canvas-based games. |
| **CSS variable token consistency rule** | **DONE — 2026-03-23 (commit 8221ae2, GEN-CSS-TOKENS rule 40)** | CDN_CONSTRAINTS_BLOCK + buildGenerationPrompt rule 40: ban `--mathai-green`, `--color-red`, `--color-orange`; mandate `--mathai-success`, `--mathai-error`, `--mathai-warning`. Deployed. | Prevents invisible feedback text when CSS vars are undefined. |
| **data-testid must match element id rule** | **pending** | `data-testid` value must exactly match the element's `id` attribute (or use a consistent `btn-` prefix). Ban divergence between testid and functional id. Source: UI/UX audit word-pairs #529. | Prevents test selector inconsistencies. |
| **Standardise Sentry SDK to v10.23.0 three-script pattern** | **pending** | CDN_CONSTRAINTS_BLOCK must mandate Sentry SDK version 10.23.0 with three scripts: bundle.tracing.replay.feedback.min.js + captureconsole.min.js + browserprofiling.min.js. Ban v7 single-bundle. Source: UI/UX audit word-pairs #529. | Ensures consistent Sentry behaviour across all games. |
| **hide()/show() helpers must receive DOM elements not strings** | **DONE — 2026-03-23 (commit 8221ae2, GEN-HIDE-SHOW rule 41)** | CDN_CONSTRAINTS_BLOCK + buildGenerationPrompt rule 41: WRONG/RIGHT examples; NEVER pass string — always pass `document.getElementById('id')`. Deployed. | Prevents silent TypeError when string passed to classList/style. |
| **ProgressBarComponent slotId must use options object** | **DONE — GEN-UX-003 (25bdad0, 2026-03-23)** | See line below (GEN-UX-003 entry) — same rule, shipped earlier. 6 confirmed instances. | Ensures progress bar renders in the correct CDN slot. |
| **MCQ / timed games must declare data-phase state machine with syncDOMState() at each transition** | **pending** | Spec must explicitly declare: showStartScreen() sets gameState.phase='start_screen'; startGame() sets gameState.phase='playing'; endGame() game-over path sets gameState.phase='game_over'; endGame() victory path sets gameState.phase='results'. Each must call syncDOMState() immediately after. Without this, LLM omits syncDOMState() calls — 22% of game-flow iter-1 failures. Sources: addition-mcq-blitz spec (F3), addition-mcq-lives spec (F1), addition-mcq spec (F2) — 3 confirmed MCQ spec instances. Add to gen prompt rule 22 (SYNCDOMESTATE CALL SITES): enumerate MCQ phase transitions explicitly. | Targets 22% game-flow failure class for MCQ game type. |
| **timer.start() must only appear in renderRound() — never in setupGame()** | **pending** | setupGame() calling timer.start() then renderRound() calling timer.reset() creates a race: if timer fires onEnd before renderRound() reaches it, handleTimeout() runs with currentQuestion===null. Fix: remove timer.start() from setupGame(); only call timer.start() inside renderRound(). Add to CDN_CONSTRAINTS_BLOCK TIMER LIFECYCLE rule. Source: addition-mcq-blitz spec audit. | Eliminates handleTimeout(null) crash on slow devices at game start. |
| **All signalCollector calls must precede seal() — never call recordViewEvent after seal()** | **pending** | showResults() calling signalCollector.recordViewEvent() AFTER signalCollector.seal() in endGame() records into a finalised payload and silently loses the event. Fix: add to CDN_CONSTRAINTS_BLOCK: "seal() must be the last SignalCollector call in endGame(); move any recordViewEvent calls to before seal()." Source: addition-mcq-blitz spec audit. | Prevents silent analytics data loss at results screen in every game. |
| **endGame() for lives games must explicitly branch game-over vs victory TransitionScreen** | **pending** | Lives games need two different TransitionScreen.show() calls: game-over path (lives=0) with stars=0 and "Try again!" button vs victory path (lives>0) with stars=1-3 and "Play again!" button. Spec must show explicit if/else branching. Without it, LLM may generate unified path with identical content for both outcomes. Source: addition-mcq-lives spec (F5). | Prevents game-over showing victory stars; ensures 0-star display on game-over. |
| **GEN rule: restartGame() must reset ALL gameState fields — not just phase/isActive/gameEnded** | **pending — SHIP NOW (1st confirmed browser instance)** | lib/prompts.js | Source: UI/UX browser audit quadratic-formula #546 (UI-QF-NEW-001). restartGame() resets only `gameEnded`, `isActive`, `phase` — not `lives`, `currentRound`, `score`, `attempts`, `events`, etc. Second game session starts with wrong state (data-lives="2", data-round="3" at start). Fix: gen prompt rule — "restartGame() must reset ALL gameState fields to their initial values: `lives = gameState.totalLives; currentRound = 0; score = 0; events = []; attempts = [];` then call `showStartScreen()`." Add to CDN_CONSTRAINTS_BLOCK. | Prevents second-session start with wrong lives/round state. |
| **restartGame() for timer games must destroy and recreate TimerComponent** | **pending** | restartGame() calling timer.reset() without timer.destroy() + new TimerComponent() risks stale onEnd callbacks from the previous game firing during a fresh playthrough. Spec must outline: (1) timer.destroy(), (2) timer = new TimerComponent('timer-container', {...onEnd: handleTimeout}), (3) gameState reset, (4) showStartScreen(). Add to gen prompt TIMER LIFECYCLE rule. Source: addition-mcq-lives spec (F6). | Prevents stale timer callbacks triggering game-over on restart. |
| **Lives games must expose data-lives on DOM element via syncDOMState()** | **pending** | The test harness getLives() helper reads data-lives from the DOM. Lives games that only track gameState.lives but never write data-lives to a DOM element cause all mechanics test assertions on life decrements to return undefined and fail. Gen prompt must require: for games with totalLives > 0, syncDOMState() sets document.getElementById('app').setAttribute('data-lives', gameState.lives). Sources: addition-mcq-lives spec (F7), addition-mcq spec (F5), math-cross-grid spec (F7) — 3 confirmed instances, not MCQ-exclusive. | Eliminates getLives() returning undefined in mechanics tests for lives-based games. |
| **gameState must always include gameId field** | **DONE — pre-existing (GEN-GAMEID, confirmed 2026-03-23)** | Already in buildGenerationPrompt rule 26b + CDN_CONSTRAINTS_BLOCK. Duplicate of line 446 entry (marked DONE). 6 confirmed instances. | Same rule confirmed shipped. |
| **GEN rule: window.endGame must always be assigned** | **DONE — pre-existing (GEN-WINDOW-EXPOSE rule 36, confirmed 2026-03-23)** | Already in buildGenerationPrompt rule 36 + CDN_CONSTRAINTS_BLOCK. 6 confirmed instances. T1 W3 check also ships this. | Same rule confirmed shipped. |
| **GEN rule: data-phase / syncDOMState applies to ALL game types, not just MCQ** | **pending** | Source: UI/UX audit math-cross-grid spec (F4) — first confirmed non-MCQ drag-and-drop instance. GEN-MCQ-PHASE rule scopes syncDOMState() to MCQ games. Drag-and-drop and other game types also transition between named phases (gameplay and results) with no data-phase signal. Extend gen prompt rule: all games with 2 or more named phases must call syncDOMState() at each transition, regardless of game type. | Fixes harness phase detection for non-MCQ game types. |
| **GEN-UX-002: extend 44px selector to .option-btn and all interactive button classes** | **DONE — pre-existing (confirmed 2026-03-23)** | CDN_CONSTRAINTS_BLOCK line 121 already lists `.choice-btn`, `.option-btn`, `.adj-btn`, `.reset-btn` explicitly + buildGenerationPrompt rule 32 covers all by name. No additional change needed. | Confirmed via agent grep 2026-03-23. |
| **GEN rule: waitForPackages() must only await packages the game actually uses** | **pending** | Source: UI/UX audit math-cross-grid spec (F8). waitForPackages() polls for TimerComponent but spec says Timer: None. Gen prompt must state: only poll for CDN components the game actually instantiates. Do not include TimerComponent unless the game creates a TimerComponent instance. | Prevents game-start timeout on CDN delay for unused components. |
| **Spec addition: game_complete postMessage must fire on BOTH victory AND game-over paths (explicit dual-path)** | **pending** | Sources: math-mcq-quiz spec (F8), addition-mcq-lives spec (F4) — 2nd confirmed instance. Specs describe game-over as "GAME OVER screen" only without stating game_complete fires on that path too. Spec template must add: "Both victory and game-over paths call endGame() which fires game_complete postMessage." Update spec template in docs/resources/spec-rca-template.md and enforce in per-game specs before first build. | Prevents silent omission of game_complete on game-over path, which fails postMessage contract tests. |
| **Spec addition: restartGame() in timer games must destroy + recreate TimerComponent** | **pending** | Sources: math-mcq-quiz spec (F9), addition-mcq-lives spec (F6) — 2nd confirmed timer game instance. Spec says "restartGame() → reset state" with no mention of timer lifecycle. If restartGame() resets state in-place (the generated default), stale onEnd callbacks from the previous timer fire on the new round. Spec must mandate: timer.destroy() then timer = new TimerComponent(...) (or page reload) in restartGame(). Add this to the timer game spec template. | Prevents stale timer callbacks from triggering duplicate life deductions or game-over on restart. |
| **Gen prompt T1 compliance — eliminate static-fix LLM call** | **done (2026-03-20)** | Trace finding: rules already existed at line 356–357 (end of 360-line prompt) but LLM makes structural decisions before reaching them. Root cause confirmed: initSentry() before waitForPackages() → ReferenceError → ScreenLayout.inject() skipped → #mathai-transition-slot absent → blank page, all tests fail. Fix: moved initSentry + debug-function constraints into rule 5 (CDN INIT ORDER) so LLM reads them before writing the init sequence. Also shipped blank-page detection in smoke check (cad2ca3) as backstop. 447 tests pass; deployed 2026-03-20 (commit 14ab33c). | Expected: 75–80% reduction in static-fix LLM calls. Measure: track `static-validation-passed` rate across next 10 builds. |
| **Cross-build failure pattern injection** | **done (2026-03-20)** | Trace result: DB sparse (73 patterns, 5 games, adjustment-strategy dominates). 80% of failures are iterations=0 (pre-fix-loop) so injection only helps 20% of builds. Scoped to current-game-only lookup at iteration 1. 45 lines across db.js + pipeline-fix-loop.js. Reuses existing `triageFixHints` + `fixHintContext` infrastructure. 3 new tests; 444 total pass; deployed 2026-03-20 (commit 26b21b0). | Modest: saves ~1 iteration for repeat-pattern games. Low-risk, low-effort — worth shipping but not the leverage point originally hypothesized. Real leverage is upstream (T1 gen compliance). |
| **Cross-game learning injection** | **done (2026-03-20)** | Extract fix patterns + root-cause notes from every APPROVED build's `learnings` table and inject as a "lessons from similar games" block into generation + fix prompts for new builds of the same CDN part mix | Reduce avg iterations from ~3 → ~1.5; fewer triage calls; higher first-pass approval rate — biggest throughput lever remaining |
| **Semantic learning deduplication** | **done (2026-03-20)** | `jaccardSimilarity()` + dedup pass in `getRelevantLearnings()`: keeps most-recent entry per cluster (threshold 0.6), caps at 20 bullets, fetch limit 3× cap — 10 new tests, 357 pass | Keeps injected learnings block compact (≤ 20 bullets) even after 100+ approved builds; preserves prompt token budget |
| **Spec-similarity retrieval** | **done (2026-03-20)** | `extractSpecKeywords()` extracts PART-XXX IDs + PascalCase CDN names + mechanic nouns from spec; `getRelevantLearnings()` scores deduped learnings via Jaccard against spec keywords and sorts most-relevant first; falls back to recency when no specContent; 5 new tests, 362 total / 358 pass; deployed 2026-03-20 | Reduces prompt noise: irrelevant learnings from dissimilar game types sort to bottom; novel game types get targeted learnings surfaced first |
| **Learning category boosting** | **done (2026-03-20)** | `getCategoryBoost()` in `lib/pipeline.js`: `contract` +0.2 always, `cdncompat` +0.2 when PART-xxx in spec, `audio` +0.2 when FeedbackManager in spec, `layout` +0.2 when ScreenLayout in spec; exported for testability; 10 new unit tests; all 372 tests pass; deployed 2026-03-20 | Category-aware secondary sort surfaces contract/CDN/audio/layout learnings above equally-similar general entries — most actionable patterns reach the LLM first |
| **Spec-keyword SQL pre-filtering** | **done (2026-03-20)** | `spec_keywords TEXT` column added to `builds` table (idempotent ALTER TABLE migration); `db.updateBuildSpecKeywords()` stores extracted keywords as JSON array; `deriveRelevantCategories()` maps spec keyword signals → category set (contract+general always; cdncompat when PART-xxx; audio when feedbackmanager; layout when screenlayout); `getRelevantLearnings()` adds SQL `WHERE l.category IN (...)` pre-filter when specContent provided; falls back to no-filter when specContent null; `buildId` threaded through `runPipeline()` options so worker saves keywords to DB early in Step 0; 13 new tests; 385 total pass; deployed 2026-03-20 | SQL pre-filter reduces rows fetched from O(N) to O(matching-category-rows), keeping JS dedup work O(k) as learnings table grows to 1000+ entries; `spec_keywords` column enables future LLM-side retrieval augmentation |
| **Learning retrieval — index-scan optimization** | **done (2026-03-20)** | `CREATE INDEX IF NOT EXISTS idx_learnings_cat_build ON learnings(category, build_id DESC)` makes `WHERE l.category IN (...) ORDER BY l.build_id DESC` use an index scan. `CREATE INDEX IF NOT EXISTS idx_builds_approved ON builds(id) WHERE status='approved'` partial index covers the approved-builds JOIN. Both added to schema init in `lib/db.js`; idempotent `IF NOT EXISTS`; 385 tests pass; deployed 2026-03-20 (no worker restart needed — indexes applied on next DB connection open). | Per-call latency O(log N + k) instead of O(N) at 10k+ learning rows |
| **pipeline.js Phase 3: split remaining orchestration** | **done (2026-03-20)** | pipeline.js: 2433 → 839 lines; lib/pipeline-fix-loop.js (802 lines), lib/pipeline-test-gen.js (569 lines), lib/pipeline-targeted-fix.js (422 lines) created; 385 tests pass; deployed 2026-03-20 (commit 73cc250) | Unlocks targeted unit tests for fix-loop and targeted-fix paths; reduces merge conflict surface; onboarding time drops by ~5× |
| **Queue-sync resilience: auto-requeue job-lost builds at startup** | **done (2026-03-20)** | worker.js | `requeueQueueSyncBuilds()` runs at worker startup after `cleanupOrphanedBuilds()`. Queries `status='failed' AND error_message LIKE '%queue-sync%' AND retry_count < 1`, skips games with active builds, enqueues via BullMQ Queue, sets `retry_count=1`. 7 new tests (435 total pass); deployed 2026-03-20. | Zero manual re-queues needed after worker restart; recovers ~5-9 builds per restart event automatically. |
| **Rendering-pattern detection: toBeVisible failure auto-skip** | **done (2026-03-20)** | lib/pipeline-fix-loop.js | `detectRenderingMismatch()` added: if >3 failures match `toBeVisible\|toBeHidden`, skip_tests without LLM triage call. Exported for unit tests. 6 new tests (441 total pass); deployed 2026-03-20 (commit 9c2e641). Saves one LLM round-trip per affected batch. |
| **P6: Reduce review rejection rate** | **done (2026-03-20)** | Rules 22/23/24 added to gen prompt (isActive guard, game_over stars, TransitionScreen routing); CDN_CONSTRAINTS_BLOCK updated for fix prompts; T1 Check 11 (game_over star display) + Check 12 (isActive guard) added as warnings; 389 tests pass; deployed 2026-03-20 (commit 1a6e01e) | Should cut review rejections from ~20% to ~10% |
| **P8: Relax isInitFailure guard** | **done (2026-03-20)** | `isInitFailure()` refactored to exported function; ANY-match + passed===0; 14 regex patterns; 10 new unit tests; 399 tests pass; deployed 2026-03-20 (commit 5e7eaa0) | Catches partial-init failures that ALL-match missed; saves 5 wasted iterations per affected build |
| **P7: Deduplicate CDN constraint rules across fix prompts** | **done (2026-03-20)** | lib/prompts.js | CDN_CONSTRAINTS_BLOCK expanded with SENTRY ORDER, DEBUG FUNCTIONS, SCREENLAYOUT rules; reused in buildFixPrompt, buildGlobalFixPrompt, buildTargetedFixPrompt. Eliminated 3 diverged copies (net -34 lines, +5 to CDN_CONSTRAINTS_BLOCK). 441 tests pass; deployed 2026-03-20 (commit e7878e7). Any future CDN rule change needs one edit, not 3+. |
| **P8: Deduplicate build queue at enqueue** | **done (2026-03-20)** | `POST /api/build` checks for existing `queued/running` build before creating new one; returns existing `buildId` with `deduplicated: true`; `force: true` bypasses; approved game check; webhook handler calls `shouldSkipWebhookBuild()`; 5 new integration tests; 409 total pass; deployed 2026-03-20 (commit 0c6f2d2) | Prevents duplicate queue entries; rapid-challenge manual fix now automated |
| **Queue-sync resilience: auto-requeue BullMQ job-lost builds** | **done (2026-03-20)** | worker.js | See R&D table above. |
| **P8: Ensure pre-pipeline failures record error_message** | **done (2026-03-20)** | `worker.on('failed')` normalised error message: non-Error/empty-message cases now fall back to `err.toString()` or sentinel string; inner catch now calls `failBuild()` before re-throw; DB never left with NULL; 5 new tests; 404 total pass; deployed 2026-03-20 (commit 05a9070) | Future `null error_message` failures now impossible; debugging orphaned builds no longer blind |
| **require() cache bust after git pull** | **done (2026-03-20)** | worker.js self-patches lib/ require() cache when HEAD changes after git pull; prevents stale code running on new jobs after in-place deploys; commit f77b404 | Zero stale-code builds after hot-deploy; no worker restart needed for lib/ changes |
| **Gen prompt T1 compliance: initSentry + debug rules early** | **done (2026-03-20)** | Moved initSentry order + debug-function constraints into rule 5 (CDN INIT ORDER) so LLM reads them before writing the init sequence; blank-page detection in smoke check as backstop; 447 tests pass; commits 14ab33c + cad2ca3 | 75-80% reduction in static-fix LLM calls expected; ScreenLayout-blocked blank-page failures caught before test-gen |
| **E9 cross-build failure pattern injection** | **done (2026-03-20)** | `findMatchingPattern()` in db.js; injected at iteration 1 via triageFixHints + fixHintContext; scoped to current-game-only lookup; 3 new tests; 444 pass; commit 26b21b0 | Saves ~1 iteration for repeat-pattern games; low overhead |
| **First-pass failure root cause analysis** | **done (2026-03-20)** | Traced 65 triage events across builds 218–232: 58% HTML fatal init, 22% phase-transition missing syncDOMState(), 9% data-shape mismatch, 11% other. Shipped two fixes: (1) abort-on-snapshot-failure (pipeline-test-gen.js throws FatalSnapshotError on null snapshot; pipeline.js regen + retry; abort if retry fails) — eliminates test-gen LLM call on confirmed-broken pages (44% of fatal-init cases). (2) Rule 22 in lib/prompts.js: syncDOMState() required after every gameState.phase assignment — targets 22% of iter-1 failures. Full analysis at docs/rnd-first-pass-failure-analysis.md. | Committed 2026-03-20. Expected: ~44% reduction in wasted test-gen LLM calls; 22% reduction in phase-transition iter-1 failures. |
| **Reduce fatal CDN init failures (58% of iter-1 failures)** | **done (2026-03-20)** | Hypothesis confirmed. Critical finding: `transitionScreen.show()` MUST be called as the last step of `DOMContentLoaded` when using PART-025/ScreenLayout — a missing call leaves `#mathai-transition-slot` empty, causing 100% test failure on all waitForPhase() calls. Rule added to gen prompt (commit 316f32a). This addresses the 58% fatal-init class. Measurable: before/after iteration-1 game-flow pass rate across next 10 builds. | |
| **Review rejection root cause analysis** | **done (2026-03-20)** | Classified 28 rejection events (builds 50–295). Top causes: VisibilityTracker API misuse (39%), debug-function window-exposure rule conflict (29%), postMessage payload incomplete (18%). Critical finding: CDN_CONSTRAINTS_BLOCK says "debug functions MUST NOT be on window" but spec checklist requires them ON window — creates unfixable loop. Fix: change rule to MUST expose on window. Full analysis at docs/rnd-review-rejection-analysis.md. | Fixing rule conflict + adding VisibilityTracker template expected to eliminate ~50% of early-review rejections |
| **Cross-batch fix-loop regression detection** | **done (2026-03-20)** | `detectCrossBatchRegression()` added to pipeline-fix-loop.js. Smoke-checks all prior-passing batch spec files after each batch completes; rolls back to preBatchHtml and marks batch as rolled_back on regression. Empirical trace: 63% of 19 multi-batch builds had cross-batch regressions. 6 new unit tests; 453 total pass. Shipped commit 76996c1. | Eliminates the class of silent regressions where fixing batch N breaks batch N+1 — previously undetectable until Step 3b final re-test |
| **Iteration-1 pass rate analysis + gen prompt improvement** | **done (2026-03-20, commit 929c6ef)** | Shipped 3 gen prompt rules: Rule 20 ROUND LIFECYCLE RESET (isProcessing=false + isActive=true + syncDOMState() as first 3 lines of every loadRound()); Rule 21 SYNCDOMESTATE CALL SITES (enumerated all 6 required call-sites); Rule 22 inline POSTMESSAGE REQUIRED FIELDS (explicit payload schema). All 453 tests pass. | Expected: reduce avg iterations; fewer fix-loop LLM calls. Measure: per-category iter-1 pass rate across next 10 builds. |
| **Review rejection follow-up analysis** | **measuring** | Post-fix-1a6e01e measurement: classify rejection events from builds 296+ to measure whether top-3 fix (debug-function rule, VisibilityTracker template, postMessage schema) reduced rejection rate. Identify new top patterns. Awaiting 5+ post-fix builds. |
| **403 CDN non-fatal handling** | **done (2026-03-20, commits c775187 + d887043)** | Root cause: prompts.js CDN_CONSTRAINTS_BLOCK told LLM to use `cdn.homeworkapp.ai` — now returns 403. Correct domain: `storage.googleapis.com/test-dynamic-assets`. Fix: (1) updated CDN_CONSTRAINTS_BLOCK + buildCliGenPrompt with correct domain + canonical 3-package URL list; (2) pipeline.js + pipeline-test-gen.js post-gen CDN cleanup now also strips `cdn.homeworkapp.ai` script tags and injects canonical block; (3) d887043: `fixCdnDomainsInFile` applied after EVERY LLM HTML write (not just initial gen). 518 tests pass; deployed 2026-03-20. | Unblocks all CDN games in queue; eliminates 403-caused Step 1d failures |
| **T1 W3: data-testid enforcement** | **done (2026-03-20, commit c33f8cd)** | Mechanics category 75% iter-2 rate traced to missing data-testid attributes. Added W3 warning to validate-static.js: warns when >50% of button/input/select elements lack data-testid. Strengthened CLI gen prompt Rule 15 with explicit element-type enumeration. 522 tests pass; deployed 2026-03-20. | Surfaces missing data-testid before test-gen; saves ~54 triage+fix LLM calls per 121 builds |
| **T1 W4: syncDOMState call-site enforcement** | **done (2026-03-20, commit bdb1889)** | W4 warns when gameState.phase assignment lacks syncDOMState() in ±200-char window. 4 new tests; 532 total pass; deployed 2026-03-20. | Surfaces missing syncDOMState before test-gen; targets 22% of game-flow iter-1 failures |
| **Contract failure pattern analysis** | **done (2026-03-20, commit 9abfaa3)** | Top patterns: (1) wrong postMessage structure — Rule 5 specified flat payload but CDN needs nested `{data: {metrics,...}}` (23% of failures); (2) missing window.calcStars (10%). Fix: corrected Rule 5 in buildGenerationPrompt + buildCliGenPrompt; T2 check for flat payloads + missing window.calcStars. 537 tests pass. Expected: 33% reduction in contract iter-2 rate. Analysis: docs/rnd-contract-failure-analysis.md |
| **Fix loop improvement: reduce per-category iter-2 rates** | **measuring** | Three T1 checks shipped (W2/W3/W4) targeting mechanics 75% + game-flow 22% patterns. Contract fix shipped (9abfaa3). Awaiting 10 builds post-fix to measure iter-2 rate change per category. |
| **adjustment-strategy spec contradiction fix** | **done (2026-03-20)** | Root cause: spec said "DO NOT call FeedbackManager.init()" but code examples at lines 630+935 still called it — LLM followed executable code, causing blocking audio popup on every build (59 attempts total, 5 approvals but oscillating). Also fixed: postMessage CRITICAL comment, calcStars game_over=0 guard, adjuster button visibility (was replacing DOM instead of toggling class). Build #350 queued for first clean run. Analysis: docs/rnd-chronic-failures-diagnosis.md | |
| **Spec quality: proactive contradiction scan** | **done (2026-03-20)** | CRITICAL FINDING: 48/50 specs had `await FeedbackManager.init()` as executable code in DOMContentLoaded examples — the exact bug that caused adjustment-strategy's 59-build loop. Every CDN game in queue was generating a blocking audio popup on every build. Fix: replaced all instances with `// DO NOT call FeedbackManager.init() — PART-015 auto-inits on load` comment across 46 specs. Also fixed: simon-says + adjustment-strategy wrong CDN audio URLs (cdn.homeworkapp.ai → cdn.mathai.ai). Local + server specs updated. | Expected: dramatic throughput improvement — all 43 queued CDN games now generate without blocking audio popup; removes primary cause of test harness injection failures |
| **BullMQ lock renewal for long Opus calls** | **done (2026-03-20, commit 95ed7c7)** | worker.js: `lockDuration: 90 * 60 * 1000` (90min), `lockRenewTime: 10 * 60 * 1000` (10min). Initial value was 30min; raised to 90min in commit 95ed7c7 to cover Opus generation calls on large specs. Deployed. Eliminates lock-loss on long Opus calls. Task #59 complete. |
| **Auto-dismiss FeedbackManager audio popup in test harness** | **done (2026-03-20, commit 16cc686)** | lib/pipeline-utils.js (test harness injection) | `beforeEach` in injected harness now explicitly clicks the "Okay!" button from FeedbackManager audio permission popup with a 2s timeout and silent catch. Prevents non-deterministic test failures caused by the popup appearing after `startGame()` in headless Playwright when `FeedbackManager.init()` was called (Lesson 51 race condition). Complements gen-prompt fix (no `FeedbackManager.init()` if PART-017=NO) as a defence-in-depth backstop. |
| **Debug-function window exposure rule conflict fix (Lesson 55)** | **done (2026-03-20, commit dd7f170)** | lib/prompts.js (CDN_CONSTRAINTS_BLOCK) | `CDN_CONSTRAINTS_BLOCK` previously told the gen LLM "debug functions MUST NOT be on window", but the spec Verification Checklist requires them ON window. The contradictory rule caused 29% of early-review rejections — an unfixable loop. Fixed by changing the rule to: "Debug functions MUST be exposed on window — define as named functions inside DOMContentLoaded then assign: `window.debugGame = debugGame`". Queens build 285 was rejected 3 consecutive times for this before the fix. |
| **Mechanics test-gen failure root cause analysis** | **done (2026-03-20, commit e60dbb1)** | Traced 15 mechanics failures: #1 hardcoded wrong values (5+), #2 click timeout on isProcessing=true (4), #3 waitForPhase timeout (4), #4 toHaveText mismatch on nested cells (2), #5 invented gameState properties (2). Fix: 5 mechanics-specific rules M1-M5 in buildTestGenCategoryPrompt; W3 escalated to error when >80% missing. 537 tests pass. Deployed to server. Expected: reduce mechanics iter-2 from 75% toward 40%. |
| **Generation LLM timeout fix** | **done (2026-03-20, commit 4eb1d29)** | `RALPH_LLM_TIMEOUT=300` was killing large-spec HTML generation (interactive-chat 59KB, bubbles-pairs 64KB) at exactly 5 min with 0 iterations. Fix: added `RALPH_GEN_LLM_TIMEOUT` to config (default 600s); all 4 gen call sites use it. Triage/fix calls keep 300s. 537 tests pass. | Unblocks all large-spec games that were timing out before iteration 1 |
| **Cross-batch-guard false rollback fix** | **done (2026-03-20, commit 7d27432)** | `detectCrossBatchRegression()` was treating 0/0 test results (timeout/infra failure) as regression because `0 < prevPassed`. Queens build rolled back every batch due to 30s smoke timeout being too short for game-flow tests. Fix: skip regression when `nowTotal===0` (inconclusive); increase timeout from 30s → 90s. 537 tests pass. | Eliminates false rollbacks that were wasting all post-mechanics-fix batches in queens |
| **Game-flow iter-2 root cause + spec phase hints** | **done (2026-03-20, commit 83011a6)** | game-flow has 34% iter-2 rate. Root cause: waitForPhase() uses wrong phase strings ('playing' when game uses 'game', etc.). Fix: `extractPhaseNamesFromGame()` in prompts.js parses actual phase names from HTML (assignments, comparisons, data-phase attributes, gameState shape) and injects GF1 rule into game-flow test-gen: "use ONLY these exact phase names — never guess". GF2 rule handles init-only snapshot case. 8 unit tests; 545 total pass. Deployed 2026-03-20. Expected: significant reduction in game-flow iter-2 wrong-phase timeout failures. |
| **Measure cross-batch-guard + gen-timeout + phase-hints impact** | **measuring** | 4 fixes shipped this session: (1) cross-batch-guard false rollback fix, (2) gen LLM timeout 300→600s, (3) M1-M5 mechanics rules, (4) GF1/GF2 game-flow phase hints. Awaiting 5-10 builds on the new code to measure: (a) iteration-1 pass rates per category, (b) no more iterations=0 failures for large specs, (c) no more cross-batch false rollbacks. Track builds 374+ (first clean run with all 4 fixes). Next: if all 4 improvements confirmed, pivot to non-standard lifecycle test-gen measurement. |
| **Rendering failure prevention: R1-R5 rules in test-gen + gen-prompt** | **done (2026-03-20, commit 1645449)** | Top 3 patterns identified from 106 rendering learnings: (1) toBeVisible called before startGame(), (2) transition-slot button checked immediately after startGame(), (3) direct window.gameState.content access instead of fallbackContent. Added R1-R5 rules to buildTestGenCategoryPrompt + 2 gen-prompt rules. 545 tests pass. |
| **Queue-sync: detect orphaned DB-queued builds with no BullMQ job** | **done (2026-03-20, commit 924c9a5)** | `requeueOrphanedQueuedBuilds()` added to worker.js startup — compares DB `status='queued'` count against BullMQ waiting+active count; if BullMQ has fewer jobs than DB-queued builds (e.g. after crash-loop empties Redis), re-enqueues all orphaned builds using `jobId='build-{id}'` for idempotency. 5 new unit tests; 550 total pass. Recovers up to 37 builds per crash-loop incident automatically. | Zero manual re-queues needed after any future worker crash-loop; prevents silent queue loss |
| **Increase RALPH_GEN_LLM_TIMEOUT default 600s → 1200s** | **done (2026-03-20, commit 60bd7ae)** | `lib/config.js` | zip (56KB spec) timed out at exactly 600s. two-digit-doubles-aided took 373s for a smaller spec; interactive-chat (59KB) and bubbles-pairs (64KB) also in queue. 1200s gives headroom for large CDN game specs generating 32K output tokens via Claude CLI. | Unblocks large-spec games timing out at 600s boundary; prevents 0-iteration failures on specs >50KB |
| **Non-standard lifecycle test gen — H1** | **done (2026-03-20, commit b27e010, confirmed 2026-03-21)** | lib/prompts.js | H1 (LIVES SYSTEM CHECK) confirmed: associations build 405 mechanics tests contained no lives-decrement assertions after H1 ship — correct non-lives tests generated ("Correct Match Scoring", "Incorrect Match Feedback", "Play Again Resets State"). Hypothesis confirmed. |
| **Non-standard lifecycle test gen — H2** | **done (2026-03-21, confirmed)** | lib/pipeline-test-gen.js | H2 (GAME FEATURE FLAGS) was already fully implemented: `buildGameFeaturesBlock()` called in both `buildTestCasesPrompt()` (Step 2a) and `buildTestGenCategoryPrompt()` (Step 2b). Measurement agent incorrectly reported it as missing — code review confirmed injection active. Both H1 + H2 complete. |
| **Smoke-regen effectiveness measurement** | **done (R&D, 2026-03-21)** | lib/pipeline.js | Measured: 13 definitive smoke-regen events post-ScreenLayout slots fix. Repeat-failure rate: 38.5% (5/13) — fails the 10% criterion. Root cause: smoke-regen uses genPrompt+smokeErrorContext (full regen from scratch), so LLM can still produce broken ScreenLayout.inject(). 4/5 failures = missing #gameContent from bad regen. Fix: switch to surgical CDN init fix prompt (show broken HTML, fix just init sequence). Lesson 83. |
| **Surgical CDN init fix for smoke-regen** | **measuring (2026-03-21)** | lib/pipeline.js, lib/prompts.js | Implementation shipped: `buildSmokeRegenFixPrompt()` in prompts.js shows LLM the failing HTML + asks to fix ONLY the CDN init sequence. CDN games use surgical fix; non-CDN fall back to old full-regen. Extended (commits 4899b4e + 274796a): detects TimerComponent, TransitionScreenComponent, ProgressBarComponent in failing HTML and injects component-specific typeof guard instructions. Post-scale data: 15 smoke-regen events total across 11 games — spread thin enough that no single game pattern dominates. Measurement ongoing. Success: repeat-failure rate <10% across next 10 events. |
| **Contract iter-2 root cause analysis + CT3-CT5 rules** | **measuring (2026-03-21, commit 310c928)** | lib/prompts.js | Audit found 3 real patterns: (CT3) transition-slot button in contract tests — 15 instances across associations/light-up/keep-track (same anti-pattern as GF5); (CT4) getLastPostMessage() called before waitForPhase('results', 20000) — 10 instances in disappearing-numbers/keep-track; (CT5) flat payload assertions against CDN nested structure (msg.score vs msg.data.metrics.score, wrong msg.type) — 8+ instances. Rules CT3/CT4/CT5 added to buildTestGenCategoryPrompt. 599 tests pass. Expected: contract iter-2 rate from 22.7% → ~12%. Measure across next 20 builds. |
| **Game-flow iter-2 root cause analysis + GF3-GF5 rules** | **measuring (2026-03-21, commit 6d16090)** | lib/prompts.js | Top 3 patterns: (GF3) gameover phase timeout — lives reach 0 but data-phase stays 'playing' for 300-800ms, 10s timeout borderline; fix: use waitForPhase('gameover', 15000). (GF4) results phase timeout after skipToEnd/endGame — syncDOMState 500ms poll chain can exceed 10s; fix: waitForPhase('results', 20000) + check results-screen visible before game-screen hidden. (GF5) #mathai-transition-slot button assumed present — type='level-transition' auto-advances without button, 9 failures across 3 games; fix: never wait on that selector in game-flow tests. 599 tests pass; deployed 2026-03-21. Measure: game-flow iter-2 rate across next 20 builds vs 23.1% baseline. |
| **BullMQ/DB cancelled-build sync guard** | **done (2026-03-21, commit 2274342)** | lib/worker.js, lib/db.js, server.js | Fix 1: Worker pre-flight adds 'cancelled' to terminal-state array — skips job with warning before any pipeline runs. Fix 2: `db.cancelBuild()` added; `POST /api/builds/:id/cancel` calls `queue.getJob(id).remove()` to evict BullMQ job on cancellation. Fix 3: 7 new tests; 599 total pass. Deployed without restart (builds running). Ghost-job waste eliminated. |
| **Edge-cases iter-2 root cause analysis + EC1-EC3 rules** | **measuring (2026-03-21, commit 2d7e78f)** | lib/prompts.js | Audit found 3 real patterns: (EC1) gameover phase assertion with 10s default timeout after guard-triggered action — phase stays 'playing' 300-800ms; fix: waitForPhase('gameover', 15000). (EC2) debounce/isProcessing guard race — second click arrives before flag is set; fix: await first action settle before rapid-fire second click. (EC3) wrong start-button selector blocks mid-game setup — generic 'button:has-text("Start")' resolves to restart button on results screen; fix: use 'button[data-action="start"], #start-game' or wait for game-screen before clicking. 606 tests pass. Expected: edge-cases iter-2 from 19% → ~12%. Measure across next 20 builds. |
| **Level-progression iter-2 root cause analysis + LP4-LP6 rules** | **measuring (2026-03-21, commit c76fde4)** | lib/prompts.js | LP1-LP3 already existed. Added LP4-LP6: (LP4) transition-slot button in LP tests — `#mathai-transition-slot button` never renders for type='level-transition' (auto-dismiss); same anti-pattern as GF5/CT3/EC3. (LP5) stale getRound()/data-round after answer — syncDOMState 500ms poll means synchronous assertion reads pre-answer value; fix: await waitForFunction. (LP6) hardcoded spec-derived difficulty counts — `toBe(N)` for pair/item counts fails when implementation uses `>=N`; fix: use `toBeGreaterThanOrEqual`. 606 tests pass; deployed. All 5 categories now have complete rule sets. Measure iter-2 rate across next 20 builds vs 16.1% baseline. |
| **All-category test-gen quality initiative complete** | **done (2026-03-21)** | lib/prompts.js | All 5 test categories now have dedicated iter-2 reduction rules: mechanics M10-M12 (target 32.9%→?%), game-flow GF3-GF5 (23.1%), contract CT3-CT5 (22.7%), edge-cases EC1-EC3 (19%), level-progression LP4-LP6 (16.1%). Combined: ~15-20 failure patterns eliminated from test generation. All measuring across next 20 builds. |
| **Prompt token optimization — smart context compression** | **done (2026-03-22)** | lib/prompts.js, lib/pipeline.js, lib/pipeline-fix-loop.js | Three optimizations shipped: (1) CSS stripping from HTML before passing to fix/review LLMs — ~24% token reduction (CSS irrelevant to JS logic fixes); (2) diff-based fix prompts at iteration 2+ — shows only changed lines vs prior iteration instead of full HTML, ~91-99% token reduction on large files; (3) fixWaitForPackagesTimeout() deterministic pre-fix — detects the `10000` timeout pattern and patches it directly without an LLM static-fix call (saves 1 LLM call on every CDN game with old timeout). Measure: prompt token counts tracked per build. |
| **Test case completeness — cover all user interaction paths from spec** | **done (2026-03-22, commits a35d432 + b35b60c + shipped)** | lib/prompts.js, lib/pipeline-test-gen.js | Shipped rules: M13 (wrong-answer path ≥1 test case), GF6 (lives-exhaustion gameover path), EC4 (timer-expiry path for timed games), EC5 (option-visibility after CDN slot transition). EC-FIX-1/2/3 shipped. extractUserScenarios() shipped (a35d432). EC-TRIAGE-1 (bans skip_tests for selector/strict-mode violations without DOM evidence) shipped (b35b60c). hasLives heuristic refinement shipped (unlimited-lives guard). All completeness and triage correctness improvements complete. |
| **Post-CT6 first-attempt approval rate re-measurement + Lesson 133 rules** | **done (2026-03-22, commits 060b442 + f0861ea)** | analysis, lib/prompts.js | Hypothesis confirmed (partial). CT6 + EC-TRIAGE-1 + hasLives fix addressed top 3 blockers from builds 508-512. Lesson 133 shipped additional rules (commit f0861ea, 2026-03-22): (1) M13 syncDOMState race guard — getLives/getScore/getRound now use expect.poll() instead of immediate read; (2) CT4 strengthened — mandatory 4-step postMessage sequence (waitForPhase results → wait 200ms → getLastPostMessage → null-guard before property access); (3) GF6 sub-phase waitForPhase guard — wait for game-screen visible before asserting sub-phase elements; (4) GF3/EC1/EC4 gameover timeout 15000→20000ms; (5) RULE-DUP — globally unique data-testid requirement across all test categories. All 5 rules address iteration-1 failure patterns observed in builds 508-512. Next measurement: ≥20% first-attempt approval across next 5 completed builds. |
| **Post-Lesson-133 first-attempt approval rate measurement** | **done (2026-03-22)** | analysis | Hypothesis confirmed: Lesson 133 rules shipped. Gap analysis (docs/rnd-prompt-gap-analysis.md) identified 5 additional gaps in builds 505-516: GEN-109 (isProcessing during reveal), M15 (CSS class timing race), GF8 (screen visibility before phase settled), CT7 (postMessage type 'game_complete' vs 'gameOver' mismatch — latent bug), EC6 (locator arg type). All shipped in commit 4e6f5f6 (Lesson 134). |
| **Test linter effectiveness analysis + dead rule replacement** | **done (2026-03-22, commit 7cd470a)** | lib/pipeline-test-gen.js, test/pipeline-test-gen.test.js | R&D: grepped 664 test files / 145 builds against all 8 lint rules. Finding: 4/8 rules had 0 hits (M15, CT7, CT7_GAMEOVER, CT6_NULL) — regex patterns didn't match actual LLM output. Fixed: M15 widened; CT7→HARDCODED_TIMEOUT (`page.waitForTimeout(N)`); CT7_GAMEOVER→RAW_CLICK (`await page.click(`); CT6_NULL simplified to direct-access pattern. +24 unit tests; 636 total pass. Active rules now validated against real corpus: TRANSITION_SLOT 100%, GF8 64%, M13 45%, RULE-DUP 27%. | |
| **GF9 rule: ban #mathai-transition-slot selector in test-gen** | **done (2026-03-22, commit e87d8c9)** | lib/prompts.js | Root cause: global "Transition slot selectors" template block actively advertised the selector as canonical, overriding per-category prohibitions in GF5/LP4/CT3. TRANSITION_SLOT lint rule fired in 100% of approved builds despite existing category-level bans. Fix: CRITICAL note added to global section + GF9 explicit rule in game-flow block with WRONG/RIGHT examples. No test count change (prompt-only). Expected: TRANSITION_SLOT violations drop from 100% → <20%. | |
| **GF8+M13 root cause fix: teaching examples + lint bypass** | **done (2026-03-22, commit 74eab2e)** | lib/prompts.js, lib/pipeline-test-gen.js | M13: "Helper usage" section showed verbatim M13 violations as teaching examples — LLM copied them. GF8: (1) R1/R4 RIGHT examples showed toBeVisible() without waitForPhase; (2) lint regex bypassed by `{ timeout: 10000 }` arg. Fixes: examples corrected to show expect.poll() / waitForPhase-first patterns; lint regex widened. 636 tests pass. Expected: M13 45%→<15%, GF8 64%→<25%. | |
| **RULE-DUP root cause fix: gen prompt generic testids + lint threshold** | **done (2026-03-22, commit 17a325a)** | lib/prompts.js, lib/pipeline-test-gen.js | Gen prompt MANDATORY RULE #1 listed specific generic testids ("answer-input", "btn-check", "option-{index}") as required minimums — HTML gen stamped them on every game, test gen copied them into all 5 category files. Fix: CRITICAL note added — only use testid values verbatim from DOM snapshot. Lint threshold raised from 2 → 3+ categories to eliminate legitimate cross-references. 637 tests pass. Expected: RULE-DUP violations drop from 27% → near-0% false positives. | |
| **Gen prompt contradictory-instruction audit (Lesson 140)** | **done (2026-03-22, commit 0bc9b8d)** | lib/prompts.js | Applied contradictory-instruction methodology to HTML gen prompt. One contradiction found: GEN-110 CORRECT example taught `gameState.phase = 'game_over'` (underscore) while canonical phase is `'gameover'` (no underscore) — caused syncDOMState() to set wrong data-phase value, timing out all waitForPhase('gameover') calls. No contradictions found for CDN URLs, waitForPackages timeout, or window exposure rules. 637 tests pass. | |
| **Fix/triage/review prompt contradictory-instruction audit (Lesson 141)** | **done (2026-03-22, commit 21d1479)** | lib/prompts.js, lib/pipeline-fix-loop.js | 5 contradictions found: (1) CRITICAL: LESSON_PATTERNS taught `cdn.homeworkapp.ai` as correct CDN domain (the banned one) — fix loop would swap to another banned domain; (2) HIGH: onComplete impossibility loop — REVIEW_SHARED_GUIDANCE required it, CDN_CONSTRAINTS_BLOCK banned it; (3) HIGH: Rule 25 RIGHT example showed onComplete; (4) HIGH: CDN_CONSTRAINTS_BLOCK lines 109 vs 111 contradicted each other on onComplete; (5) MEDIUM: syncDOMState terminology. All fixed. 637 tests pass. Key lesson: LESSON_PATTERNS entries can become stale and dangerous — must audit when CDN rules change. | |
| **Post-Lesson-134 first-attempt approval rate measurement** | **done (2026-03-22)** | analysis | Evolved into contradictory-instruction audit series (Lessons 136–141) — all 5 lint rule patterns corrected, all 4 prompt contradictions fixed. First-attempt rate measurement deferred until next build batch post-Lesson-141. |
| **Per-category test-gen prompt audit — contradictory instructions in mechanics, game-flow, contract, edge-cases, level-progression** | **done (2026-03-22, commit ce2e3cd)** | lib/prompts.js | 10 contradictions found and fixed (Lesson 142). #4 CRITICAL: beforeAll block contained `page.waitForTimeout(3000)` — triggered HARDCODED_TIMEOUT in 100% of generated test files. #8 CRITICAL: CT5 asserted `msg.data.duration_data` but correct path is `msg.data.metrics.duration_data` — duration contract assertions silently returned undefined in every build. #1/#2 HIGH: M1/M5 RIGHT examples taught M13 violations (direct DOM read without expect.poll after answer()). #3 HIGH: GF7 RIGHT read getLives before first syncDOMState cycle. #6 MEDIUM: R2 RIGHT showed banned `#mathai-transition-slot button` selector. #5 MEDIUM: CT4/CT6 taught `page.waitForTimeout(200)` for postMessage wait. #9 LOW: M15 offered banned toHaveClass as valid OR alternative. #7 LOW: EC2 hardcoded score assertion. #10 LOW: CT6 null-guard used silent return (false-pass). All fixed. |
| **CDN cold-start timing fix — waitForPackages 120s→180s gen rule (Lesson 143)** | **done (2026-03-22, commit b61296d)** | lib/prompts.js | Fix loop gets stuck on CDN cold-start failures: fix LLM applies cosmetic changes (CSS stripping) because it can't find a logic bug — there is none. Root cause: waitForPackages() 120s timeout < GCP CDN cold-start latency (~150s). Fix: increased waitForPackages timeout to ≥180s in gen prompt as GEN rule. Diagnostic signal documented: identical "start button never appears" across 2+ fix iterations + correct init structure = CDN timing, not HTML bug. |
| **TimerComponent null slot ID pattern (Lesson 144)** | **done (2026-03-22, commit 2a4e74b)** | lib/prompts.js | Build #527 static analysis: `new TimerComponent('headless-timer', {...})` — slot ID 'headless-timer' absent from ScreenLayout slots → constructor throws → TransitionScreenComponent teardown fails → #mathai-transition-slot button stays visible → all game-flow tests fail at post-click assertion. Fix: `new TimerComponent(null, {...})` is the correct headless pattern. WRONG/RIGHT example added to CDN_CONSTRAINTS_BLOCK. |
| **E8 script-only fix can strip CDN <script src> tags — T1 CDN_SCRIPTS_MISSING check (Lesson 145)** | **done (2026-03-22, commit 2a4e74b)** | lib/validate-static.js | Build #527 static analysis: E8 "script-only" fix sends script sections to LLM which sometimes omits CDN `<script src>` load tags in response. E8 merge then produces HTML with no CDN scripts → waitForPackages() spins 180s then throws → all tests fail with blank page. Fix: T1 error check — if waitForPackages() present but no CDN `<script src>` tags → CDN_SCRIPTS_MISSING error. T1 post-fix validation (Lesson 135) discards the bad HTML before it reaches test runner. |
| **Post-Lesson-142 violation rate measurement** | **done (2026-03-22)** | analysis | Measured across builds #525 (approved), #526 (approved), #527 (killed), #528 (approved), #529 (word-pairs, APPROVED — 2 global fix iterations, 33 min). Result: ~105 violations per build — 40 HARDCODED_TIMEOUT, 40 TRANSITION_SLOT, 25 GF8. Hypothesis FALSIFIED: violations did NOT drop. Finding (Lesson 146): ~30-40 TRANSITION_SLOT violations are FALSE POSITIVES from the `startGame()`/`clickNextLevel()` boilerplate template which legitimately uses `#mathai-transition-slot button`. The linter has no function-context awareness — it flags the template helpers' own code. Next: linter context-awareness fix to suppress template false positives. |
| **Linter context-awareness fix — exempt startGame/clickNextLevel from TRANSITION_SLOT/HARDCODED_TIMEOUT** | **done (2026-03-22, commit 06a0dbd, being verified in build #530)** | lib/pipeline-test-gen.js | Lesson 146: ~30-40 of 105 violations per build are false positives from the `startGame()`/`clickNextLevel()` boilerplate. Fix: track whether linter is inside `startGame(page)` or `clickNextLevel(page)` function bodies — suppress TRANSITION_SLOT and HARDCODED_TIMEOUT flags for those functions (pre-approved CDN navigation helpers). Expected reduction: ~40 TRANSITION_SLOT + ~40 HARDCODED_TIMEOUT → <15 each. 644 tests pass. Auto-deployed to server via git pull. Build #530 (right-triangle-area) is the first build that exercises this fix. |
| **Linter fix violation rate measurement — TRANSITION_SLOT/HARDCODED_TIMEOUT before/after** | **done (2026-03-22, commit 725713b)** | lib/pipeline-test-gen.js | Build #531 measured 78 violations total. Finding: startGame/clickNextLevel suppression IS working for TRANSITION_SLOT/HARDCODED_TIMEOUT in those functions, but other boilerplate sources (dismissPopupIfPresent, beforeEach, GF8 in startGame) still fire. Extended fix: boundary-based suppression using `test.describe(` line + GF8 added to HELPER_SUPPRESSED_RULES. Also shipped: signalCollector.trackEvent T1 check (root cause of right-triangle-area builds #527/#530/#532) + session planner Phase 1 (POST /api/session-plan). 679 tests pass. |
| **CDN timing early-exit heuristic** | **done (2026-03-22, commit 56a2baa)** | lib/pipeline-fix-loop.js | isCdnTimingFailure(errors, html): 15 CDN timeout patterns, tokenOverlap() Jaccard similarity, early-exit at iter 2+ when all errors match CDN cold-start pattern + >60% token overlap. Saves 2-4 LLM calls per affected build. 37 new tests, 716 total pass. |
| **E8 merge integrity check** | **done (2026-03-22, commit dc7dbc5)** | lib/pipeline-fix-loop.js | verifyE8MergeIntegrity(): extracts all <script src> URLs, diffs against original, sets fixedHtml=null if any CDN scripts missing. Emits e8-integrity-failed progress event. Prevents blank-page builds from E8 stripping CDN scripts (build #527 root cause). 5 new tests, 725 total pass. |
| **RULE-SYNC-1 impact measurement — startGame setTimeout ban** | **CONFIRMED (build #535, 2026-03-22)** | lib/validate-static.js, lib/prompts.js | Hypothesis: banning setTimeout in startGame() at T1 + gen prompt will eliminate game-flow 0/5 failures caused by CDN TransitionScreen slot not dismissing. Measure: soh-cah-toa-worked-example build #535 — expect game-flow tests to pass at iteration 1 (vs 0/5 in build #531). CONFIRMED if: game-flow ≥3/5 on first attempt AND no RULE-SYNC-1 T1 error emitted. HYPOTHESIS WRONG if: game-flow still 0/5 AND T1 check fires (LLM regenerated setTimeout despite gen prompt rule) OR T1 does not fire (LLM found a different failure mode). **Result (build #535):** startGame() IS synchronous — T1 §5i + gen prompt rule successfully prevented setTimeout usage. Build rejected at review for different reason (showResults pattern), not RULE-SYNC-1. RULE-SYNC-1 hypothesis confirmed. |
| **Global fix loop suppression at high per-category pass rate** | **done (2026-03-22, commit d7c0d7e)** | lib/pipeline-fix-loop.js | Hypothesis: when ≥80% of per-category tests pass before the global fix loop fires, running the global fix loop causes net harm — it regresses previously-passing categories to 0 (soh-cah-toa build #531: 6/7 categories passing → global fix → 0/11). Fix: `shouldSuppressGlobalFix()` helper exported from pipeline-fix-loop.js — if `passingCategoryCount / totalCategories >= 0.8` at Step 3c entry, skip global fix loop entirely and go straight to Step 3b final re-test. 7 new tests, 745 total passing. Expected: prevent the class of "global fix regression" failures seen in builds #531, and any future worked-example game with similar sub-phase structure. |
| **T1 validator effectiveness measurement — false positive rate across builds #530–#537** | **done (2026-03-22)** | lib/validate-static.js | Hypothesis confirmed (partial). Key findings across builds #530–#537: (1) signalCollector.trackEvent §5h — true positive in right-triangle-area #530/#532 (real hallucination caught); (2) SentryHelper §5h2 added (commit 88b965d) — SentryHelper is not a CDN global, causes infinite hang in waitForPackages(); (3) two false positives found and fixed: endGame arrow-function regex (commit e948a73) + initSentry() in HTML comment (commit e948a73); (4) async-check regex false positive on indented functions fixed (commit e948a73 lookahead). All 8 checks now validated: ≥1 true positive, ≤1 false positive each. T1 effectiveness R&D COMPLETE — docs/rnd-linter-effectiveness.md written. |
| **T1 false positive: endGame/restartGame/nextRound arrow function** | **done (2026-03-22, commit e948a73)** | lib/validate-static.js | Root cause: `/function\s+endGame\s*\(/` regex didn't match `const endGame = async (reason) => {`. Fixed: regex updated to also accept `const endGame\s*=` / `const restartGame\s*=` / `const nextRound\s*=` patterns. |
| **T1 false positive: initSentry() in HTML comment** | **done (2026-03-22, commit e948a73)** | lib/validate-static.js | Root cause: initSentry order check used `html.indexOf('initSentry()')` which matched comment text `<!-- initSentry() function definition -->`. Fixed: HTML comments stripped before position check so only actual call sites are compared. |
| **T1 §5h2: SentryHelper ban in waitForPackages()** | **done (2026-03-22, commit 88b965d)** | lib/validate-static.js | SentryHelper is not a CDN global — calling it inside waitForPackages() causes infinite hang (promise never resolves). T1 §5h2 added to ban SentryHelper usage in waitForPackages context. Gen/fix prompts updated. |
| **T1 async-check regex false positive on indented functions** | **done (2026-03-22, commit e948a73)** | lib/validate-static.js | Lookahead regex for async-check was misidentifying indented helper functions as violations. Fixed with corrected lookahead that handles indented function bodies without false triggering. |
| **RULE-RESULTS-1 gen prompt rule — showResults pattern** | **done (2026-03-22, commit db38ede)** | lib/prompts.js | showResults() must populate #results-screen content before calling transitionScreen.show('results'). Pattern derived from soh-cah-toa-worked-example build #535 review rejection. Added as gen prompt rule RULE-RESULTS-1. |
| **RULE-SENTRY-ORDER gen prompt rule** | **done (2026-03-22, commit db38ede)** | lib/prompts.js | initSentry() must be called inside waitForPackages() callback, not at top level of DOMContentLoaded. RULE-SENTRY-ORDER added to gen prompt to reinforce ordering constraint that T1 §5i already checks at static validation time. |
| **right-triangle-area CDN init failure analysis — DONE (2026-03-22). 7 layers peeled (§5h2, §5f0, §5f5, §5f6, §5f7, §5f8, §5f9). Build #543 APPROVED after 13 consecutive failures. 764 tests.** | **done (2026-03-22)** | lib/validate-static.js, lib/prompts.js | right-triangle-area had 13 consecutive build failures across 7 independent layers: Layer 1 (T1 false-positive, #530), Layer 2 (signalCollector.trackEvent, #532), Layer 3 (SentryHelper/initSentry, #536/#538), Layer 4 (Canvas API CSS variable, #540 — T1 §5f6 + gen prompt, commit cd04177), Layer 5 (progressBar.timer, #541 — T1 §5f7 + gen prompt, commit dd844f4), Layer 6 (TimerComponent slot not in ScreenLayout, #542 — T1 §5f8 + gen prompt + smoke-regen BUG 5, commits 8657a6d + ad4a15a), Layer 7 (progressBar.init() hallucination from early-review-fix, #543 — T1 §5f9 + gen prompt + smoke-regen BUG 6, commit ede9df4, 764 tests). Build #543 APPROVED 2026-03-22: 2 iterations, 1373s, 5/5 batches passed. |
| **Contract test timing improvements — reduce contract 0/2 failures from async postMessage polling** | **done (2026-03-22, commit 9eff5e6, Lesson 169)** | lib/prompts.js | Root cause analysis COMPLETE. Hypothesis was wrong: build #544 timing issue was real CDN cold-start; build #545 contract 0/2 was NOT a timing issue — it was a postMessage FIELD MISMATCH. Root cause: prompts.js line 550 had hardcoded template `metrics: { score, accuracy, time, livesRemaining, attempts, duration_data }` that taught LLM wrong field names instead of reading spec. Quadratic-formula-worked-example spec requires `{ stars, accuracy, rounds_completed, wrong_in_practice, duration_ms }`. CT5 rule also referenced same wrong field names. Fix: both rules now say "use EXACT field names from spec's postMessage contract" with type-specific examples. CT6 timing (5s poll) confirmed sufficient — no timeout increase needed. Build #546 queued to verify. Success metric: contract 2/2 on first attempt for next worked-example build. |
| **waitForPackages() pattern validation — T1 check for wrong CDN package check pattern** | **done (2026-03-22, commits f38bc2a + 2304570)** | lib/validate-static.js, lib/prompts.js | DONE: T1 §5fa added — detects while (!FeedbackManager) / while (!window.FeedbackManager) anti-pattern for all 5 CDN packages. Gen prompt rule added: typeof check MUST be used, not truthy. 773 tests. Build #544 waitForPackages review rejection root cause eliminated. |
| **CDN ProgressBarComponent API validation — enumerate all valid methods and add T1 checks + prompt rules for any remaining undefined-method patterns across other CDN components** | **done (2026-03-22, commits 25558f8 + 1cc5853)** | lib/validate-static.js, lib/prompts.js | DONE: §5f10 added banning 5 hallucinated ProgressBarComponent methods (.start/.reset/.setLives/.pause/.resume). TimerComponent .stop/.pause/.resume/.reset confirmed VALID (TypeScript defs). BUG 6 smoke-regen trigger expanded. 771 tests. Success metric pending: need 10 CDN builds to verify zero undefined-method failures. |
| **T1 §5f0: initSentry() called but not defined** | **done (2026-03-22, commit 13b7d7b)** | lib/validate-static.js, lib/prompts.js | Root cause: gen prompt said "initSentry() checks typeof SentryConfig internally" implying it was CDN-provided. LLM called initSentry() without defining function → ReferenceError at runtime → blank page. Fix: T1 §5f0 ERROR if initSentry() called but function initSentry() not defined; RULE-SENTRY-ORDER updated with canonical function body template and explicit "NOT A CDN FUNCTION" warning. 752 tests pass. |
| **soh-cah-toa-worked-example build #539 — verify RULE-RESULTS-1 + RULE-SENTRY-ORDER** | **done (confirmed via right-triangle-area #543 APPROVED, 2026-03-22)** | lib/validate-static.js, lib/prompts.js | RULE-RESULTS-1 + RULE-SENTRY-ORDER confirmed working. right-triangle-area #543 APPROVED without showResults or SentryHelper violations. Build #539 itself was superseded — soh-cah-toa requeued as later builds after additional layers fixed. Both rules active and verified. |
| **Generated test linter (Step 2c)** | **done (2026-03-22, commit 49bf948)** | lib/pipeline-test-gen.js | `lintGeneratedTests(testFiles, log)` added as Step 2c after test generation. 8 lint rules: M13 (getLives/getScore/getRound immediate assertion), M15 (CSS class immediate .toHaveClass), CT7 (wrong 'gameOver' type string), CT7_GAMEOVER (wrong 'game_over' phase name), CT6_NULL (getLastPostMessage without null-guard), GF8 (toBeVisible without prior waitForPhase), TRANSITION_SLOT (#mathai-transition-slot button selector), RULE-DUP (duplicate data-testid across test files). Violations WARN-logged + emitted as progress events — never blocks build. 17 new unit tests; 629 total pass. Deployed. Impact: fix loop now has violation context at iteration 1, enabling targeted fixes rather than blind retries. |
| **Lesson 135: endGame phase order + T1 post-fix validation** | **done (2026-03-22, commit 5d0cdb7)** | lib/prompts.js, lib/pipeline-fix-loop.js | Two fixes from consistent-failure RCA (docs/rnd-consistent-failures-rca.md): (1) GEN-110 — endGame() must set gameState.phase='game_over'; syncDOMState() BEFORE postMessage; fixes 7-build phase-stuck pattern (memory-flip, count-and-tap, light-up, one-digit-doubles, match-the-cards). (2) T1 post-fix validation — runStaticValidationLocal() after each fix LLM call; discards HTML with new T1 errors vs original; fixes 8-build CDN init corruption pattern. 629 tests pass. |
| **Pipeline reliability/autonomy/self-healing improvements** | **done (2026-03-22, partial)** | worker.js, server.js, lib/validate-static.js, lib/prompts.js | Shipped: (1) mobile viewport gen rule RULE-27 — body must not use overflow:hidden when content needs scroll + viewport meta must not block user-scalable; (2) T1 warnings for viewport+overflow violations; (3) review rejection rules: RULE-REV-1 handleGameOver bypass = rejected (endGame must call handleGameOver), RULE-REV-2 signalPayload spread = rejected (spread into postMessage loses nested metrics), RULE-REV-3 calcStars accuracy = rejected (star count must match exact spec thresholds); (4) automated queue disabled — builds now manual-only (no auto-queue cron). Remaining (planned): build health watchdog endpoint; auto-requeue on infra failure. |
| **Combined rule impact — first-attempt approval rate measurement** | **done (2026-03-22)** | analysis | Pre-rule baseline (49-build scale run): 64% first-attempt approval (iterations=0), per-category iter-2 rates mechanics 32.9%, game-flow 23.1%, contract 22.7%, edge-cases 19%, level-progression 16.1%. Now all 5 categories have rules deployed (M10-M12, GF3-GF5, CT3-CT5, EC1-EC3, LP4-LP6). **Measurement update (2026-03-22, builds 508-512, n=5 completed):** iter-2 rates: mechanics 0% (↓32.9%), game-flow 0% (↓23.1%), contract 20% (≈22.7%, 1 failure: build 509 PostMessage null×2 before passing iter 3), edge-cases 0% fixed / 40% skipped-not-fixed (2/5 builds skipped tests: 511 strict-mode selector violation, 512 wrong lives deduction assumption), level-progression 0% (↓16.1%). First-attempt approval (iter=0): 0/5 = 0% — all 5 required at least 1 fix loop run. Key findings: (1) mechanics/game-flow/level-progression rules are working — zero failures across 5 builds; (2) contract rules partially helped: CT3-CT5 targeting 3 patterns, but PostMessage null still hit disappearing-numbers (CT4/CT5 didn't prevent it); (3) edge-cases: skip rate is high (40%) — tests are being skipped as "spec ambiguous" rather than HTML being fixed, inflating apparent pass rate. |
| **claude-opus-4-6 for gen + fix models** | **done (2026-03-22)** | lib/config.js, .env | GEN_MODEL and FIX_MODEL switched from gemini-3.1-pro-preview to claude-opus-4-6 (was all-Gemini after 2026-03-20 auth outage). Hypothesis: Opus-class model produces fewer structural errors in CDN game init — fewer smoke-regen events + lower iter-1 failure rate. Success metric: smoke-regen rate + iter-1 pass rate across next 10 builds vs Gemini baseline. |
| **@file_path prompt references for Claude gen/fix** | **done (2026-03-22, commit 626021c)** | lib/pipeline.js, lib/prompts.js | Shipped: spec + prior HTML written to temp files; referenced via `@file_path` in Claude API calls instead of inlining content. Reduces prompt token count for large specs (~64KB) and enables Claude's file-aware context window management. Success: gen calls on 50KB+ specs no longer hit output token limits. |
| **PostCompact + SessionStart hooks in .claude/settings.json** | **done (2026-03-22)** | .claude/settings.json | PostCompact hook recreates all 5 crons (Build Doctor, Slack notifier, Queue Strategist, Roadmap Manager, Roadmap task check) after context compaction. SessionStart hook runs the session-start checklist (worker status, R&D slot, local test slot). Prevents background task continuity loss on compaction. |
| **Spec-driven test coverage — extractUserScenarios()** | **done (2026-03-22, commit a35d432)** | lib/pipeline-test-gen.js, lib/prompts.js | Shipped: `extractUserScenarios(specContent)` parses spec markdown for all user-facing interaction scenarios (correct answer, wrong answer, lives exhaustion, timer expiry, level skip, accessibility paths). Output injected into Step 2a as required coverage checklist — test-gen must produce ≥1 test() per scenario. Expected impact: close gap between "tests pass" and "review approves" by ensuring all reviewer-visible paths are exercised. |
| **Edge-cases skip rate root cause + fix** | **done (2026-03-22, commit b35b60c)** | lib/pipeline-fix-loop.js, lib/prompts.js | EC-TRIAGE-1 rule added — bans skip_tests for selector failures/strict-mode violations without DOM evidence; allows only for genuinely untestable scenarios. 40% false-positive skip rate targeted. |
| **hasLives heuristic refinement** | **done (2026-03-22, commit shipped)** | lib/pipeline-test-gen.js | Hypothesis confirmed. `hasLives` heuristic checked only for lives field existence, not whether the game actually uses them — `extractUserScenarios()` was injecting lives-deduction scenario for unlimited-lives games (totalLives:0). Fix: GAME FEATURE FLAGS `unlimitedLives` sentinel + totalLives===0 check added before injecting lives-deduction scenario. No lives-deduction test generated for unlimited-lives games; eliminates false-fail category on those builds. |
| **Contract PostMessage null — CT4/CT5 gap analysis + CT6** | **done (2026-03-22, commit 060b442)** | lib/prompts.js, lib/pipeline-fix-loop.js | Hypothesis confirmed. CT3-CT5 rules shipped but build 509 (disappearing-numbers) still hit PostMessage null×2 before passing at iteration 3 — CT4 (getLastPostMessage before results phase) and CT5 (flat payload assertions) did not prevent it. Root cause: test gen calls `getLastPostMessage()` synchronously inside `waitForPhase('results')` callback, racing with the CDN postMessage dispatch. Fix: added CT6 rule — always `await page.waitForTimeout(200)` after waitForPhase('results') before getLastPostMessage(); added explicit null-guard assertion before payload property access. 200ms post-results wait + null-guard shipped. Success metric: zero PostMessage null failures in contract tests across next 10 builds. |
| **Fix loop early-exit on consecutive 0/0 + both-models-fail guard** | **done (2026-03-22, commit a092b86)** | lib/pipeline-fix-loop.js | Shipped: (1) break fix loop immediately when both primary + fallback models return no-op HTML (prevents wasted iterations after API failure); (2) consecutive 0/0 guard — if two successive iterations both return 0/0 (page fully broken), break instead of continuing to a third LLM fix call. Saves 2 LLM calls per fully-broken build. |
| **Runtime dependency pre-validation (CDN + API mocking)** | **done (2026-03-22, commit 2498828)** | lib/pipeline-utils.js, lib/pipeline-test-gen.js | Implemented: extended page.on('request') to capture ALL external HTTPS URLs by resource type (scripts/XHR HEAD-checked, media/image/font saved to 'media' field in runtime-dependencies.json). buildBeforeEach() now accepts routedMediaUrls and injects page.route() that fulfills known media URLs with 204 No Content — suppresses requestfailed console noise and makes tests deterministic w.r.t. external media. 609 tests pass. Next R&D: analyze combined rule impact data from builds 508-510+ |
| **Mechanics test gen quality — reduce 32.9% iter-2 rate** | **measuring (2026-03-21, commit 26d1cf6)** | lib/prompts.js | Post-scale analysis: mechanics 32.9% iter-2 rate vs 19% edge-cases baseline. Audited failures, found 3 root causes: (M10) runtime-undefined selectors built from gameState at gen-time (`data-value="undefined"`); (M11) multi-sub-phase games where tests assert phase2 visibility after completing only 1 of N required phase1 steps; (M12) counter tests lacking `startGame(page)` reset — prior tests leave incremented state. Rules M10/M11/M12 added to buildTestGenCategoryPrompt. 592 tests pass; deployed 2026-03-21. Measure: iter-2 rate across next 20 builds vs 32.9% baseline. |
| **DOM snapshot CDN cold-start timeout (#53)** | **done (2026-03-21, commit ce79a04, Lesson 116)** | lib/pipeline-utils.js | `captureGameDomSnapshot()` detected CDN games via HTML `<script src>` patterns and increased poll deadline from 65s → 120s + final waitFor from 5s → 60s. Evidence: disappearing-numbers #475 — timeout at 5s final check after 65s poll exhausted. Impact: eliminates static-fallback on CDN games when CDN loads in <180s (120s poll + 60s final), preserving `window.gameState` shape for test-gen. 577 tests pass; deployed without restart. |
| **TimerComponent race condition + CDN load order enforcement** | **done (2026-03-21, commits 4899b4e + 274796a, Lessons 94+96)** | lib/prompts.js, lib/validate-static.js | Visual-memory #422/#439 + face-memory #446 both failed with CDN component race conditions. Two root causes: (1) TimerComponent loads at CDN step 7 — gen prompt had contradiction (line 85 said IS available, line 185 said NEVER use); (2) TransitionScreenComponent/ProgressBarComponent load at steps 4/3 — only ScreenLayout (step 2) was checked in waitForPackages(). Fix: gen prompt CDN load order table added; T1 checks 5f3b/5f3c added for TransitionScreen+ProgressBar; smoke-regen prompt extended to detect all 3 components; `/\binit\s+error\b/i` added to SMOKE_FATAL_PATTERNS. 573 tests pass. |
| **Audio/media 404 false-positive in smoke check** | **done (2026-03-21, commit c5bfa4c, Lesson 95)** | lib/pipeline-utils.js | expression-completer #444 failed Step 1d with 12× audio MP3 404s triggering SMOKE_FATAL_PATTERNS. Pattern `/failed\s+to\s+load\s+resource(?!.*status of 403)/i` excluded only 403s (CDN auth). Audio 404s are non-fatal — game still runs. Fix: extended exclusion to `40[34]`; unit test updated. Prevents false-fatal Step 1d failures for all PART-017=YES games. 573 tests pass. |
| **ScreenLayout.inject() missing slots: wrapper** | **done (2026-03-21, commit aa3a503, Lesson 97)** | lib/validate-static.js | disappearing-numbers #400: LLM called `ScreenLayout.inject('app', { progressBar: true })` without `slots:` key → ScreenLayout silently ignores options → #gameContent never created → blank page. T1 check 5e2 added: warns when ScreenLayout.inject() present but `slots:` absent in the call region. Catches silent blank-page failure before Step 1d. 573 tests pass. |
| **TimerComponent constructor wrong first argument** | **done (2026-03-21, commit 3ce80aa, Lesson 98)** | lib/prompts.js, lib/validate-static.js | disappearing-numbers #442: LLM called `new TimerComponent({ container: el, ... })` — first arg must be string container ID. Object coerces to `[object Object]`, `getElementById` returns null, crash. Gen prompt updated with explicit constructor signature. T1 check 5f4 warns on `new TimerComponent({` or `new TimerComponent(document.`. 573 tests pass. |
| **CDN script URL pre-validation before smoke-regen** | **done (2026-03-21, commit e867f36)** | lib/pipeline.js, lib/prompts.js | `checkCdnScriptUrls()` HEAD-checks all CDN script tags in failing HTML before smoke-regen. Non-200 URLs reported to LLM as "BROKEN CDN SCRIPT URLS (HIGH PRIORITY)" with exact HTTP status. Addresses CDN 404/403 failure mode that surgical init fix cannot resolve (wrong URL path vs wrong init sequence). 567 tests pass. Lesson 85. |
| **0/0 no-snapshot early-exit in per-category fix loop** | **done (2026-03-21, commit 15f9c70, Lesson 123)** | lib/pipeline-fix-loop.js | Observed in keep-track #483 + position-maximizer #484: mechanics iter 1 = 0 passed (no best snapshot saved), fix LLM ran and broke page → iter 2+3 both 0/0. Loop continued because existing guard only restores bestHtmlSnapshot (null when bestPassed=0), then `continue` ran 2 more wasted fix iterations. Fix: when `passed===0 && failed===0 && !bestHtmlSnapshot`, break immediately — no recovery path exists. Saves 2 LLM calls per category that starts at 0 passed. 599 tests pass. |
| **0/0 category false-approval gate** | **done (2026-03-21, commits 5642a23 + d758361)** | lib/pipeline.js, lib/pipeline-fix-loop.js | Two gaps fixed: (1) `zeroCoverageCats.length >= 2` threshold changed to `>= 1` — single 0/0 category now halts build before review (Gap 1); (2) `hasCrossFailures` updated to include 0/0 batches in global fix loop triggering (Gap 2). DB audit confirmed no existing approved builds had 0/0 categories (bug was latent). 6 new unit tests; 573 total pass. Lesson 86. |
| **Recurring failure patterns tracker** | **done (2026-03-21, commit cc8d63d)** | docs/failure-patterns-tracker.md | Frequency-ranked document created from DB failure_patterns table + recent build logs. 4 active patterns ranked by R&D leverage: (1) Step 1d smoke-check missing #gameContent (13/50 failed builds), (2) toBeVisible scatter (19 occurrences), (3) CDN 404 wrong URL/path (4/50 failed builds), (4) JS init error at smoke-check (2/50 failed builds). Becomes primary input for R&D slot selection. |
| **Step 1b T1 re-check null guard** | **done (2026-03-21, commit 6e4f06b)** | lib/pipeline.js | Same class of bug as Lesson 79 (cc5fae7) — one level deeper. `reStaticResult.output.split(...)` threw TypeError when `err.stdout` and `err.message` both undefined in catch path; was silently caught by outer try/catch, making the T1 re-check after contract auto-fix non-functional. Fix: `(reStaticResult.output || '').split(...)` + `(recheckErrors || []).length` + full stack trace in catch. Lesson 84. 567 tests pass. |
| **Runtime dependency pre-validation (CDN + API mocking)** | **done (2026-03-22, commit 2498828)** | lib/pipeline-utils.js, lib/pipeline-test-gen.js | Duplicate — see entry above (line ~259). Implemented. |
| **TimerComponent hallucination + phase-name normalization impact measurement** | **done (2026-03-21, commit 48e9992)** | lib/prompts.js, lib/validate-static.js, lib/pipeline-test-gen.js | Two failure patterns from builds 420-422 addressed: (1) `TimerComponent is not defined` in visual-memory and truth-tellers-liars — hallucinated CDN component; banned in gen prompt + T1 check (shipped commit 48e9992, Lesson 87); (2) phase-name normalization fix (32785d3) shipped. Hypothesis: banning TimerComponent + phase-name fix together should cut Step 1d failures by ~30% and game-flow iter-1 timeout rate by ~20%. Success: zero TimerComponent errors + game-flow iter-1 pass rate >70% across next 10 CDN builds. |
| **Screenshot-on-timeout fix context** | **done (2026-03-20, commit dc0c72f)** | Playwright config: `screenshot: 'only-on-failure'`; `collectTimeoutScreenshots()` walks JSON result tree and returns base64 PNGs for failing timeout tests (max 2); fix prompt becomes a vision content array when screenshots exist. 550 tests pass. Expected: timeout-class failures diagnosed visually in first fix iteration — LLM sees what game looked like when test hung. |
| **adjustment-strategy spec fix verification** | **measuring** | 4 root causes fixed in spec (button IDs, isProcessing race, calcStars game_over→0, postMessage fields) via commit 4789948. Builds #376 and #377 queued for verification. Awaiting first clean build with corrected spec to confirm approval in ≤3 iterations. |
| **Rate limit raised 10→20 builds/hr** | **done (2026-03-21, commit ac6588a)** | server.js or worker.js | Manually-cancelled builds consumed rate-limiter slots; 8+ admin cancellations blocked all legitimate builds for ~50 min. Raised limit gives 3× headroom. Lesson 60 in docs/lessons-learned.md. 550 tests pass. |
| **Claude CLI auth reliability → Gemini-only fallback** | **done (2026-03-20)** | .env, docs/lessons-learned.md | On 2026-03-20 ~19:07, `claude auth status` loggedIn=true but all Claude calls failed. CLIProxyAPI also returned auth_unavailable for Claude OAuth. Fix: set RALPH_USE_CLAUDE_CLI=0, switched all 5 RALPH_*_MODEL vars to gemini-3.1-pro-preview. Pipeline runs fully on Gemini. Also fixed RALPH_TEST_CASES_MODEL (was separate from RALPH_TEST_MODEL — hidden gotcha). Also fixed silent test-gen failure swallowing auth errors (commit a890454). Lessons 61+62 in docs/lessons-learned.md. |
| **Gemini-only pipeline quality verification** | **done (2026-03-21)** | Builds #381 (adjustment-strategy), #382–#384 (matching-doubles) | matching-doubles #384 APPROVED 13/13 tests, all-Gemini pipeline confirmed functional. gemini-3.1-pro-preview generates valid test-cases.json (JSON block format), passes static validation, and produces approvable HTML. Hypothesis confirmed. |
| **Fix skip_test/skip_tests triage mismatch** | **done (2026-03-21, commit 5158275)** | lib/pipeline-fix-loop.js, lib/prompts.js | Normalization added: `if (triageDecision === 'skip_test') triageDecision = 'skip_tests'` after JSON parse. Prompt JSON schema also corrected to `skip_test`. Lesson 63. Prevents unnecessary fix LLM calls → HTML corruption cascade. |
| **Fix non-CDN startGame() helper** | **done (2026-03-21, commit 3df4a3e)** | lib/pipeline-test-gen.js | `startGame()` and `clickNextLevel()` now conditional on `hasTransitionSlot`. Non-CDN games use generic button selectors then `waitForPhase(page, 'playing')` fallback. Lesson 64. Prevents 0/0 cascade on all 5 categories for non-CDN games. |
| **Non-CDN test gen quality verification** | **done (2026-03-21)** | Build #385 (adjustment-strategy) APPROVED | Hypothesis confirmed: adjusted `startGame()` helper produced valid tests for non-CDN game. adjustment-strategy #385 passed game-flow 2/2, mechanics 6/6 (with fix), level-progression 1/1, edge-cases 1/1 (1 skip), contract 1/1. No all-skip cascade. |
| **Review rejection rate analysis** | **done (2026-03-21, commit 8d76c48)** | Builds #384–#386 review reports | Analysis complete. 4 patterns found: (1) postMessage payload incomplete — fixed by gen-prompt rule; (2) calcStars never called — fixed; (3) Sentry not initialized — fixed; (4) debug functions on window — fixed (rule conflict reversed). Gen-prompt rules added in commit 8d76c48. bubbles-pairs #386 needed 2 review-fix iterations (3 review calls, 455s) — root-cause confirmed and pre-empted. |
| **Global fix 0/0 regression guard** | **done (2026-03-21, commit 25ca9e6)** | lib/pipeline-fix-loop.js, lib/prompts.js | See P8 table above. 550 tests pass; active on server via auto-pull. |
| **count-and-tap failure root cause — CDN classification fix** | **done (2026-03-21, commit 54dd4b7)** | lib/pipeline-test-gen.js | Root cause confirmed: count-and-tap uses `ScreenLayout.inject()` which injects `#mathai-transition-slot` at runtime — it is absent from raw HTML. `hasTransitionSlot` detection only checked raw HTML, so count-and-tap was classified as Non-CDN → `startGame()` used 2s timeout fallback path instead of CDN slot click → always timed out before game started. Fix: extended `hasTransitionSlot` / `hasSlot` detection to check for `ScreenLayout.inject`, CDN package URL (`test-dynamic-assets/packages/components`), and `TransitionScreenComponent` reference in HTML. Both boilerplate generation and post-processing beforeEach fixup now use the extended detection. Measure: count-and-tap #440+ should approve in ≤2 iterations. |
| **First-attempt approval rate measurement** | **done (2026-03-21, R&D #65)** | Builds #388-447 | Completed analysis: 0% all-time first-attempt rate, 29% in recent epoch (builds 350+), 5.5x per-build approval rate improvement (4.6% → 25.4%). Critical finding: ALL approved builds in recent epoch had iterations=0 — fix loop does NOT rescue bad gens; gen quality is the only lever. 20 never-approved games identified. Root cause of iterations=0 pattern: silent pipeline failures (db.completeBuild() never wrote error_message; iterations always 0 in DB). Both fixed in commit 4131eca. Full analysis in docs/rnd-first-attempt-rate.md. |
| **Never-approved games root cause + gen prompt improvements** | **done (2026-03-21)** | 20 games across builds 411-447+ | Completed: waitForPackages/PART-017=NO fix shipped (fd7a36c); debug-window rule corrected (d827777); 0/0 approval gate tightened (e843db2); CSS comma-ID escaping added (e843db2); audio URL CDN constraint added (a32341d); fixCdnDomainsInFile audio corruption fixed (7adcb0f). Research phase complete — analysis docs written (docs/rnd-test-failure-analysis-global.md, docs/rnd-test-failure-analysis-per-spec.md). 4 concrete fixes shipped, 3 already deployed. |
| **Phase name injection from DOM snapshot** | **done (2026-03-21, commit 7796007)** | lib/pipeline-test-gen.js | Post-processing reads dom-snapshot.json `gameStateShape.phase` after test-gen; replaces `waitForPhase(page, 'playing')` with actual runtime phase when non-standard (not in STANDARD_PHASES set). Only fires when dom-snapshot.json exists, phase is non-standard, and tests contain 'playing' reference. Lesson 75 in docs/lessons-learned.md. 558 tests pass; deployed to server. |
| **Null error_message build failures — pre-pipeline guard** | **done (2026-03-21, commit 89a97f2)** | worker.js | Top-level try/catch wraps entire BullMQ processor body; extracts buildId before try so crash in pre-pipeline setup (git pull, spec fetch, warehouse hygiene) still calls db.failBuild(). Pre-flight writability check throws clear error before LLM calls if output dir unwritable. 558 tests pass; deployed to server without restart (build #421 running). |
| **Contract auto-fix T1 re-check crash** | **done (2026-03-21, commit cc5fae7)** | lib/pipeline.js | Step 1b re-check after contract auto-fix accessed `reStaticResult.errors.length` — property doesn't exist on `{ passed, output }` return type. Threw TypeError silently caught; T1 re-check was non-functional in all builds. Fix: use `!reStaticResult.passed` + parse errors from output string. Lesson 79. 558 tests pass. |
| **Sentry captureConsoleIntegration crash** | **done (2026-03-21, commit 562387c)** | lib/prompts.js | CDN_CONSTRAINTS_BLOCK told LLM to use Sentry.captureConsoleIntegration() — only in separate captureconsole.min.js not loaded by default. Calling it crashes initSentry() before ScreenLayout.inject() → #gameContent never created → smoke check fails. Fix: banned captureConsoleIntegration, mandated omitting integrations entirely. 558 tests pass; deployed to server. |
| **TimerComponent ordering after template clone + T1 ERROR upgrade** | **done (2026-03-21, commit 7e8688d, Lesson 99)** | lib/validate-static.js, lib/prompts.js | keep-track #452/#427: two compounding bugs — wrong constructor API (same as Lesson 98) AND calling new TimerComponent() before template clone (timer-container inside `<template>` is null until ScreenLayout.inject() + cloneNode). T1 5f4 upgraded WARNING→ERROR forces static-fix LLM to correct both. buildSmokeRegenFixPrompt() extended with ordering requirement. Lesson 99. |
| **T1 initSentry ordering: false positive on function declaration** | **done (2026-03-21, commit e91c637)** | lib/validate-static.js | `html.indexOf('initSentry()')` matched function declaration body `function initSentry() {` — same substring. Fixed with negative lookahead regex `/initSentry\s*\(\s*\)(?!\s*\{)/` so only actual call sites are flagged. Eliminated false-positive T1 ERROR that was blocking valid CDN games in Step 1a. |
| **PART-006 contradiction: spec YES with implementation note NO** | **done (2026-03-21, commit 6eb7c4a, Lesson 100)** | specs/Two Player Race.md | PART-006=YES triggered TimerComponent CDN class; parenthetical "manual setInterval, not TimerComponent class" was ignored. Changed PART-006→NO in spec. Secondary: LLM hallucinated generic audio paths instead of spec's cdn.mathai.ai URLs. Smoke check 403 exclusion already covered false alarm. two-player-race #466 approved 2 iterations. |
| **unawaited transitionScreen.show() upgraded WARNING → ERROR** | **done (2026-03-21, commit 42830fd, Lesson 101)** | lib/validate-static.js, lib/prompts.js | keep-track #465: unawaited initial transitionScreen.show() corrupts CDN state machine — subsequent await show() calls silently hang with button visibility:hidden. Gen prompt RULE-008 updated: await REQUIRED on ALL calls including initial start-screen call (exception removed). T1 check upgraded ERROR with full explanation. |
| **Test-gen M6 rule: await isActive before answer() in shuffle games** | **done (2026-03-21, commit 42830fd, Lesson 102)** | lib/prompts.js | keep-track #465: test called answer() immediately after startGame() but game was in shuffle/reveal phase (gameState.isActive=false) — clicks silently swallowed, transition button never appeared. M6 rule added to buildTestGenCategoryPrompt: for animation/reveal-phase games, await `expect.poll(() => page.evaluate(() => window.gameState?.isActive === true), { timeout: 15000 }).toBeTruthy()` before first answer(). |
| **Test-gen M7 rule: read correctCup dynamically, not hardcoded signal-id** | **done (2026-03-21, commit 42830fd, Lesson 103)** | lib/prompts.js | keep-track #465: test used hardcoded `data-signal-id` to identify correct cup after shuffle — but signal-id tracks original position, not post-shuffle position. M7 rule added: for shell/shuffle games, read `window.gameState?.correctCup ?? window.gameState?.correctIndex ?? 0` dynamically before each tap. |
| **fixCdnDomainsInFile Fix 2 corrupting valid cdn.mathai.ai audio URLs** | **done (2026-03-21, commit e81f410, Lesson 104)** | lib/pipeline.js | Fix 2 was replacing ALL cdn.mathai.ai occurrences globally — including valid audio/media asset URLs from specs (HTTP 200). Converted them to storage.googleapis.com/test-dynamic-assets/mathai-assets/... which 404s. Root cause of hide-unhide #426/#449 failures. Fix: scope Fix 2 to cdn.homeworkapp.ai only (universally wrong, 403). Fix 1 already handles cdn.mathai.ai in `<script src>` specifically. 573 tests pass. |
| **T1 typeof-check WARNINGs → ERRORs for CDN component race** | **done (2026-03-21, commit d2a3324, Lesson 106)** | lib/validate-static.js, test/validate-static.test.js | TimerComponent, TransitionScreenComponent, ProgressBarComponent typeof-check WARNINGs upgraded to ERRORs. Missing typeof guards cause 100% blank-page failures (CDN race) — true-or-false #436 passed T1 as WARNINGs but failed Step 1d. ERRORs force static-fix LLM to add guards. 4 new unit tests. 577 tests pass. |
| **beforeEach CDN poll 50s→120s + test timeout 90s→180s** | **done (2026-03-21, commit 89149d4, Lesson 107)** | lib/pipeline-test-gen.js, lib/pipeline-utils.js | CDN cold-start takes 60-120s on server. 50s poll expired before CDN loaded — same HTML fails on server but passes locally. Increased poll deadline to 120s and Playwright test timeout to 180s (120s poll + 55s buffer for test logic + animation phases). Unblocks keep-track and count-and-tap CDN timing failures. 577 tests pass. |
| **Reveal-phase games: isProcessing timing + M8 test-gen rule** | **done (2026-03-21, commit 833da9f, Lesson 109)** | lib/prompts.js, docs/lessons-learned.md | count-and-tap #457: renderRound() set isProcessing=false immediately but option buttons appeared 1.5s later in setTimeout. harness answer() polled isProcessing=false, found it, querySelectorAll('.option-btn') → empty → click silently failed. Timer fired 10s later, test desync'd. Fix: (1) ROUND LIFECYCLE RESET rule updated with EXCEPTION for reveal-phase games — keep isProcessing=true until reveal timeout fires and options rendered; (2) M8 mechanics rule: wait for .option-btn visible before calling answer() in reveal-phase games. 577 tests pass. Deployed without restart. |
| **Contract test-gen direct gameState mutation → 0/0 evidence gate** | **done (2026-03-21, commit 97b1cc0, Lesson 108)** | lib/prompts.js | visual-memory #456 failed at Step 4 (0/0 contract evidence gate). Contract test assigned `window.gameState.score = X` directly — triage skip_tested all contract tests → 0 run → gate tripped. Fix: (1) triage KNOWN TEST BUGS: direct gameState assignment is always skip_test; (2) test-gen OUTPUT INSTRUCTIONS: NEVER assign to window.gameState — use answer()/skipToEnd() + getLastPostMessage(). 577 tests pass. Deployed without restart. |
| **Contract-fix LLM breaks Sentry order during full HTML rewrite** | **done (2026-03-21, commit 67c868e, Lesson 110)** | lib/prompts.js | keep-track #465: original gen had initSentry() correctly INSIDE waitForPackages(). Contract-fix LLM (full HTML rewrite) accidentally moved it outside → T1 FORBIDDEN: initSentry() before waitForPackages() → failed after 3 iters. CDN_CONSTRAINTS_BLOCK has the rule but LLM ignored it. Fix: added VERIFY BEFORE RETURNING checklist to buildContractFixPrompt with explicit initSentry() guard + window exports + CDN script order. |
| **T1 5f2: captureConsoleIntegration also not in CDN bundle** | **done (2026-03-21, commit 3d8528c, Lesson 105)** | lib/validate-static.js | T1 warning 5f2 incorrectly recommended Sentry.captureConsoleIntegration() as fix for new Sentry.Integrations.CaptureConsole() — but captureConsoleIntegration is ALSO absent from CDN bundle (throws "not a function", aborts initSentry()). Root cause of light-up #428. Fix: T1 5f2 now says OMIT integrations entirely (pass [] or no arg). New T1 check catches captureConsoleIntegration() calls as WARNING. |
| **Per-spec failure analysis fixes** | **done (2026-03-21, commit 576f3a2)** | lib/pipeline-test-gen.js, lib/prompts.js | Applied 2 unshipped fixes from per-spec analysis: (1) `.resolves` post-processing fixup — strips `.resolves` from `expect(await ...).resolves.toBe()` patterns (throws on non-Promise); (2) R6 rule: use `{ timeout: 10000 }` on toBeVisible after startGame() for CDN games; R7 rule: ban `.resolves` on awaited values. All other recommendations were already shipped. 558 tests pass; deployed to server. |
| **CDN init error visibility via window.__initError** | **done (2026-03-21, commit 842c649)** | lib/pipeline-test-gen.js, lib/prompts.js | `beforeEach` now reads `window.__initError` after page.goto() and logs it before the 50s slot-button poll — CDN package load failures visible in test output instead of silent 50s timeout. CDN_CONSTRAINTS_BLOCK rule added: "DOMContentLoaded catch block MUST set window.__initError = e.message". 562 tests pass; deployed without restart. |
| **associations chronic failure root cause** | **done (2026-03-21, commit 7a1ee80)** | specs/Associations.md | Root cause analysis complete (4 causes across 15 builds). Fix applied: CRITICAL no-lives note (unlimited-lives accuracy game — no hearts tests) + correct fallbackContent structure (`{ emoji, name }` pairs, not SignalCollector API names). Build 447 queued with fixed spec. Lesson 80. |
| **Corrupt fallbackContent detection** | **done (2026-03-21, commit 668c087)** | lib/pipeline-test-gen.js | `detectCorruptFallbackContent()` exported function: checks if >50% of question/answer values match 10-member CDN API name set (Event, Target, Input, Action, etc.). If corrupt, nullifies capturedGameContent and falls through to extractSpecRounds path. 4 new unit tests; 562 total pass. Deployed to server without restart. |
| **Smoke-regen effectiveness: improve #gameContent recovery rate** | **measuring (fix shipped 2026-03-21, commit 2666e36)** | lib/pipeline.js, lib/prompts.js | Fix shipped: CDN_CONSTRAINTS_BLOCK updated with exact ScreenLayout.inject() format (slots wrapper); smoke-regen prompt includes CORRECT/WRONG example. Now tracking whether `smoke-check-failed` events drop in upcoming CDN builds. Metric: compare smoke-regen repeat-failure rate before fix (~40%) vs post-fix across next 10 CDN builds. Success criterion: repeat-failure rate <10%. |
| **Include full error messages in fix loop failure descriptions** | **done (2026-03-20, commit d436b2c)** | lib/pipeline-fix-loop.js | Fix loop failure descriptions now include full error messages instead of truncated summaries — LLM fix prompt has full context for root-cause diagnosis. Previously, error messages were being summarized, causing the fix LLM to diagnose based on partial information. |
| **Enforce test.describe() API in Playwright test gen** | **done (2026-03-20, commit 4a6314c)** | lib/pipeline-test-gen.js, lib/prompts.js | Test generation prompt now explicitly enforces `test.describe()` wrapper API — prevents LLM from generating flat `test()` calls at the top level, which Playwright rejects with "test() not expected here" when run outside a describe block. |
| **Show pipeline errors in Slack failure messages** | **done (2026-03-20, commit 948e455)** | worker.js, lib/slack.js | Worker now includes pipeline error details in Slack failure notifications — operators see the actual error reason (e.g., "Step 1d: Page load failed") in Slack instead of just "FAILED". Eliminates needing to SSH and check logs to know why a build failed. |
| **Fix CLI gen timeout: apply RALPH_GEN_LLM_TIMEOUT to Claude CLI path** | **done (2026-03-20, commit efd2bdc)** | lib/pipeline.js or ralph.sh | The separate `RALPH_GEN_LLM_TIMEOUT` (600s) was only applied to API path; CLI (`claude -p`) generation calls still used the shorter `RALPH_LLM_TIMEOUT`. Fixed to use the longer timeout for both paths — large-spec games no longer time out on the CLI gen path. |
| **Fix learning extraction: handle string failures from fix-loop** | **done (2026-03-20, commit c3511d5)** | worker.js or lib/pipeline.js | Learning extraction crashed when `failures` array contained string entries (from fix-loop error strings) instead of structured objects. Added guard: strings are skipped or normalized before extraction. Prevents learning-extraction from throwing on every build with string-format failures. |
| **Step 1d regen: #gameContent missing after regen (bubbles-pairs pattern)** | **done (2026-03-21, commit c70b0d3)** | lib/pipeline.js | `smokeErrorContext` now explicitly states: game MUST include `<div id="gameContent">`, CDN script URLs must match spec. Previously only passed raw error string — Gemini would regenerate without the required div. Task #62. |
| **Step 1d regen: CDN packages failed context clarification** | **done (2026-03-21, commit a4a7903)** | lib/pipeline.js | Refined smoke-regen context further: "missing #gameContent" on CDN games means ScreenLayout.inject() never ran (CDN packages failed to load) — NOT that a static div is missing. Correct fix instructions: fix CDN script URLs, waitForPackages timeout, initSentry order, and ScreenLayout.inject() call sequence. associations #392 CDN smoke-check triggered this path. |
| **Re-queue right-triangle-area** | **done (2026-03-22, build #530 queued)** | — | Build #527 failed due to now-fixed issues: TimerComponent null slot (Lesson 144) + CDN script T1 check (Lesson 145). Both fixes shipped in commit 2a4e74b. Re-queued as build #530 after Lessons 143-145 confirmed deployed (d892c0e on server). Running now. |
| **CDN timing early-exit heuristic v2 — string-history similarity** | **CONFIRMED (build #546, 2026-03-22, commit 745e637)** | lib/pipeline-fix-loop.js | Hypothesis confirmed: `detectCdnTimingStall()` — token similarity >75% between iter-N and iter-N+1 failure messages + CDN timeout pattern matching → break fix loop early, emit `cdn-timing-early-exit`. Build #546 live proof at 11:57:44: game-flow 1/4 → 2/4 across 2 iterations, stall detected, loop broke early instead of burning iter 3+. 14 unit tests; 787 total pass. |
| **E8 merge integrity check** | **done (2026-03-22, commit dc7dbc5, CONFIRMED build #546)** | lib/pipeline-fix-loop.js | Hypothesis confirmed: `verifyE8MergeIntegrity()` extracts all `<script src>` URLs, diffs against original, sets `fixedHtml=null` if any CDN scripts missing. Build #546 logs at 12:05:29: "E8 merge integrity check failed — missing 7 CDN scripts", correctly fell back to full HTML fix. 5 new tests; 725 total pass. Zero T1 CDN_SCRIPTS_MISSING errors after E8 merge in verified build. |
| **Contract test gen quality — ban invalid expect.poll() syntax** | **done (2026-03-22, build #549 APPROVED)** | lib/prompts.js, lib/pipeline-test-gen.js, lib/validate-static.js | CT8 (expect.poll() ban) + GEN-111 (MCQ correctAnswer) + GEN-112 (ProgressBarComponent API, Lesson 172) all active in build #549. Root cause of build #547 100% failure: wrong ProgressBarComponent API → progressBar null → renderRound() throws → transitionScreen never dismisses. Fix: T1 PART-023-API/PART-023-UPDATE + GEN-112 prompt rule. Build #548 rejected (new date() typo). Build #549 running with all 3 fixes. 787 tests pass. |
| **FeedbackManager.init() ban — eliminate top review rejection cause (GEN-113)** | **active — needs more CDN builds with FeedbackManager to verify zero PART-011-INIT fires; build #550 failed for unrelated reason (GEN-114)** | lib/prompts.js, lib/validate-static.js | GEN-113 (27% of review rejections). T1 PART-011-INIT check + CDN_CONSTRAINTS_BLOCK WRONG/RIGHT example. Hypothesis: FeedbackManager.init() review rejections eliminated from all future CDN builds using FeedbackManager. 789 tests pass. Verification: need 3+ CDN builds that use FeedbackManager to confirm zero PART-011-INIT T1 fires. |
| **window.loadRound exposure — test harness jumpToRound() fix (GEN-114)** | **verified in #555/#557 (approved, iter=3, 2026-03-22)** | lib/prompts.js, lib/validate-static.js | GEN-114: CDN round-based games must expose `window.loadRound = function(n) { gameState.currentRound = n - 1; nextRound(); }`. Without it, `__ralph.jumpToRound()` is a silent no-op — sets currentRound but never updates phase, leaving data-phase='results' from prior endGame() call → 100% mechanics/level-progression failure. Root cause of name-the-sides build #550 (0/6 × 3 iters). T1 PART-021-LOADROUND warning added. 793 tests pass. Build #552 failed for GEN-115 reason. Build #553 failed for GEN-116 reason (interactionType false-positive). Build #554 queued with all three fixes deployed. Build #555 APPROVED (reviewer) — confirmed present in approved HTML; failed post-approval due to infra (warehouse dir owned root:root). |
| **Contract auto-fix T1 regression → early-review rejection (GEN-115)** | **verified in #555/#557 (approved, iter=3, 2026-03-22)** | lib/pipeline.js, lib/pipeline-fix-loop.js | Root cause of build #552 rejection: contract auto-fix (Step 1b) stripped max-width CSS + broke ...signalPayload spread → T1 errors baked in → early reviewer correctly rejected twice. Fix: pipeline now captures T1 errors introduced by contract-fix and passes them into iteration 1's fix prompt (`contractFixT1Errors` ctx field). Build #553 failed for GEN-116 reason before this fix could be verified. Build #554 verifies. 793 tests pass. Build #555 confirmed no T1 regression — contract 1/1 passed. |
| **Interaction type false-positive on drag prohibition text (GEN-116)** | **verified in #555/#557 (approved, iter=3, 2026-03-22)** | lib/pipeline-utils.js | Root cause of name-the-sides build #553 (0p/3f game-flow, 0p/5f mechanics, 0p/2f edge-cases, 0p/1f contract — global best 0 passing). `extractSpecMetadata()` regex `/drag[\s-]?(?:and[\s-]?drop|drop)|draggable/i` matched prohibition text "Do NOT use drag-and-drop" (spec.md line 681) → `interactionType=drag` for an MCQ game → DOM snapshot metadata, test harness answer() routing, and test generator all assumed drag interaction → 100% failure. Fix: `dragProhibited` guard — only classify as drag if drag is present AND spec does NOT contain prohibition phrase `\b(?:do\s+not|avoid|no|without)\s+(?:use\s+)?drag/i`. 793 tests pass. Build #555 confirmed correct interactionType=mcq-click — not triggered. |
| **GEN-117** | **verified in #555/#557 (approved, iter=3, 2026-03-22)** | lib/prompts.js, lib/validate-static.js | transitionScreen.hide() mandate: startGame() must call hide() to reveal #gameContent. ScreenLayout.inject() sets #gameContent to display:none; CDN TransitionScreenComponent does NOT auto-reveal it — caller must call transitionScreen.hide() explicitly. Without it, all isVisible() checks fail. Root cause of build #554 kill (iter=0, confirmed init failure). Gen prompt line 128 WRONG/RIGHT example updated; CORRECT PATTERN at line 369 updated; T1 PART-025-HIDE ERROR check added (validate-static.js line 209). Build #555 confirmed present in approved HTML. |
| **GEN-118: startGame() must explicitly show #gameContent after transitionScreen.hide()** | **verified in #557 (approved, iter=3, 2026-03-22)** | lib/prompts.js, lib/validate-static.js | Root cause of build #556: transitionScreen.hide() does NOT auto-reveal #gameContent; startGame() must call document.getElementById('gameContent').style.display = 'block'. GEN-117 rule was incomplete. T1 PART-026-GAMECONTENT WARNING added. Build #557 APPROVED at iter=3 — init failure class resolved. Note: iter=3 not iter=1; first-attempt game-flow data from which-ratio (#558) will clarify whether GEN-118 alone achieves iter=1 approval. |
| **GEN-119: fallbackContent JS SyntaxError** | **verified in #559 (APPROVED by reviewer, iter=1 game-flow data pending post-approval, 2026-03-23)** | lib/prompts.js, lib/validate-static.js | Root cause: fallbackContent object literal missing closing brace on own line → JS SyntaxError killed DOMContentLoaded → blank page. T1 PART-027 check + gen prompt rule shipped (commit 8c2fd92). Build #559 APPROVED 8/10 by reviewer — EACCES post-approval infra failure (warehouse dir root-owned); infra fixed, #560 re-queued. |
| **CT9: ban transition-slot visibility in contract tests** | **shipped (2026-03-23)** | lib/prompts.js | Root cause B of contract 30.8% pass rate: `#mathai-transition-slot button` visibility assertion was flaky precondition. CT9 bans it; use waitForPhase only. C1 timeout fixed 10000→20000ms. |
| **RULE-003 transitionScreen try/catch** | **shipped (2026-03-23)** | lib/prompts.js | Build #560 review rejection: transitionScreen.hide()/show() calls without try/catch. Explicit WRONG/RIGHT example added to CDN_CONSTRAINTS_BLOCK and REVIEW_SHARED_GUIDANCE. |
| **PART-028 CSS-stripped T1 check + FIX-001 preserve-CSS rule + ARIA-001 feedback accessibility** | **done (2026-03-23, commit dc03155)** | lib/validate-static.js, lib/prompts.js | PART-028: T1 static check detects when CSS stripping in E8/fix prompts removes structural layout styles (position:fixed, display:flex, z-index) — logs warning and falls back to full HTML. FIX-001: CDN_CONSTRAINTS_BLOCK rule: never strip CSS when applying structural or visibility fixes — CSS stripping is safe only for cosmetic style-only sections. ARIA-001: feedback element accessibility — gen prompt rule requires aria-live="polite" on feedback/status display divs so screen readers announce correct/incorrect state changes. |
| **Slack parent message live updates** | **done (2026-03-23, commit 9101650)** | worker.js, lib/slack.js | Parent Slack thread message now updated in real-time at each pipeline step boundary and on failure — progress visible in channel without scrolling thread. Previously only posted at build start and end. |
| **Cross-slot feed links** | **done (2026-03-23, commit 4f96009)** | worker.js, lib/slack.js | UI/UX findings now route to R&D slot (gen prompt rules), test quality slot (test-gen fixes), and education slot (spec revisions) via Slack thread links. Closes the loop between audit findings and implementation. |
| **Cron 7 Slot Watchdog** | **done (2026-03-23, commit c57447d)** | .claude/settings.json | Detect and act on idle slots every 5 min — checks if any of the 4 mandatory slots (R&D, test quality, education, UI/UX) has gone silent, and triggers re-activation with next-task prompt. Prevents slots from silently stalling between user messages. |
| **Post-GEN-116/117/118 first-attempt approval rate measurement** | **active — 3 data points collected, need 2 more CDN builds** | analysis | GEN-116 (drag prohibition false-positive), GEN-117 (transitionScreen.hide() mandate), and GEN-118 (explicit #gameContent show) are the three highest-impact gen-level init fixes shipped in this session. Hypothesis: these three rules together eliminate the class of "startGame init failure → all game-flow 0/N on every iteration" which caused name-the-sides to require 7 builds across 5 independent failure layers. Data point 1: build #557 (name-the-sides) APPROVED iter=3 — init failure class resolved, GEN-119 (fallbackContent SyntaxError) discovered as new blocking bug. Data point 2: build #559 (which-ratio) APPROVED 8/10 by reviewer — EACCES infra failure post-approval, not an HTML bug; #560 re-queued. Both approved without game-flow 0/N init failure, supporting hypothesis. Data point 3: build #561 (which-ratio) APPROVED iter=3 — no game-flow 0/N init failure. Pattern holds: all 3 recent CDN builds with ScreenLayout.inject() approved without init failure class. 2 more data points needed. Measure: track game-flow iter-1 pass rate + first-attempt approval rate across next 5 CDN builds using ScreenLayout.inject(). Success: ≥3/5 CDN builds with ScreenLayout approve in ≤2 iterations AND no game-flow 0/N iter-1 failures caused by missing #gameContent show. |
| **T1 §5f6: Canvas API CSS variable check** | **done (2026-03-22, commit cd04177)** | lib/validate-static.js | Lesson 158 (build #540 right-triangle-area): LLM passed `'var(--color-sky)'` to `addColorStop()` — Canvas2D API does not resolve CSS custom properties, only literal color strings work. T1 §5f6 detects `addColorStop\s*\(\s*[\d.]+\s*,\s*['"]var(` pattern and flags as ERROR. Also checks `fillStyle\s*=\s*['"]var(` and `strokeStyle\s*=\s*['"]var(`. Gen prompt rule: "Canvas API does not resolve CSS variables — use literal hex/rgba". |
| **T1 §5f8: TimerComponent slot not in ScreenLayout** | **done (2026-03-22, commits 8657a6d + ad4a15a, 762 tests)** | lib/validate-static.js, lib/prompts.js | Lesson 160/162 (build #542 right-triangle-area): `new TimerComponent('mathai-timer-slot', ...)` with `timer: true` missing from ScreenLayout slots — div never created, constructor throws 'Container with id "mathai-timer-slot" not found' → blank page. T1 §5f8 detects this pattern. Gen prompt rule + smoke-regen BUG 5 provide defense in depth. 762 tests pass. |
| **T1 §5f9: progressBar.init() hallucination** | **done (2026-03-22, commit ede9df4, 764 tests)** | lib/validate-static.js, lib/prompts.js | Lesson 163 (build #543 right-triangle-area Layer 7): early-review-fix introduced `progressBar.init()` — method does not exist on ProgressBarComponent. API is EXACTLY: constructor(slotId, config) + .update(currentRound, totalRounds) + .destroy(). No .init(), .start(), .reset(), .setLives(). T1 §5f9 catches `progressBar.init(` pattern as ERROR. Gen prompt rule lists all 3 allowed methods explicitly. Smoke-regen BUG 6 added as defense in depth. Smoke PASSED in build #543 — first time in 13 builds. 764 tests pass. |
| **T1 §5f7: ProgressBarComponent .timer ban** | **done (2026-03-22, commit dd844f4, 760 tests)** | lib/validate-static.js, lib/prompts.js | Lesson 159 (build #541 right-triangle-area): LLM assigned `timer = progressBar.timer` — ProgressBarComponent does NOT expose a `.timer` property; `progressBar.timer` is always undefined → `timer.start()` throws TypeError → blank page. T1 §5f7 detects `progressBar\.timer` pattern and flags as ERROR. Gen prompt rule: "ProgressBarComponent DOES NOT EXPOSE a .timer PROPERTY — always create TimerComponent separately." Build #542 queued. |
| **passRate < 0.5 false-fail when triage deletes all batch spec files** | **done (2026-03-21, commit dc20844)** | lib/pipeline-fix-loop.js | When skip_tests deletes all spec files in a batch, per-batch loop accumulated failures in `totalFailed`. Step 3b skips deleted batches so those failures were never corrected. passRate = (remaining passing) / (total including deleted failures) fell below 0.5, failing the build before review. Fix: subtract deleted batches from totals before Step 3b. Interactive-chat #387 failed with passRate=5/12=42%; re-queued as #390. Lesson 66. |
| **Early-review-fix CDN protection** | **done (2026-03-21, commit e7b3805)** | lib/prompts.js | `buildEarlyReviewFixPrompt` had "Preserve CDN initialization order exactly" but Gemini still rewrote CDN script tags when fixing spec compliance. Strengthened to explicit "DO NOT TOUCH" list: all `<head>` script/link tags, CDN package loading, Sentry init, mathai-transition-slot, window.gameState. kakuro #389 failed Step 1d after early-review-fix broke CDN init; re-queued as #391. |
| **GEN rule: min-height 44px on all interactive buttons** | **DONE — GEN-UX-002 (2026-03-23)** | lib/prompts.js | GEN-TOUCH-TARGET: CDN_CONSTRAINTS_BLOCK line 121 + buildGenerationPrompt() rule 32 (lines 536–539). Shipped. 7 confirmed instances. |
| **GEN rule: results screen must be position:fixed overlay** | **pending** | lib/prompts.js | Source: UI/UX audit name-the-sides (UI-NS-004) + find-triangle-side (UI-FT-001). Two confirmed instances. Results screen uses `position: static` — stacks in DOM flow, clipped by `overflow: hidden` on `#app` container. On mobile, "Play Again" button unreachable. Fix: gen prompt rule requiring results screen to use `position: fixed; top: 0; left: 0; width: 100%; height: 100%; z-index: 100` or equivalent overlay pattern. Measure: zero off-screen results buttons in next 3 CDN build UI/UX audits. |
| **GEN rule: ProgressBarComponent slot ID must be exactly 'mathai-progress-slot'** | **DONE — GEN-UX-003 (25bdad0) 2026-03-23** | lib/prompts.js | Source: UI/UX audit find-triangle-side (UI-FT-002). 4 confirmed instances. SHIPPED: CDN_CONSTRAINTS_BLOCK + buildGenerationPrompt() numbered rules with WRONG/RIGHT examples. |
| **GEN rule: never use local asset paths in TransitionScreen icons** | **pending** | lib/prompts.js | Source: UI/UX audit find-triangle-side (UI-FT-005). `icons: ['assets/game_find_triangle_side_icon.svg']` will 404 in GCP-hosted builds — there is no local assets directory. Fix: gen prompt rule: "TransitionScreen icons must be either an empty array `[]` or emoji strings `['🔢']`. Never reference local file paths — they do not exist in the deployed build." Measure: zero 404 icon requests in next 3 CDN build audits. |
| **GEN rule: always pass constructor args to SignalCollector** | **DONE — GEN-UX-005 (25bdad0) 2026-03-23** | lib/prompts.js | Source: UI/UX audit find-triangle-side (UI-FT-006), real-world-problem #564. 2 confirmed instances. SHIPPED: CDN_CONSTRAINTS_BLOCK + buildGenerationPrompt() numbered rule with canonical { sessionId, studentId, templateId } signature. |
| **GEN rule: never use alert() or confirm() in game code** | **DONE — GEN-UX-004 (25bdad0) 2026-03-23** | lib/prompts.js | Source: UI/UX audit real-world-problem #564 (F5). 1 confirmed instance (new). SHIPPED: CDN_CONSTRAINTS_BLOCK + buildGenerationPrompt() numbered rule with inline aria-live feedback div pattern. |
| **GEN rule: typed numeric input must support Enter key submission** | **DONE — pre-existing (GEN-INPUT-001, confirmed 2026-03-23)** | Already in CDN_CONSTRAINTS_BLOCK line 225 and buildGenerationPrompt rule 35. 2 confirmed instances. Enter key binding required on all numeric/text inputs. | Confirmed via agent grep 2026-03-23. |
| **GEN rule: gameState must declare gameId field** | **DONE — pre-existing (GEN-GAMEID, confirmed 2026-03-23)** | Already in buildGenerationPrompt rule 26b (line 469) and CDN_CONSTRAINTS_BLOCK. 6 confirmed instances. gameId must be first field in gameState matching spec Game ID. | Confirmed via agent grep 2026-03-23. |
| **GEN rule: secondary/utility buttons must also have min-height: 44px** | **pending — ESCALATE** | lib/prompts.js | Source: UI/UX audit adjustment-strategy #385 (F1, F7), associations #513 (F5). GEN-UX-002 currently only covers `.game-btn` class (primary action buttons). Custom widget buttons (`.adj-btn` at 36px, `.reset-btn` at 30.5px, `.choice-btn` with padding-only) are exempt. associations choice-btn uses `padding: 14px 8px` only — no min-height. Fix: extend gen prompt rule — "Every interactive button, including secondary, utility, choice, and custom widget buttons, must have `min-height: 44px`". The rule must not be scoped to a specific CSS class. Measure: all buttons in next audit ≥44px height. |
| **TEST gap: choice-btn and non-.game-btn touch target assertions** | **pending** | test-gen prompt | Source: UI/UX audit associations #513 (F5d). No Playwright assertion checks that `.choice-btn` computed height >= 44px. These buttons are dynamically generated so static T1 cannot catch them. Add to test-gen prompt: for MCQ/button games, assert `page.locator('.choice-btn').first()` has `min-height: 44px` CSS. Route to Test Engineering. |
| **TEST gap: .option-btn touch target assertion missing from test-gen M16** | **DONE — 2026-03-23 (commit a8524a0)** | M16 extended to cover .option-btn alongside .choice-btn. "skip if .option-btn" carve-out removed. 986 tests pass. Deployed. | Medium |
| **TEST gap: waitForPackages ScreenLayout contract test missing** | **pending** | Source: stats-identify-class UI/UX audit F4. waitForPackages awaits ScreenLayout but CDN auto-mounts it — no contract test verifies `typeof window.ScreenLayout !== 'undefined'`. Add to contract test-gen prompt: assert window.ScreenLayout is defined after game init. | Low |
| **TEST gap: M16 "both selectors" test uses empty DOM snapshot** | **pending** | Code Review a8524a0 (CR-001): Test 3 in M16 .option-btn block passes empty `<div id="app"></div>` as domSnapshot but asserts both .choice-btn and .option-btn patterns appear — misleading, tests that rule text is static (not conditional). Rename or fix to use a snapshot containing both selectors. | Low |
| **TEST gap: No M16-equivalent runtime assertion for .answer-btn** | **pending** | Code Review a8524a0 (CR-002): GEN-UX-002 instructs generator to set CSS min-height:44px on .answer-btn but no mechanics test rule asserts this at runtime via Playwright. If CSS is missing/overridden, no test catches it. Add M16-equivalent conditional assertion for .answer-btn in buildTestGenCategoryPrompt(). | Low |
| **TEST gap: window.nextRound alias missing for loadRound()-based games** | **pending** | lib/prompts.js (test-gen) | Source: UI/UX audit adjustment-strategy #385 (F6). Harness warns MISSING window.nextRound at load. Game uses `loadRound()` which is exposed as `window.loadRound` — harness `jumpToRound()` falls back to `window.loadRound()`, so functional impact may be low. Confirm fallback covers this case; if not, add `window.nextRound = loadRound;` to gen prompt spec and test assertion. Also: remove `nextRound` from harness MISSING check if `loadRound` exposure is the canonical pattern. | Test Engineering |
| **GEN rule: never negate a guard flag immediately after checking it** | **pending** | lib/prompts.js | Source: UI/UX audit count-and-tap #551 (UI-CT-004). Pattern: `if (!isActive) return; isActive = false; isActive = true;` — guard is defeated the instant it fires. Pattern left behind by fix-loop LLMs that add temporary lock flags then immediately undo them. Fix: gen prompt rule: "Never assign a flag to false and then immediately to true — this defeats the guard and is dead code. If the guard is not needed, remove it. Use `answerLocked` or `isProcessing` as the single source of truth for double-click protection." |
| **GEN rule: progressBar.update() arg must be clamped to valid range** | **pending** | lib/prompts.js | Source: UI/UX audit name-the-sides (UI-NS-007). `progressBar.update(-9)` fires PAGEERROR every round — negative count value is invalid. Fix: gen prompt rule requiring progressBar.update() argument to be clamped to [0, totalRounds] before call. Also: T1 check for progressBar.update() calls with literal negative constants. Measure: zero progressBar PAGEERROR in next 3 CDN builds using ProgressBarComponent. |
| **Re-run T1 validation on targeted fix HTML before Playwright** | **pending** | lib/pipeline-fix-loop.js | Root cause: targeted fix LLM generates patched HTML → pipeline runs Playwright only → T1 checks are NOT re-run on patched HTML → CSS-stripped HTML passes Playwright (DOM state asserted, not rendering) → approved game visually broken. Fix: after targeted fix LLM returns patched HTML, call validateStatic(patchedHtml) before running Playwright; if T1 ERRORS found, mark fix as failed and try again (up to 2 attempts). This would have caught CSS stripping in name-the-sides #557 and which-ratio #560 before they were approved. |
| **Local verification workflow: download-apply-test before build queue** | **pending** | docs/areas/local-verification.md | When a gen rule or T1 check is shipped, current workflow is: deploy → queue build → wait 30 min → discover if fix works. New workflow: deploy → download most recent relevant failed HTML from GCP → apply fix to HTML → run Playwright locally → if passes, queue build. Creates docs/areas/local-verification.md documenting the standard local verification process (curl GCP HTML, inject fix, run diagnostic.js, run Playwright). Reduces fix verification from 30 min to 5-10 min. |
| **TEST gap: TC-005 timeout test (46s wait) will cause slow test suite** | **pending** | lib/prompts.js test-gen | Source: stats-median UI/UX audit (2026-03-23). TC-005 spec requires waiting 46 seconds for timer expiry — this is correct behaviour but will be generated as a real 46s `page.waitForTimeout(46000)` in the edge-cases test. For timer games, test-gen should call `window.gameState.timer.pause()` + directly invoke `handleTimeout()` via `window.handleTimeout()` instead of waiting the full timer duration. Reduces edge-case test time from 46s → <1s. Needs window.handleTimeout exposed in spec (similar to window.loadQuestion). | Low |

### Cross-game learning injection — design notes

- **Source**: `learnings` table already populated per build (`level`, `category`, `content`). APPROVED builds are ground truth.
- **Retrieval**: at Step 1 (gen) and Step 3 (fix), query `learnings WHERE resolved=0` for games sharing the same PART set (parsed from spec CDN section). Top-N by recency + category relevance.
- **Injection**: append as `## Lessons from similar games` block to gen/fix system prompt — same slot as spec-derived hints.
- **Scope**: `pipeline.js` only. No new DB schema (learnings table already exists). No new env var needed.
- **Success metric**: average iterations-to-pass drops; track via `builds.iterations` across next 10 builds post-deploy.

---

## P7 — Code Architecture & Best Practices

> Audit date: 2026-03-21 (updated). lib/pipeline.js (4105 → 1432 lines), pipeline-fix-loop.js (1255), pipeline-test-gen.js (776), pipeline-targeted-fix.js (392), worker.js (~1111 lines), server.js (~572 lines), lib/db.js (~468 lines), lib/slack.js (~561 lines). P7 split complete — no single file exceeds 2000 lines. pipeline.js grew from 1270 (P7 split) as new lessons/features were added; still well under threshold.

| Item | Status | File(s) | Notes |
|------|--------|---------|-------|
| Extract all LLM prompt strings into lib/prompts/ | **done (2026-03-20)** | lib/prompts.js | 18 prompt builders extracted from pipeline.js inline template literals; pipeline.js: 4105 → 3592 lines. Shipped commit d6d619f. |
| Extract snapshot/harness/spec utilities into lib/pipeline-utils.js | **done (2026-03-20)** | lib/pipeline-utils.js | 12 utility functions (captureGameDomSnapshot, captureBehavioralTranscript, injectTestHarness, extractSpecRounds, extractSpecMetadata, extractTestGenerationHints, etc.) extracted; pipeline.js: 3592 → 2433 lines. Shipped commit 6c52240. |
| Split pipeline.js (~2433 lines) into focused sub-modules | **done (2026-03-20)** | lib/pipeline.js, lib/pipeline-fix-loop.js, lib/pipeline-test-gen.js, lib/pipeline-targeted-fix.js | pipeline.js: 2433 → 819 lines; lib/pipeline-fix-loop.js (802 lines), lib/pipeline-test-gen.js (569 lines), lib/pipeline-targeted-fix.js (422 lines) created; 385 tests pass; deployed 2026-03-20 (commit 73cc250). |
| Deduplicate REVIEW_SHARED_GUIDANCE (used as literal string in re-review prompt) | **done (2026-03-20)** | lib/prompts.js | buildReReviewPrompt had an inline copy of REVIEW_SHARED_GUIDANCE that was missing RULE-003 and RULE-005 and had a stricter General guidance clause. Replaced with `${REVIEW_SHARED_GUIDANCE}` constant reference. All 3 review prompts (buildEarlyReviewPrompt, buildEarlyReReviewPrompt, buildReReviewPrompt) now use the canonical constant. 441 tests pass. |
| Deduplicate playwright.config.js template (written twice) | **done (2026-03-20)** | lib/pipeline.js | `buildPlaywrightConfig(port)` helper extracted; both runPipeline() and runTargetedFix() use it. Shipped commit a8392bc. |
| Deduplicate CATEGORIES / SPEC_ORDER constant (defined three times) | **done (2026-03-20)** | lib/pipeline-utils.js | CATEGORY_SPEC_ORDER added to pipeline-utils.js as single source; removed inline declarations from pipeline-fix-loop.js, pipeline-test-gen.js, pipeline-targeted-fix.js. MODEL_COSTS/estimateCost and findFreePort also centralized. 417 tests pass. Commit 0e753c0. |
| Deduplicate CDN constraint rules across fix prompts | **done (2026-03-20)** | lib/prompts.js | CDN_CONSTRAINTS_BLOCK expanded with SENTRY ORDER, DEBUG FUNCTIONS, SCREENLAYOUT rules; reused in buildFixPrompt, buildGlobalFixPrompt, buildTargetedFixPrompt. Net -34 lines. 441 tests pass; deployed 2026-03-20 (commit e7878e7). |
| Deduplicate beforeEach replacement blocks | **done (2026-03-20)** | lib/pipeline-test-gen.js | `buildBeforeEach(hasCdnSlot, slotId)` extracted; both sharedBoilerplate and newBeforeEach rewrite use it. 487 tests pass. Commit c164b67. |
| Centralize all env var reads into lib/config.js | **done (2026-03-20)** | lib/config.js | Created lib/config.js with loadConfig() exporting a frozen object of all 40+ env vars with defaults and type coercions. Throws in production if GITHUB_WEBHOOK_SECRET/PROXY_URL/PROXY_KEY missing; warns if SLACK_BOT_TOKEN/RALPH_GCP_BUCKET empty. Updated pipeline.js, pipeline-fix-loop.js, pipeline-test-gen.js, pipeline-targeted-fix.js to import from config. Skipped gcp.js/slack.js/db.js/llm.js/server.js/worker.js (tests mock process.env via require-cache invalidation — safe to leave as-is). 417 tests pass. |
| Remove lazy require() calls inside function bodies | **done (2026-03-20)** | lib/pipeline.js, lib/pipeline-targeted-fix.js, lib/pipeline-utils.js, worker.js, server.js | Moved require('child_process'), require('net'), require('./db'), require('crypto'), require('./lib/pipeline'), require('bullmq').Queue, require('fs'), require('path') from function bodies to top-level. Kept intentional lazy requires: @playwright/test in captureGameDomSnapshot, ./db in getRelevantLearnings (optional dep comment), ./lib/mcp in getMcpServer (optional dep), @modelcontextprotocol/sdk and ioredis/bullmq in require.main block. 417 tests pass. |
| Replace duplicate server proc spawn with a shared helper | **done (2026-03-20)** | lib/pipeline-utils.js | `spawnServeProcess(dir, port)` extracted; 3 call sites replaced. Commit aa14901. |
| (stale entry) | **done** | child_process.spawn('npx', ['-y', 'serve', dir, '-l', port, '-s', '--no-clipboard']) appears three times: DOM snapshot server (captureGameDomSnapshot), test server in runPipeline(), test server in runTargetedFix(). Extract to spawnServeProcess(dir, port) returning the child process; all three sites become one-liners and the 2000ms startup wait lives in one place. |
| Extract magic numbers to named module-level constants | **done (2026-03-20)** | lib/pipeline.js, lib/pipeline-targeted-fix.js | Named VALIDATOR_TIMEOUT_MS=10000, SERVER_START_DELAY_MS=2000 in both pipeline.js and pipeline-targeted-fix.js (same values appear in both files). Numbers inside template-string-generated playwright.config.js are not JS literals in our code and were left as-is. 417 tests pass. |
| Add schema migration versioning to lib/db.js | **done (2026-03-20)** | lib/db.js | Replaced 9 try/catch ALTER TABLE blocks with a MIGRATIONS array + user_version pragma. Reads PRAGMA user_version at startup, runs only migrations from currentVersion..DB_VERSION, then writes updated version. To add a migration: append to MIGRATIONS array — no other changes needed. 417 tests pass. |
| Extract Block Kit builder logic from lib/slack.js into composable helpers | **done (2026-03-20)** | lib/slack.js | `buildStatusField()`, `buildProgressFields()`, `buildLinksRow()` extracted; `buildParentBlocks()` 111→34 lines. 487 tests pass. Commit 89ab57e. |
| Add JSDoc to all exported public API functions | **done (2026-03-20)** | lib/pipeline.js, lib/db.js, lib/slack.js, lib/llm.js, lib/pipeline-fix-loop.js, lib/pipeline-utils.js, lib/pipeline-targeted-fix.js | 41 functions documented across 7 files. 487 tests pass. Commit 5d66c74. |
| Define and enforce consistent error handling tiers | **done (2026-03-20)** | lib/pipeline.js, worker.js | 5 real bugs fixed (bare catch {} silently swallowing static-fix, server-start, review LLM, learning-extraction errors); all 34 catch blocks tier-documented (critical/degraded/cosmetic). 487 tests pass. Commit 5929982. |
| Expand pipeline.js test coverage for fix-loop and snapshot paths | **done (2026-03-20)** | test/pipeline.test.js | +15 tests: extractSpecRounds (6), deterministicTriage (4), isInitFailure (3), detectCrossBatchRegression improvement case (1), buildBeforeEach (1). 502 total. Commit f505017. |
| injectTestHarness truncation on $ chars (Sentry) | **done (2026-03-20)** | lib/pipeline-utils.js | Latent bug: `String.replace('</body>', harnessScript)` — if harnessScript ever gained $&/$`/$' patterns, output would silently corrupt. Fixed to `replace('</body>', () => harnessScript)`. 16 new tests (518 total). Commit 351924b. |

---

## Code Review — Pipeline Source Code (active)

**Active slot state:**
| Field | Value |
|-------|-------|
| Current task | DONE — CR-016 + CR-017 RESOLVED (2026-03-23). GEN-TIMER-ONLY WRONG example fixed (timer before renderRound); GEN-HIDE-SHOW soh-cah-toa #544 citation retracted, WHY updated to element-expected helpers only. Both CDN_CONSTRAINTS_BLOCK and numbered rules updated. 997/997 tests pass. Deployed. Next: pick next lib/ commit for review. |
| Waiting on | unblocked |
| Blocked by | none |

**Log:**
| Date | Files reviewed | Finding | Routed to |
|------|---------------|---------|-----------|
| 2026-03-23 | lib/prompts.js (2fce92d: GEN-RESTART-RESET + GEN-PHASE-ALL + GEN-TIMER-ONLY + GEN-HIDE-SHOW evidence audit) | **OVERALL: PASS with findings.** **CR-014 [LOW] — GEN-RESTART-RESET minimum reset set may be misread as exhaustive:** The rule states "minimum reset set: lives, currentRound, score, gameEnded, isActive" with a parenthetical "(plus any game-specific arrays)". An LLM may interpret the minimum set as the complete list and skip custom fields like `currentProblem`, `streak`, `selectedAnswers`. The WRONG example only omits lives/currentRound, not arbitrary custom fields. No game-type-specific guidance for what counts as a "mutable field". Route: Gen Quality — add a sentence: "For any field set during setupGame() or startGame(), ask: does this field need resetting on restart? If yes, include it." **CR-015 [LOW] — GEN-PHASE-ALL creates naming ambiguity alongside GEN-MCQ-PHASE:** GEN-MCQ-PHASE header was updated to "ALL GAMES" in commit 3704eb7; GEN-PHASE-ALL now says "extends GEN-MCQ-PHASE — applies to all game types." Two rules both claiming "ALL game types" without clear delineation: GEN-MCQ-PHASE provides the phase-by-phase table (showStartScreen/startGame/renderRound/endGame), GEN-PHASE-ALL adds only a drag-and-drop showResults() example. An LLM reading both may treat GEN-PHASE-ALL as a second independent requirement or conflate them. Route: Gen Quality — add clarification sentence to GEN-PHASE-ALL: "GEN-MCQ-PHASE already requires this; GEN-PHASE-ALL confirms the same requirement extends to non-MCQ game types." **CR-016 [MED] — GEN-TIMER-ONLY WRONG example does not actually trigger the race condition:** WRONG example shows `function setupGame() { renderRound(); timer.start(); }` — timer.start() is called AFTER renderRound(), so currentQuestion IS set when the timer fires. The real antipattern is `timer.start()` BEFORE `renderRound()`, or in a separate setupGame() call that precedes renderRound(). Existing GEN-MCQ-TIMER correctly describes the race ("timer fires its onEnd callback before the first round is rendered"). The WRONG example in GEN-TIMER-ONLY is misleading and partially contradicts GEN-MCQ-TIMER's framing. Route: Gen Quality — fix WRONG example to `function setupGame() { timer.start(); renderRound(); }` (timer before renderRound), or use GEN-MCQ-TIMER's form: `timer = new TimerComponent(...); timer.start();` in setupGame() with renderRound() called separately. **CR-017 [HIGH] — GEN-HIDE-SHOW rule 41 evidence citation is factually wrong:** The WHY clause states "caused invisible results screens in soh-cah-toa #544" and cites "UI/UX audit soh-cah-toa #544; CR-026" as source. The browser playthrough of soh-cah-toa #544 (2026-03-23) retracted UI-SC-007: that game's helper is `hide = (selector) => document.querySelector(selector)?.classList.add('hidden')` — a CSS selector pattern. Calling `hide('#results-screen')` on that helper is correct and produces zero TypeErrors. The rule premise (TypeError on string argument) is only valid for `el.style.display`-pattern helpers, not querySelector-pattern helpers. The rule text already scopes itself with "e.g. const hide = el => el.style.display = 'none'" which is technically accurate, but the evidence citation falsely attributes a bug to soh-cah-toa #544 that did not occur there. Recommendation: REVISE (not remove — the DOM element mandate is a valid coding standard for style.display helpers). Update WHY to: "When the helper uses `el.style.display` (not `querySelector`), passing a string causes a silent TypeError. soh-cah-toa #544 citation retracted — that game used querySelector pattern with no TypeError. Rule applies only to style.display pattern helpers." Route: Gen Quality — update WHY clause and evidence citation in both CDN_CONSTRAINTS_BLOCK and rule 41. | Gen Quality (CR-014, CR-016, CR-017), Gen Quality backlog (CR-015) |
| 2026-03-23 | lib/prompts.js + lib/categorize-failure.js (d60ccc9: GEN-CR-003 assertive sub-note) | **CR-008 [MED] — GEN-CR-003 assertive condition is over-constrained (AND should be OR):** Rule 30 sub-note reads "assertive is ONLY correct for a div with `role="alert"` AND id or class containing 'error'". The AND is more restrictive than WCAG/ARIA — `role="alert"` alone is sufficient to justify assertive. An error alert with `role="alert"` but id `#validation-msg` (no 'error' substring) is falsely disqualified. An LLM following this rule will rename error elements to `#error-msg` to comply (prescriptive naming) or avoid assertive on legitimate error alerts. CDN_CONSTRAINTS_BLOCK line ~120 has the same AND phrasing. **Route:** Gen Quality — change AND to OR in both locations: "assertive is ONLY for a div that is a true error alert (`role='alert'` OR id/class containing 'error') — NEVER for correct/incorrect answer feedback." | Gen Quality |
| 2026-03-23 | lib/categorize-failure.js (d60ccc9: GEN-CR-002 narrowed expect regex) | **CR-009 [MED] — Short-form Playwright numeric diff `Expected: N, Received: N` not caught after narrowing to `expect\(received\)`:** Playwright emits two formats: (1) long-form `expect(received).toBe(expected)\n\nExpected: 5\nReceived: 3` — caught by new regex; (2) short-form `Expected: 5, Received: 3 — score mismatch` without the `(received)` token — confirmed in test/pipeline.test.js line 775. Short-form now falls to `unknown` bucket. Impact: any numeric score/lives `toBe()` assertion failure is uncategorized. **Route:** Gen Quality — add second branch: `/expected:\s*\d+.*received:\s*\d+/i` → 'rendering'. Handles short Playwright diff form without re-opening the false-positive door. | Gen Quality |
| 2026-03-23 | lib/prompts.js (d60ccc9: GEN-GAMEID as rule 26b) | **CR-010 [LOW] — GEN-GAMEID placed as sub-rule 26b, not a first-class numbered rule:** `gameId` is first field of `gameState` and required for SignalCollector + postMessage — more critical than `isActive`. Sub-rule lettering reduces visual weight; LLMs may treat it as a footnote on rule 26 rather than a standalone requirement. 3 confirmed missing instances cited. No immediate risk if all generation goes through `buildGenerationPrompt`. **Route:** Gen Quality — promote to its own number (e.g. rule 26a) when next editing this area. | Gen Quality |
| 2026-03-23 | lib/prompts.js (d60ccc9: GEN-UX-002 button class list) | **CR-011 [LOW] — GEN-UX-002 explicit list missing `.btn-option`, `.btn-primary`, `.btn-secondary`, `.check-btn`:** Approved games use these classes (quadratic-formula-worked-example uses `.btn-option`; adjustment-strategy uses `.check-btn`/`#btn-check`; associations + math-cross-grid use `.btn-primary`). Rule has a catch-all "any other clickable element" which is sufficient. Impact is only prompt clarity, not enforcement. **CLEAN for now** — add to list opportunistically if a missed-touch-target failure is seen on one of these classes. | — |
| 2026-03-23 | lib/prompts.js (d60ccc9) — overall | **CLEAN: GEN-CR-002 narrowing correct. worker.test.js line 600–607 confirms `expect(value).toEqual(0)` → unknown and `expect(received).toBe(expected)` → rendering. GEN-UX-002 M16 test pattern is well-formed. gameState.gameId WRONG/RIGHT examples are clear with 3 cited instances. CDN_CONSTRAINTS_BLOCK assertive text and rule 30 are in sync with each other.** | — |
| 2026-03-23 | lib/prompts.js (3704eb7: GEN-PHASE-001 header propagation to 3 locations) | **CLEAN — all 3 locations correctly updated:** CDN_CONSTRAINTS_BLOCK (~L211), buildGenerationPrompt rule 33 (~L565), buildCliGenPrompt (~L780). Diff confirms all 3 old "MCQ AND TIMED GAMES" / "MCQ/TIMED GAMES" headers replaced with "ALL GAMES — MANDATORY". Explicit game-type enumeration (drag-and-drop, canvas, worked-example, text-input, MCQ) in all 3. WHY section cites math-cross-grid F4 as first non-MCQ instance — credibility signal for LLM. | — |
| 2026-03-23 | lib/prompts.js (3704eb7: GEN-PHASE-001 phase table for non-MCQ games) | **CR-012 [LOW-MED] — Phase transition table uses MCQ-centric `renderRound()` naming with no alias guidance for non-MCQ games:** Table shows `renderRound() → syncDOMState()` for all game types. Drag-and-drop and canvas games use `renderGrid()`, `loadPuzzle()`, `initBoard()`. The comment was updated from "before rendering MCQ options" to "before rendering options" but the function name `renderRound` itself still prescribes MCQ convention. An LLM implementing math-cross-grid may not map `renderGrid()` to the `renderRound()` slot without an alias note. **Route:** Gen Quality — add parenthetical: "renderRound() or equivalent function that initializes each puzzle/level (renderGrid, loadPuzzle, initBoard, etc.)". | Gen Quality |
| 2026-03-23 | lib/prompts.js (3704eb7: GEN-WINDOW-EXPOSE rule 36 — CLI path gap) | **CR-013 [MED] — GEN-WINDOW-EXPOSE (rule 36) added to buildGenerationPrompt but NOT to buildCliGenPrompt:** Rule 36 is at prompts.js line 622 inside buildGenerationPrompt(). buildCliGenPrompt() ends at GEN-MCQ-TIMER (line 789) — no rule 36. Same gap pattern as CR-002/CR-004. CLI-generated games will miss mandatory `window.endGame`, `window.restartGame`, `window.nextRound`, `window.gameState` at bottom of DOMContentLoaded — test harness cannot call `window.endGame()`, causing 100% game-over test failures. 4 confirmed missing instances in math-cross-grid. **Route:** Gen Quality — add GEN-WINDOW-EXPOSE to buildCliGenPrompt after GEN-MCQ-TIMER (same block, same WRONG/RIGHT format). | Gen Quality |
| 2026-03-23 | lib/prompts.js (3704eb7: GEN-WINDOW-EXPOSE rule 36 — rule quality) | **CLEAN — rule 36 is well-structured.** WRONG/RIGHT pattern is highest-reliability LLM format. WHY section explains test-harness call chain. 4 confirmed instances cited. Length (32 lines) appropriate for a MANDATORY rule. No verbosity risk. | — |
| 2026-03-23 | lib/prompts.js (buildGenerationPrompt rules 33–34, CDN_CONSTRAINTS_BLOCK, buildCliGenPrompt) | **CR-001 [MED] — Rules 33–34 duplicated between CDN_CONSTRAINTS_BLOCK and buildGenerationPrompt numbered list:** GEN-MCQ-PHASE (line 556) and GEN-MCQ-TIMER (line 580) in the numbered-rules section repeat verbatim content already present in CDN_CONSTRAINTS_BLOCK at lines 214–223. Two copies of the same rule — divergence on any future edit will silently create contradictory guidance. No immediate conflict today. **Route:** Gen Quality backlog — deduplicate: reference CDN_CONSTRAINTS_BLOCK from numbered list rather than repeating. | Gen Quality |
| 2026-03-23 | lib/prompts.js (buildCliGenPrompt, ~L621–733) | **CR-002 [MED] — Rules 33 (GEN-MCQ-PHASE) and 34 (GEN-MCQ-TIMER) missing from CLI gen path:** buildCliGenPrompt ends at GEN-UX-005 with no MCQ-PHASE or MCQ-TIMER rules. Games generated via the `claude -p` CLI path will not receive these two rules, leading to syncDOMState() omissions and timer.start()-in-setupGame() bugs on MCQ/timed games. If the CLI path is used for MCQ-type games, expect 22% game-flow failure rate (same pattern that prompted the original GEN-MCQ-PHASE rule). **Route:** Gen Quality — add MCQ-PHASE and MCQ-TIMER to buildCliGenPrompt numbered rules list. | Gen Quality |
| 2026-03-23 | lib/prompts.js (entire file) | **CR-003 [LOW] — Zero unit test coverage for lib/prompts.js:** No test file exists for prompts.js. All builder functions (buildGenerationPrompt, buildStaticFixPrompt, buildContractFixPrompt, etc.) are untested. CDN_CONSTRAINTS_BLOCK rule additions/removals go unverified until a full 30-min build runs. **Route:** Test Engineering backlog — add test/prompts.test.js with at minimum: CDN_CONSTRAINTS_BLOCK contains key constraint strings; buildGenerationPrompt includes spec content; buildStaticFixPrompt includes error text; rule 33/34 present in buildGenerationPrompt output. | Test Engineering |
| 2026-03-23 | lib/session-planner.js (generateSpec, ~L707–791) | **CR-004 [MED] — generateSpec() proceeds with empty templateContent when templateSpecId is null:** Regex at line 707 extracts templateSpecId from the instructions file. If the line uses the `_(no template spec…)_` placeholder (templateSpecId=null), templateContent stays `''` and the LLM prompt has an empty template section. The system prompt instructs the LLM to "PRESERVE unchanged: All CDN import blocks…" — with nothing to preserve, the LLM will invent CDN structure from scratch. No warning is logged. Generated spec will likely fail validate-static. **Route:** Gen Quality — add a warning log + consider throwing or returning early when templateSpecId is null and no template content is available. | Gen Quality |
| 2026-03-23 | lib/session-planner.js (planSession, line 302) | **CR-005 [MED] — planSession() bloomTarget default is 6 but parseGoal() default is 3:** When `parsedGoal.bloomTarget` is not a number, planSession falls back to `6` (include all skills), but parseGoal() clamps to `3` (Apply). A caller that bypasses parseGoal and passes a raw object without bloomTarget gets a full 5-game session instead of the 3-game Apply-level plan. The mismatch is invisible — no error, just a longer session than expected. **Route:** Gen Quality — change planSession default from `6` to `3` to match parseGoal(). | Gen Quality |
| 2026-03-23 | lib/session-planner.js (writeSessionDirectory, line 430; generateSessionSpecs, line 837) | **CR-006 [LOW] — writeSessionDirectory() silently accepts empty games array:** If `sessionPlan.games` is `[]` (returned by planSession() `no_skills_in_range` error), writeSessionDirectory writes a session-plan.md with an empty game table and 0 spec-instructions.md files. No error is thrown. planSessionFromObjective() guards against `sessionPlan.error` but the `no_skills_in_range` path also sets `games: []` without an error field, so the guard passes and writeSessionDirectory is called with an empty games array. **CR-007 [LOW] — generateSessionSpecs() returns `[]` silently when no game dirs found:** Cannot distinguish "session had no games" from "wrong sessionDir path" from the return value alone. **Route:** Test Engineering — add test for writeSessionDirectory with games:[] and verify it either throws or returns a meaningful warning; add test for generateSessionSpecs with empty sessionDir. | Test Engineering |
| 2026-03-23 | lib/validate-static.js (PART-028), lib/slack.js (updateThreadOpener), worker.js (onProgress) | **Edge case gap:** PART-028-CSS-STRIPPED does NOT fire when `<style></style>` is empty or whitespace-only (condition: `stripped.length===0 && content.trim().length>0` — empty block fails second predicate, so no error fires and no warning fires either since styleBlocks.length>0). A targeted fix LLM that outputs `<style></style>` with no content at all bypasses PART-028 entirely. **No test coverage** for empty/whitespace-only style block case. | Test Engineering: add test for empty `<style></style>`; Gen Quality: extend PART-028 condition or add separate check |
| 2026-03-23 | lib/slack.js (updateThreadOpener), test/slack.test.js | **Test gap:** `updateThreadOpener` has zero test coverage in test/slack.test.js — the 'running' status emoji/label branching added in 9101650 is untested. `buildParentBlocks` is also uncovered for status='running'. | Test Engineering: add unit tests for updateThreadOpener with status='running', 'APPROVED', 'FAILED' |
| 2026-03-23 | worker.js (onProgress non-blocking updateThreadOpener call, line 507) | **Clean — no logic error.** Non-blocking `.catch(()=>{})` is correct pattern; `currentBuildStep !== prevBuildStep` guard prevents flood of Slack API calls on high-frequency progress events. `iterations: 0` passed during running state is intentional (build not yet complete). | — |
| 2026-03-23 | worker.js (`resolveFailurePattern()`) | **CODE-001: resolve rate is 0% across all categories** — Analytics shows `resolveFailurePattern()` has never been called successfully on any build approval. Verify this function is called correctly in the pipeline when a build passes; check call site and argument shapes. [from Analytics 2026-03-23] | Gen Quality: if function is broken, fix call site; if logic gap, add call on APPROVED path |
| 2026-03-23 | lib/pipeline-test-gen.js (GEN-TE-001 banned-selector enforcement, commit bdfc44d), lib/prompts.js (CDN_CONSTRAINTS_BLOCK, buildTestGenCategoryPrompt, extractUserScenarios, extractPhaseNamesFromGame) | **P1 — Silent zero-test spec after banned-selector strip (pipeline-test-gen.js ~L472–493):** Banned-selector stripper replaces matching lines with comment strings, then writes the file. No validation that the resulting file still has ≥1 `test()` call. If an LLM writes all 3 tests for a category using `#mathai-transition-slot button` patterns, all 3 get stripped to comments and `hasRunnableTests()` returns false (comments don't match `/^\s*test\s*\(/m`) — BUT the spec file has already been written and the category is marked as regenerated. The deletion-before-regen logic at L141–148 runs only at the start, not after post-processing. Result: a category spec with 0 runnable tests ships into the fix loop undetected. **Route:** Test Engineering. | Test Engineering |
| 2026-03-23 | lib/pipeline-test-gen.js (CDN domain fixup chain, ~L514–536) | **P1 — Double `ctx.injectHarnessToFile()` call risk:** When HTML contains both `cdn.mathai.ai` AND `cdn.homeworkapp.ai` (e.g. multi-line `<head>`), both if-branches fire and `htmlChanged = true` twice. At L535, `ctx.injectHarnessToFile(htmlFile)` is called once — but only because the write happens in the final `if (htmlChanged)` block. **Actual risk:** if `cdn.mathai.ai` only is present, branch 1 renames to `cdn.homeworkapp.ai`, branch 2 strips and appends GCS scripts, htmlChanged=true and a single write + inject occurs — correct. The code is currently safe; the risk is latent: if someone adds an early `return` or restructures the block, double-inject would occur. The logical dependency between two sequential if-blocks mutating the same `rawHtml` string is a code smell. **Route:** Gen Quality — low-priority refactor to single-pass domain normalization. | Gen Quality |
| 2026-03-23 | lib/pipeline-test-gen.js (lintGeneratedTests RULE-DUP, ~L1017–1053) | **P2 — RULE-DUP false positives on universally-required testids:** `btn-restart`, `score-display`, `stars-display` appear in all 5 categories by design (required by gen prompt). RULE-DUP threshold is ≥3 categories, so these testids will always fire a violation — 5 WARN entries per build per universal testid. No suppression allowlist exists. Genuine cross-category testid contamination (real violation) is drowned out by these expected entries. **Route:** Test Engineering — add `RULE_DUP_ALLOWLIST` of known-universal testids (`btn-restart`, `score-display`, `stars-display`, `lives-display`, `answer-input`) and suppress RULE-DUP when a testid is in the allowlist. | Test Engineering |
| 2026-03-23 | lib/prompts.js (buildTestGenCategoryPrompt GF1 block, extractPhaseNamesFromGame, ~L992–1029) | **P2 — extractUserScenarios `descMatch` regex is too greedy on long specs (prompts.js ~L900):** Pattern `/Description[:\s]+(.+?)(?:\n\n|\n##|\n\|)/is` uses lazy `.+?` with the `s` flag (dotAll) — the `s` flag makes `.` match newlines, so `.+?` can span multiple lines. The lazy quantifier stops at the first `\n\n`, `\n##`, or `\n|`. But if the Description section has no blank line before the next section (a spec formatting quirk), the regex may consume the entire spec body and the `desc` string becomes the rest of the file. The subsequent `if (/adjust|…/i.test(desc))` checks on a multi-thousand-character `desc` are harmless (false positives are bounded — only up to 4 extra scenarios), and deduplication + `slice(0,7)` cap prevents overflow. Actual impact: at most 1–2 spurious scenarios injected into the test-gen prompt. **Route:** Gen Quality — low priority; constrain regex to `[^#]{0,400}` or remove `s` flag. | Gen Quality |
| 2026-03-23 | lib/pipeline-test-gen.js + lib/prompts.js (commit bdfc44d: GEN-TE-001 banned-selector enforcement) | **PASS — Defense-in-depth is correct and well-structured.** Two-layer enforcement: (1) prompts.js BANNED TEST SELECTORS block placed above MANDATORY RULES — correct positioning for LLM attention; WHY/WRONG/RIGHT format is highest-reliability pattern. (2) pipeline-test-gen.js post-gen strip: 3 regex patterns covering click, toBeVisible/not.toBeVisible/toHaveText/toBeHidden, and expect.poll() — all multiline (`gm` flag), correct. Zero-test guard (hasRunnableTestCalls + unlinkSync for regen) is solid. Test coverage: lintGeneratedTests TRANSITION_SLOT tests (5 cases covering flag/no-flag/helper-suppression/outside-helpers), hasRunnableTestCalls zero-test guard (5 cases). **CR-014 [LOW] — Strip regexes do not handle chained `.nth(N)` or `.filter()` locator calls:** Pattern `[^)]*` in the locator capture stops at `)` — a locator like `page.locator('#mathai-transition-slot button').nth(0).click()` has an extra `.nth(0)` chain before `.click()` which breaks the match. The `[^;]*` in the click pattern may catch this, but the toBeVisible/toBeHidden pattern uses `[^)]*` strictly. Edge case — LLMs rarely chain nth() on this selector — but worth noting. No immediate action needed. **Route:** Test Engineering backlog. | Test Engineering |
| 2026-03-23 | lib/prompts.js + lib/session-planner.js (3508ef4: ARIA-001 inline panels + GEN-CR-005 null guard; d76ecb0: CR-019 GEN-112 wrong-args to buildGenerationPrompt; 268b27b: GEN-CR-006 bloomTarget default) | **OVERALL: PASS with 1 MEDIUM finding and 1 LOW finding. 988/988 tests pass.** **3508ef4 ARIA-001 PASS — sync confirmed:** CDN_CONSTRAINTS_BLOCK (~L122) and buildGenerationPrompt rule 30 (~L548) both updated to include the INLINE PANELS selector block. Both locations list the same selectors. The catch-all "ANY div whose id or class contains the substring 'feedback'" is not overly broad — it mirrors the WCAG behavioral requirement (dynamic content → aria-live). String contains 'feedback' is a reasonable proxy: any false positive (a non-dynamic div with 'feedback' in its name) is harmless (extra aria-live attribute does not break anything). **3508ef4 GEN-CR-005 PASS — guard logic correct:** `!templateSpecId \|\| !templateContent.trim()` → warn → throw when `dryRun=false`, safe return when `dryRun=true`. Throw message is informative (includes templateSpecId, explains CDN hallucination risk, instructs how to fix). No legitimate case where null templateSpecId should NOT throw on a live run — every skill node in CONCEPT_GRAPH that has a null templateSpecId is a session-planner authoring error, not a runtime branch to silently accept. 988 tests pass (generateSpec covered by test/session-planner.test.js). **d76ecb0 CR-019 PASS — rule 38 position correct:** Rule 38 is last in the buildGenerationPrompt numbered list, immediately before the `SPECIFICATION:` footer — correct position (high recency weight for LLM attention). Semantic match with CLI version confirmed: both blocks list the same two WRONG patterns (totalRounds as 2nd arg; 3-arg form) and the same CORRECT pattern (currentRound, lives; 0 for no-lives). **268b27b GEN-CR-006 PASS — default change is safe:** Old default 6 → new default 3 matches parseGoal() clamp. All planSession() tests pass explicit bloomTarget; no test asserts on default-triggered session size. All callers in production go through planSessionFromObjective() → parseGoal() → planSession() with an explicit bloomTarget, so production behavior is unchanged. The direct-caller path is the only affected path, and consistency with parseGoal() is correct. **CR-020 [MED] — CDN init fix prompt at ~L2235–2238 still uses GEN-112-banned wrong-arg pattern:** BUG 6 block in the CDN fix prompt (buildCdnInitFixPrompt or equivalent) shows `progressBar.update(0, totalRounds)` as the CORRECT example and describes the API as `.update(currentRound, totalRounds)`. This directly contradicts GEN-112 rule 38 (2nd arg must be livesRemaining, NEVER totalRounds). An LLM applying this fix prompt will see the "CORRECT" example and copy `update(0, totalRounds)` — triggering the exact RangeError that GEN-112 was written to prevent. Route: Gen Quality — update BUG 6 block: change `progressBar.update(0, totalRounds)` to `progressBar.update(0, gameState.lives)` (or `progressBar.update(0, 0)` for no-lives games), and update the trailing description "progressBar.update(currentRound, totalRounds)" to "progressBar.update(currentRound, livesRemaining)". **CR-021 [LOW] — GEN-112 in buildGenerationPrompt (rule 38) omits the constructor API contract present in buildCliGenPrompt:** The CLI version of GEN-112 (~L787) includes both the constructor options-object API mistake (positional string first arg) AND the wrong-args enforcement. Rule 38 in buildGenerationPrompt covers only the wrong-args half — it does not repeat the constructor API constraint. This is acceptable because buildGenerationPrompt rule 27/GEN-UX-003 (~L211 CDN_CONSTRAINTS_BLOCK) already covers the constructor API separately. No gap in enforcement; split is logical. No action needed. | Gen Quality (CR-020 MED open), — (CR-021 LOW clean) |
| 2026-03-23 | worker.js (commit c688f0e: CODE-001 resolveFailurePattern on approval path) | **PASS — Fix is correct and addresses root cause.** Signature match confirmed: `resolveFailurePattern(gameId, pattern)` in db.js takes exactly those two args; call site `db.resolveFailurePattern(gameId, fp.pattern)` matches. `getFailurePatterns(gameId)` returns all patterns (no `resolved=0` filter) — the `.filter((p) => !p.resolved)` in worker.js is correct (SQLite returns 0 for false, `!0 === true`). Positioned after `db.updateGameStatus()` and before warehouse sync — correct ordering. Logger call inside `if (openPatterns.length > 0)` avoids noise on first-build approvals with no patterns. **CR-015 [LOW] — No error handling around the resolve loop:** If `db.resolveFailurePattern()` throws (DB locked, schema issue), the exception propagates and kills the post-approval block, preventing warehouse sync. The approval is still recorded in `updateGameStatus()` so the build is not lost, but the HTML will not be synced to warehouse and the Slack update will fail. Consider wrapping in try/catch with a WARN log. Medium-low risk (SQLite writes are fast; DB lock is rare on a single-writer worker). **CR-016 [LOW] — No integration test for CODE-001 approval path behavior:** failure-patterns.test.js covers `resolveFailurePattern` at the DB layer but no worker-level test asserts that APPROVED builds trigger the resolve loop. Route: Test Engineering backlog. | Test Engineering |
| 2026-03-23 | lib/prompts.js, test/pipeline.test.js (commits 49fa3f1: CR-018 M16 touch-target standardization; 34ad329: CR-020 BUG 6 fix) | **OVERALL: PASS with 1 MINOR finding. 988/988 tests pass.** **49fa3f1 CR-018 PASS — all three selectors now use getComputedStyle(el).minHeight:** `.choice-btn` (L1512), `.option-btn` (L1519), `.answer-btn` (L1526) all use `getComputedStyle(el).minHeight` + `parseInt()`. Zero `getBoundingClientRect` references remain in M16 (grep confirmed). Policy is correctly documented in M16 block comment trailing attribution line: "CR-002: .answer-btn previously excluded — now covered". Tests in test/pipeline.test.js: the `.answer-btn getComputedStyle` test (L1994–2009) asserts `prompt.includes('getComputedStyle') || prompt.includes('minHeight')` — passes. `.choice-btn` and `.option-btn` tests (L1940–1966) assert selector presence only, not the measurement method — adequate for selector coverage. **CR-022 [LOW] — parseInt('auto') returns NaN, which is NOT >= 44 — silent false-fail risk if min-height is unset or 'none':** All three M16 patterns now use `parseInt(getComputedStyle(el).minHeight)`. `getComputedStyle` returns the computed CSS value: if no `min-height` is set the browser returns `'0px'` (not `'auto'`), so `parseInt('0px') === 0 < 44` — the test correctly fails. The `'auto'` value can only appear when `min-height: auto` is set explicitly in CSS, which is valid CSS but means "content height". In that case `parseInt('auto') === NaN`, and `NaN >= 44 === false` — test also correctly fails (since auto means no enforced minimum). Risk is low: generated game CSS always sets a pixel value when GEN-UX-002 is followed. No immediate fix required, but the M16 comment should note that `parseInt(NaN)` is expected to fail on un-set min-height. **Route:** Gen Quality backlog — low priority, add comment note to M16 if a false-negative is ever observed. **34ad329 CR-020 PASS — BUG 6 code example and summary description both corrected:** Code block at L2255: `progressBar.update(0, gameState.lives)` (was `update(0, totalRounds)`). Summary description at L2257: `update(currentRound, livesRemaining)` (was `update(currentRound, totalRounds)`). Consistent with GEN-112 rule 38 in buildGenerationPrompt and the CLI version. Only BUG 6 block was modified (diff confirms +2/-2, no adjacent blocks changed). **CR-023 [LOW] — BUG 6 prose description at L2243 still says `.update(currentRound, totalRounds)`:** The one-sentence API summary at L2243 (`ProgressBarComponent has exactly 3 API points: constructor(slotId, config), .update(currentRound, totalRounds), and .destroy()`) was not updated by the commit. The code example (L2255) and trailing summary (L2257) are correct, but L2243 still names the wrong second argument. An LLM parsing the prose first may latch on `totalRounds` before reaching the corrected example. Low risk because the explicit WRONG pattern in GEN-112 rule 38 overrides this, and L2257 is the last line of the block (highest recency weight). **Route:** Gen Quality — fix L2243 to read `.update(currentRound, livesRemaining)`. Also note: `validate-static.js` L585 help text (`progressBar.update(0, totalRounds)`) is a separate instance in the progressBar.init() error message, unrelated to CR-020, but is consistent with the old API description — worth fixing in the same pass. | Gen Quality (CR-022 LOW backlog, CR-023 LOW open) |
| 2026-03-23 | lib/prompts.js + lib/validate-static.js (8221ae2: GEN-CSS-TOKENS rule 40 + GEN-HIDE-SHOW rule 41 + CR-023 BUG 6 prose fix) | **OVERALL: PASS with 3 findings (1 MED, 2 LOW).** **GEN-CSS-TOKENS PASS — rule is accurate and actionable:** Banned tokens (`--mathai-green`, `--color-red`, `--color-orange`) match the actual undefined-token patterns observed in UI/UX audit #543. Mandated tokens (`--mathai-success`, `--mathai-error`, `--mathai-warning`) are correct CDN-defined properties. Rule appears in both CDN_CONSTRAINTS_BLOCK (L229) and buildGenerationPrompt numbered rules (rule 40, L728) — sync confirmed. WRONG/RIGHT examples are in the highest-reliability format. WHY section correctly explains the silent-fallback failure mode. **CR-024 [MED] — GEN-CSS-TOKENS ban list is incomplete: `--color-green`, `--status-red`, `--color-success`, and other common ad-hoc patterns not listed:** The ban list explicitly names only three tokens. LLMs generating ad-hoc feedback colors routinely produce `--color-green`, `--color-success`, `--feedback-color`, `--answer-color`, or other novel names not on the list. The rule's catch-all "or any ad-hoc custom property" is correct English but will not stop an LLM that invents a new variant — LLMs pattern-match against examples, not infer from prose. Route: Gen Quality — expand the NEVER list to include `--color-green`, `--color-success`; reinforce catch-all with "do NOT invent new --my-color tokens for feedback; use ONLY these three." **CR-025 [LOW] — GEN-CSS-TOKENS has no T1 validator check in validate-static.js:** The rule is prompt-only — no static validator detects banned token usage in generated HTML. A regex on `var\(--(?:mathai-green|color-red|color-orange)\)` would catch the three banned names at T1 time (before test gen), preventing 5+ iterations on invisible-feedback bugs. Route: Test Engineering — add T1 check in validate-static.js emitting `WARNING [GEN-CSS-TOKENS]: undefined color token — use --mathai-success, --mathai-error, or --mathai-warning`. **GEN-HIDE-SHOW PASS — rule is clear and covers the primary failure mode:** Rule correctly identifies that strings don't have `.style` properties and that the TypeError is swallowed silently. WRONG/RIGHT examples are concrete. WHY section cites soh-cah-toa #544 as evidence. The rule targets `hide()/show()` helper patterns specifically — appropriate scope. **CR-026 [LOW] — GEN-HIDE-SHOW does not mention `classList.add('hidden')` as a safe alternative that avoids the issue entirely:** The rule instructs LLMs to always pass `document.getElementById()` to hide()/show(). An equally correct pattern — `element.classList.add('hidden')` directly — bypasses the helper entirely and is immune to the string-arg bug. A parenthetical note would help: "Or bypass helpers entirely: `document.getElementById('results-screen').classList.add('hidden')` — no TypeError risk." Route: Gen Quality — low priority, add as a NOTE after the RIGHT example when next editing this area. **CR-023 FIX PASS — all three occurrences corrected and consistent:** (1) prompts.js L96 CDN_CONSTRAINTS_BLOCK API description: `update(currentRound, livesRemaining)` — confirmed. (2) prompts.js L2276 BUG 6 prose description: `update(currentRound, livesRemaining)` — confirmed (this was the open CR-023 item). (3) validate-static.js L581 progressBar.init() error help text: `update(currentRound, livesRemaining)` — confirmed. (4) validate-static.js L585 init-state example: `progressBar.update(0, gameState.lives)` — confirmed correct. **Stale comment at validate-static.js L575 still reads `.update(round, total)` and L591 still reads `.update(roundsCompleted, lives)` — internal code comments only, not LLM-visible prompt text, so no generation impact. Route: Gen Quality — low priority, normalize on next pass through file. | Gen Quality (CR-024 MED open, CR-026 LOW backlog), Test Engineering (CR-025 LOW backlog) |
| 2026-03-23 | lib/prompts.js, test/pipeline.test.js (commits 870c6d5 + 2d5721b: GF9-ENFORCEMENT + GF10 + GEN-112 wrong-args + CR-001/CR-002 .answer-btn) | **OVERALL: PASS with 2 MINOR findings.** Tests: 988 pass, 0 fail — count increase 986→988 confirmed. **CR-001 fix PASS:** Renamed test "M16 rule text is always emitted for mechanics category (contains all three selector patterns)" accurately describes what it validates. DOM snapshot now contains real `.choice-btn` and `.option-btn` elements — no longer misleading. **GF9-ENFORCEMENT PASS:** 6 trigger phrases listed are representative of the real LLM failure patterns ("start screen to game start", "game starts when start is clicked", "transition to playing", "screen advances to game", "start button starts game", "clicking start shows game screen"). Root-cause attribution (19/70 GF failures) is credible and correctly cited. Placement immediately after GF9 is correct — high LLM attention position. Instruction to use `startGame(page)` + `waitForPhase('playing', 15000)` is the correct fix. **CR-017 [LOW] — GF9-ENFORCEMENT trigger phrases use substring matching but the rule says "contains these phrases":** LLM test names commonly include variants like "verifies transition from start to playing", "tests that game starts on click", or "clicking the start button transitions state" that do not match any listed phrase literally. The enforcement block is a prompt instruction, not a runtime regex, so partial coverage is expected — but the listed set covers the most common prefix-exact forms only. No immediate fix needed; worth expanding if a new false-negative is observed in logs. **GF10 PASS:** Rule correctly gates on step panel presence (step1-panel, step2-panel, example-panel, faded-panel, practice-panel) and explains WHY `answer(page, true)` fails (generic helper only matches .answer-btn/.option-btn/.choice-btn). WRONG/RIGHT examples are concrete and correct. Root cause citations (real-world-problem #564, find-triangle-side #549, quadratic-formula #545/#546) add LLM credibility. No edge cases missed for the current known step-panel game set. **CR-002 .answer-btn M16 PASS:** Third M16 pattern added correctly. `getComputedStyle(el).minHeight` with `parseInt()` is the right approach for checking CSS property (not rendered height) — consistent with GEN-UX-002 intent. Two new tests cover: (1) DOM snapshot trigger when `.answer-btn` present, (2) `getComputedStyle`/`minHeight` present in assertion — both correctly structured. **CR-018 [MED] — .answer-btn uses getComputedStyle(minHeight) but .choice-btn/.option-btn use getBoundingClientRect().height — inconsistent measurement methods:** `.choice-btn` and `.option-btn` patterns use `el.getBoundingClientRect().height` (rendered height), while `.answer-btn` uses `parseInt(getComputedStyle(el).minHeight)` (CSS property). If CSS `min-height:44px` is set but padding makes rendered height >44px, both methods pass. But if `min-height` is not set and the element reaches 44px via padding alone, `getComputedStyle(el).minHeight` returns `'0px'` (no min-height declaration) and `parseInt('0px')===0 < 44` — the test FAILS even though the button is visually 44px tall. The GEN-UX-002 spec intent is to enforce the CSS property itself (not just rendered height), so the `getComputedStyle` approach is arguably correct. But the inconsistency creates a higher false-fail rate for `.answer-btn` games that use padding-based sizing. **Route:** Gen Quality — decide policy: either standardize all three to `getBoundingClientRect().height` (rendered, permissive) or all three to `getComputedStyle(el).minHeight` (CSS property, strict). Document the decision in M16. **GEN-112 wrong-args expansion PASS:** The new single-line GEN-112 at line 833 (in buildCliGenPrompt) is technically correct — it adds the `progressBar.update()` wrong-args anti-patterns (totalRounds as 2nd arg) that the original GEN-112 at line 768 did NOT cover. Root cause attribution (which-ratio #559/#560/#561, "RangeError: Invalid count value: -5") is accurate and high-credibility. **CR-019 [MED] — DONE (d76ecb0, 2026-03-23)** — GEN-112 wrong-args block added to buildGenerationPrompt() as rule 38, matching CLI version. API-path games now receive CRITICAL WRONG PATTERNS warning for progressBar.update(gameState.currentRound, gameState.totalRounds). 988/988 tests pass. Deployed. | Gen Quality (CR-018 open), Test Engineering (CR-017 backlog) |

---

## Local Verification (active)

**Active slot state:**
| Field | Value |
|-------|-------|
| Current task | GEN-STEP-001 + CR-013/008/009/012 (188bbaa) + CR-015 (3154415) + GF9-ENFORCEMENT + GF10 (870c6d5) + GEN-112 in CLI path (2d5721b) + CR-002 .answer-btn (870c6d5) — all deployed 2026-03-23. PAGEERROR listener added to sharedBoilerplate (1b783f1, deployed 2026-03-23). results-screen viewport assertion added (TE-RES-001, 2026-03-23, commit 121ac01). Waiting on next build to measure game-flow lift (63% → target 85%+). |
| Waiting on | Next approved build to verify game-flow category improvement |
| Blocked by | none |

**Verification log:**
| Fix | Build | Result | Notes | Date |
|-----|-------|--------|-------|------|
| PART-028 (comment-only style → error) | #562 | VERIFIED | `validate-static.js` fires `PART-028-CSS-STRIPPED` as ERROR on build #562 — style block contains only `/* [CSS stripped — 59 chars, not relevant to JS fix] */`. T1 check working correctly. | 2026-03-23 |
| FIX-001 (CSS preservation in fix loop) | #562 | VERIFIED (rule deployed) / PENDING (build still has stripped CSS) | `CDN_CONSTRAINTS_BLOCK` in `prompts.js` includes FIX-001 rule injected into all fix prompts. Build #562 was approved before or during FIX-001 deploy — stripped CSS persists. PART-028 now catches this pattern; rule + T1 together will block it in future builds. | 2026-03-23 |
| ARIA-001 gen rule (aria-live on feedback divs) | #562 | NOT VERIFIED | No `aria-live` found on any feedback div in build #562 HTML. ARIA-001 WARNING fires for `#feedback-panel`. Rule not yet reflected in an approved build. Verify against next name-the-sides or any new approved build. | 2026-03-23 |
| ARIA-001 T1 check (fixture) | fixture | VERIFIED | Fixture test confirmed: HTML WITH `aria-live="polite"` on `#feedback-msg` → 0 ARIA-001 warnings. HTML WITHOUT → 1 ARIA-001 warning. T1 check fires correctly. | 2026-03-23 |
| CR-025 T1 validator (banned CSS token detection) | #564 + #562 | VERIFIED — no false positives | Build #564 (real-world-problem): WARNING fires for `--color-success` — TRUE POSITIVE (defined in `:root`, build approved before CR-025 deployed). Build #562 (name-the-sides): no GEN-CSS-TOKENS warning — clean. True-positive fixture (`var(--mathai-green)`) correctly caught. Validator exits cleanly on both files. | 2026-03-23 |
| GEN-119/120/121 | — | PENDING — no post-deploy build yet | — | 2026-03-23 |
| ARIA-001 expanded (c826ec1) — feedback divs without aria-live | #556 (name-the-sides, failed) | VERIFIED | validate-static.js fires `WARNING [ARIA-001]: 2 dynamic feedback div(s) found without aria-live attribute` for `#feedback-panel` and `#feedback-buttons`. Check W5 logic correct. ARIA-002 did NOT fire (no assertive elements in HTML — appropriate). | 2026-03-23 |
| MCQ data-phase rule (afb885e) — GEN-MCQ-PHASE + GEN-MCQ-TIMER | prompts.js confirmed | VERIFIED (rule text only) | `GEN-MCQ-PHASE` rule at lines 214–222 + 556–575. `GEN-MCQ-TIMER` rule at line 223. Both ship correct phase-set → syncDOMState() immediately → renderRound() pattern and timer.start()-in-renderRound() mandate. Prospective verification — pre-rule build #556 has 2/4 phase assignments missing syncDOMState() (W4 check fires). Rule will suppress this in future MCQ builds. | 2026-03-23 |
| GEN-GAMEID + GEN-UX-002 ext + GEN-CR-002 (d60ccc9) | associations #513 | VERIFIED | GEN-GAMEID: build #513 has `window.gameState = { currentRound: 0, ...}` — no `gameId` first field confirmed. GEN-UX-002: `.choice-btn` in #513 has `padding: 14px 8px` but zero `min-height` declaration — violation confirmed. GEN-CR-002: `categorizeFailure('Expected round counter to increment')` → `interaction` (not rendering) — natural-language false-positive eliminated. `categorizeFailure('expect(received) - toEqual - ...')` → `rendering` — Playwright diff format still caught correctly. validate-static.js on #513: fires PART-025-HIDE + GEN-UX-003 errors — both correct. No false-positive GEN-GAMEID or GEN-UX-002 checks in validate-static (those are gen-prompt rules only). | 2026-03-23 |
| GEN-PHASE-001 (3704eb7) — data-phase all game types | math-cross-grid spec + find-triangle-side #549 | VERIFIED | Violation confirmed: math-cross-grid spec has zero matches for `syncDOMState\|data-phase\|gameState.phase` — F4 in ui-ux.md explicitly marks this as first non-MCQ game to exhibit the absence. Baseline (find-triangle-side #549, approved non-MCQ game): 9 references to `gameState.phase` + `syncDOMState()` — pattern works for non-MCQ. Rule GEN-PHASE-001 correctly targets the gap. validate-static on #549: 2 errors (PART-025-HIDE: transitionScreen.hide() absent in startGame(); GEN-UX-005: SignalCollector no-args) — both pre-existing violations unrelated to GEN-PHASE-001, no false-positive from phase rule. | 2026-03-23 |
| GEN-UX-003 (ProgressBar slotId options object) | #561 (which-ratio, baseline) | VERIFIED (rule text confirmed) | prompts.js lines 630–642 + 633–639. Baseline HTML (build #561) uses `new ProgressBarComponent({ autoInject: true, totalRounds: ..., totalLives: 0 })` — MISSING `slotId` key entirely. T1 check in validate-static.js (§5f11) catches positional-string form but NOT the missing-slotId-key form — gap noted. New builds with rule will emit correct `slotId: 'mathai-progress-slot'`. | 2026-03-23 |
| GEN-UX-004 (alert() ban) | #561 (which-ratio, baseline) | VERIFIED (rule text confirmed; baseline clean) | prompts.js lines 212 + 644. No `alert(` found in build #561 HTML — baseline did not exhibit this violation. T1 check in validate-static.js: ABSENT — no regex for `alert(` calls. Gap: rule has no enforcement backstop. | 2026-03-23 |
| GEN-UX-005 (SignalCollector constructor args) | #561 (which-ratio, baseline) | VERIFIED (rule text confirmed; baseline shows violation) | prompts.js lines 213 + 657–664. Baseline HTML (build #561) line 804: `signalCollector = new SignalCollector()` — no args, confirmed violation. Rule ships correct form with `{ sessionId, studentId, templateId }`. T1 check in validate-static.js: ABSENT for no-arg form — gap noted. | 2026-03-23 |

---

## Education — Pedagogical Quality (active)

> See [docs/education/README.md](docs/education/README.md) for full Education slot R&D intuition and approach.

> Goal: move generated games from Bloom L1 recall (67% of current games) toward higher-order learning interactions. Tracked separately from pipeline reliability R&D.

**Active slot state:**
| Field | Value |
|-------|-------|
| Current task | Session 3 plan written (docs/education/session3-plan.md). Recommendation: Arithmetic Progressions (NCERT Class 10 Ch 5), 5 games, Bloom L1→L4. Defers Probability to Session 4. Awaiting human approval of topic before spec writing begins. |
| Status | Session 2 complete (4 games specced). Session 3 plan compares Probability vs AP across exam weight, misconception addressability, CDN compatibility, and pattern library value. |
| Waiting on | Human approval of Session 3 topic (AP recommended) before writing ap-identify spec |
| Blocked by | none |

| Task | Status | Notes |
|------|--------|-------|
| **Pedagogical analysis of warehouse games** | **done (2026-03-22)** | `docs/rnd-educational-interactions.md` (945 lines). Key finding: 67% of current warehouse games are Bloom L1 recall only — answer a question, get feedback, repeat. No worked examples, no scaffolding, no error analysis. Top opportunity: PART-036 Worked Example Component — highest-leverage pedagogical upgrade available in CDN bundle. |
| **PART-036 Worked Example spec template — soh-cah-toa-worked-example** | **done (2026-03-22, build #544 APPROVED)** | First spec using PART-036 WorkedExampleComponent from CDN. Build #544 APPROVED after 3 iterations (1505s). 4/5 batches passing (mechanics 4/4, LP 1/1, edge-cases 3/3, contract 0/2 — timing artefact, not game logic). Pattern established: sub-phases example→faded→practice, MCQ scaffolding, skip-to-phase harness. Contract 0/2 across all iterations approved at final review — confirms review model correctly handles test timing vs game logic mismatches. |
| **Replicate PART-036 pattern — algebra worked example (quadratic formula)** | **done (2026-03-22, build #546 APPROVED)** | Build on soh-cah-toa-worked-example approval to write a spec for a quadratic formula worked-example game using the same PART-036 sub-phase structure. Build #545 FAILED: prompts.js line 550 taught wrong postMessage field names (Lesson 169, commit 9eff5e6). Build #546 APPROVED (2264s, review-fix-1 for MCQ shuffle + trackEvent). CDN timing stall early-exit confirmed in production. Contract tests triage-deleted (test logic errors → led to CT8 rule). |
| **Rule 14: Education Implementation Slot** | **done (2026-03-22)** | New mandatory rule added to CLAUDE.md — one active education task must always be tracked in this section, same non-negotiable status as R&D slot. |
| **name-the-sides spec — trig session Game 1 (Bloom L2)** | **APPROVED #562 (2026-03-23, iter=3) — re-approved after spec updates** | 785-line spec for label-assignment game: learner assigns Hypotenuse/Opposite/Adjacent to 3 sides of a right triangle relative to a marked reference angle. 9 rounds (3 difficulty tiers: standard → rotated → two-angle). No lives, no timer (rounds 1-6), optional inline JS timer (7-9). Star logic based on skipped rounds. Simplification: CSS-drawn triangle + 3 MCQ button rows per side (no custom SVG CDN). Session plan position: FIRST game in trig sequence — prerequisite for soh-cah-toa-worked-example. NCERT Ch 8 §8.1 / CC HSG-SRT.C.6. 7 builds required across 5 independent failure layers (GEN-114/115/116/117/118). Build #557 APPROVED at iter=3 — GEN-116+117+118 compound fix resolved init failure class. |
| **which-ratio spec — trig session Game 4 (Bloom L2–L3)** | **APPROVED #561 (2026-03-23, iter=3)** | MCQ + worked example panel on first wrong attempt. Given a triangle with two sides labeled and an angle marked, identify which ratio (sin/cos/tan) relates those two sides to that angle. No lives — learning mode. Stars by accuracy. Bridges ratio recognition (Game 2) and computation (Game 3). NCERT Ch 8 §8.3 / CC HSG-SRT.C.7. Build #558 queued (2026-03-22). #559 APPROVED 8/10 tests by reviewer but FAILED post-approval EACCES (same root-owned infra bug as name-the-sides #555) — permissions fixed, #560 re-queued. #561 APPROVED iter=3. |
| **real-world-problem spec — trig session Game 6 (Bloom L4)** | **APPROVED #564 (2026-03-23, iter=2) — first L4 Bloom game approved. Session Planner L4 cap LIFTED.** | 714-line spec. Build #563 failed iter=0 (killed). Build #564 APPROVED iter=2 — spec fixes confirmed: FeedbackManager.sound ban + unconditional reference panel. Build #565 running (queued by analytics agent as gen rules data point — GEN-PM-001/GEN-RESTART-001 first test on real-world-problem). L4 reference example established: Session Planner can now generate L4 specs. Next: Session 2 statistics spec planning (stats-identify-class L1). |
| **Session 2 — Statistics (Mean, Median, Mode) spec planning** | **in-progress** | Candidate B chosen: no new CDN parts, broadest learner prerequisites (Class 9+ arithmetic). 5 games planned (stats-identify-class L1 → stats-mean-direct L2-L3 → stats-median L3 → stats-mode L3 → stats-which-measure L4). stats-identify-class spec written 2026-03-23 — games/stats-identify-class/spec.md. 9 rounds (Mean×3, Median×3, Mode×3), 5 Exa sources embedded, 12 test cases, 4 misconceptionTags, inline HTML frequency table pattern established. |
| **stats-identify-class spec — Session 2 Game 1** | **spec written 2026-03-23 — ready for review before queuing** | games/stats-identify-class/spec.md. Bloom L1 MCQ. 9 rounds across Mean (R1/R5/R9), Median (R2/R4/R7), Mode (R3/R6/R8). Contexts: class test marks, employee salary (outlier), shoe sizes (categorical), district area (outlier), weekly temperature, favourite fruit (nominal), house prices (skewed), blood groups (nominal), cricket batting average. 5 Exa sources cited inline. 4 misconceptionTags: always-use-mean, mean-ignores-outlier, mean-for-categorical, mode-vs-median-ordered. Worked-example panel on first wrong attempt (Measure Selector reference card). 3★ at ≥7/9 first-attempt correct. 12 test cases (TC-001–TC-012). |
| **which-ratio spec — add feedback timing requirements** | **pending** | games/which-ratio/spec.md | Source: UI/UX audit which-ratio #561 (Issues 5+6). (1) Correct feedback must display for at least 1500ms before auto-advance — current implementation uses 1200ms. (2) Skip feedback duration must be consistent across all skip paths (worked-example Skip vs second-attempt paths currently use 1500ms vs 2000ms). Add a single `FEEDBACK_DURATION_MS` constant and apply to all paths. |

---

## Analytics — Last Run

**ANALYTICS UPDATE (2026-03-23, live DB queries — test_results JSON + failure_patterns + builds tables):**

**Category pass rates (last-iteration per batch, lowest to highest):**
- game-flow: 313/412 (76%) — LOWEST, primary Gen Quality target
- level-progression: 198/256 (77%)
- mechanics: 445/556 (80%)
- contract: 202/247 (82%)
- edge-cases: 272/325 (84%) — highest

**Top 5 unresolved failure patterns:**
1. 19 occ | unknown | adjustment-strategy
2. 4 occ | unknown | count-and-tap
3. 2 occ | rendering | adjustment-strategy — game_complete postMessage on game over
4. 2 occ | rendering | adjustment-strategy — gameState init with required fields
5. 2 occ | rendering | adjustment-strategy — responsive layout fits within 480px
(+5 more rendering patterns in adjustment-strategy, all freq=2)

**First-attempt approval rate:** 45/94 approved builds (48%) — iter=0

**Never-approved games:** 1 — `template-schema.md` (non-game artifact, ignore)

**Slot routing:**
- Gen Quality: game-flow at 76% is lowest real category — trace failures to HTML vs test-gen root cause
- Test Engineering: TE-UNKNOWN-001 FIXED — 28 unknown rows found, 17 re-categorized (6 state, 7 rendering, 1 infra, 3 interaction); 11 irreducible noise rows (literal pattern='unknown') remain
- Code Review: unresolved failure_patterns persist — verify resolveFailurePattern() call site on approval path (CODE-001)

**Last updated:** 2026-03-23

---

## Summary

| Pillar | Done | Planned | Total |
|--------|------|---------|-------|
| P0 Deployment Blockers | 12 | 0 | 12 |
| P1 Testing & Validation | 8 | 0 | 8 |
| P2 Spec Compliance | 6 | 0 | 6 |
| P3 DevOps & Operations | 13 | 0 | 13 |
| P4 Code Quality | 6 | 0 | 6 |
| P5 Scalability | 13 | 1 | 14 |
| P6 Test Generation Quality | 72 | 3 | 75 |
| P7 Code Architecture | 15 | 0 | 15 |
| P8 Build Reliability | 7 | 1 | 8 |
| **Total** | **152** | **5** | **157** |

## What's Next

0. **[P8 — DONE] Warehouse hygiene gate** — shipped 2026-03-20 (commit 8202a79); worker.js deletes stale non-approved warehouse HTML before pipeline run
0. **[P8 — DONE] BullMQ stall prevention** — shipped 2026-03-20 (commit cc36e6c); `job.updateProgress()` heartbeat + KillMode=control-group prevents lock expiry stalls
0. **[P8 — DONE] Stale warehouse auto-delete: relax isInitFailure guard** — shipped 2026-03-20 (commit 5e7eaa0); ANY-match + passed===0; 10 new unit tests

1. **[R&D — done] Cross-game learning injection** — `getRelevantLearnings()` added to `lib/pipeline.js`; queries APPROVED build learnings from DB and merges into all gen/fix prompts; 347 tests pass; deployed 2026-03-20
2. **[R&D — done] Semantic learning deduplication** — `jaccardSimilarity()` + dedup pass in `getRelevantLearnings()`; Jaccard threshold 0.6, cap 20 bullets; 357 tests pass; deployed 2026-03-20
3. **[R&D — done] Spec-similarity retrieval** — `extractSpecKeywords()` + Jaccard scoring pass in `getRelevantLearnings()`; spec-relevant learnings sorted first; 362 tests / 358 pass; deployed 2026-03-20
4. **[R&D — done] Learning category boosting** — `getCategoryBoost()` in `lib/pipeline.js`; contract/cdncompat/audio/layout +0.2 additive boost; 10 new tests; 372 pass; deployed 2026-03-20
5. **[R&D — done] Spec-keyword SQL pre-filtering** — `spec_keywords TEXT` on builds table; `deriveRelevantCategories()` maps spec signals → SQL IN clause; `buildId` threaded through `runPipeline()` options; 13 new tests; 385 pass; deployed 2026-03-20
6. **[R&D — done] Learning retrieval composite index** — `idx_learnings_cat_build ON learnings(category, build_id DESC)` + partial index `idx_builds_approved ON builds(id) WHERE status='approved'`; SQL pre-filter now O(log N + k) at 10k+ rows; 385 tests pass; deployed 2026-03-20
7. **[R&D — done] pipeline.js Phase 1: prompts extracted** — 18 prompt builders extracted into lib/prompts.js; pipeline.js: 4105 → 3592 lines; 385 tests pass; deployed 2026-03-20
8. **[R&D — done] pipeline.js Phase 2: snapshot/harness/spec utilities extracted** — 12 utility functions extracted into lib/pipeline-utils.js (1221 lines); pipeline.js: 3592 → 2433 lines; 385 tests pass; deployed 2026-03-20
9. **[R&D — done] pipeline.js Phase 3: split remaining orchestration** — pipeline.js: 2433 → 839 lines; 3 sub-modules created; 385 tests pass; deployed 2026-03-20 (commit 73cc250)
9b. **[R&D — done] P6: Reduce review rejection rate** — Rules 22/23/24 in gen prompt + T1 Check 11/12; 389 tests pass; deployed 2026-03-20 (commit 1a6e01e)
9c. **[R&D — DONE] P8: Relax isInitFailure guard** — ANY-match + passed===0; shipped 2026-03-20 (commit 5e7eaa0); saves 5 wasted iterations on partial-init failures
10. **[R&D — DONE] Queue-sync resilience** — `requeueQueueSyncBuilds()` added to worker.js startup; scans for queue-sync-failed builds with retry_count<1 and re-enqueues; skips games with active builds; 7 new tests, 435 pass; deployed 2026-03-20 (commit P8)
11. **[R&D — DONE] Rendering-pattern detection: toBeVisible pre-triage** — `detectRenderingMismatch()` in `lib/pipeline-fix-loop.js`; if >3 failures match toBeVisible/toBeHidden, skip_tests without LLM triage call; 6 new tests, 441 pass; deployed 2026-03-20 (commit 9c2e641)
12. **[R&D — DONE] Deduplicate CDN constraint rules across fix prompts** — CDN_CONSTRAINTS_BLOCK expanded; reused in buildFixPrompt + buildGlobalFixPrompt + buildTargetedFixPrompt; -34 lines net; 441 tests pass; deployed 2026-03-20 (commit e7878e7)
12. **Multi-game scale validation** — run all specs in warehouse/templates/ to stress-test the pipeline; 37 builds currently queued (5 recent approvals: aided-game, one-digit-doubles, position-maximizer, queens, sequence-builder; 5 orphaned builds requeued after worker restart)
13. **[R&D — DONE] require() cache bust** — worker.js self-patches require() cache on HEAD change; commit f77b404; hot-deploy without worker restart now safe for lib/ changes
14. **[R&D — DONE] Gen prompt T1 compliance** — initSentry + debug rules moved to rule 5 (early in prompt); blank-page smoke check backstop; 447 tests pass; commits 14ab33c + cad2ca3
15. **[R&D — DONE] E9 cross-build pattern injection** — findMatchingPattern() + iteration-1 injection; 444 tests pass; commit 26b21b0
16. **[R&D — DONE] First-pass failure root cause analysis** — 65 triage events classified: 58% HTML fatal init, 22% phase-transition syncDOMState(), 9% data-shape. Shipped abort-on-snapshot-failure + Rule 22 (syncDOMState after every phase assignment). See Lessons 49 and 50 in docs/lessons-learned.md.
17. **[R&D — DONE] Measure cross-batch-guard + gen-timeout + phase-hints impact** — 4 fixes shipped: cross-batch-guard false rollback fix, gen LLM timeout 300→600s, M1-M5 mechanics rules, GF1/GF2 phase hints. Measurement window: builds 374+.
18. **[R&D — DONE] Queue-sync: detect orphaned DB-queued builds** — `requeueOrphanedQueuedBuilds()` in worker.js; compares DB queued count vs BullMQ queue depth; re-enqueues orphans with idempotency key; 5 new tests; 550 pass; deployed 2026-03-20 (commit 924c9a5). Recovers up to 37 builds per crash-loop incident.
19. **[R&D — DONE] Increase RALPH_GEN_LLM_TIMEOUT 600s → 1200s** — zip (56KB) timed out at exactly 600s; raised default in lib/config.js; deployed 2026-03-20 (commit 60bd7ae). Unblocks large-spec games.
20. **[R&D — measuring] Non-standard lifecycle test gen measurement** — H1 (LIVES SYSTEM CHECK, b27e010) + H2 (GAME FEATURE FLAGS, 86f5031) deployed 2026-03-20. Measurement builds 345 (associations) and 346 (rapid-challenge) failed due to Claude CLI auth outage before test gen ran. Hypothesis unverified: await next run of associations + rapid-challenge to check if lives-decrement tests are eliminated in iter-1 mechanics.
21. **[R&D — measuring] adjustment-strategy spec fix verification** — 4 root causes fixed in spec (button IDs, isProcessing race, calcStars game_over→0, postMessage fields) in commit 4789948. Builds #376 and #377 queued for verification. Awaiting first clean build with corrected spec to confirm approval in ≤3 iterations.
22. **[R&D — PLANNED] Measure abort-on-snapshot-failure impact** — add Prometheus counter for FatalSnapshotError regen triggers; track trigger rate over next 10 builds; if >20% investigate gen prompt improvements to reduce initial blank-page rate.
23. **[SHIPPED] popup-backdrop teardown Rule 24 + Lesson 58** — VisibilityTracker backdrop overlay intercepts clicks at iter 2; Rule 24 + CDN_CONSTRAINTS_BLOCK POPUP-BACKDROP TEARDOWN constraint deployed 2026-03-20 (commit 7428526); Lesson 58 in docs/lessons-learned.md.
24. **[SHIPPED] Passing-test-protection regex fix** — `passingContext` name extraction was always empty (regex `^\}` never matched indented `  });`); switched to name-only list format; "MUST keep passing" constraint now functional; prevents iter-3 regressions; deployed 2026-03-20 (commit 275d14f).
25. **[SHIPPED] Rate limit raised 10→20 builds/hr** — manually-cancelled builds were consuming rate-limiter slots; raised limit gives 3× headroom for admin operations; Lesson 60 in docs/lessons-learned.md; deployed 2026-03-21 (commit ac6588a). 550 tests pass.
26. **[SHIPPED] BullMQ lockDuration 30→90 min** — prevents "could not renew lock" errors on long Opus calls (build #305 took 64 min); lock renewal period adjusted proportionally; deployed 2026-03-21 (commit 95ed7c7). 550 tests pass.
27. **[SHIPPED] Screenshot-on-timeout in fix loop** — Playwright captures screenshot on failure; `collectTimeoutScreenshots()` reads timeout attachments and sends as vision content to fix LLM; 550 tests pass; deployed 2026-03-21 (commit dc0c72f).
28. **[SHIPPED] FeedbackManager audio popup auto-dismiss** — test harness auto-triggers primaryClick on PopupComponent after 30ms; eliminates manual popup handling in all tests; deployed 2026-03-21 (commit 16cc686).
29. **[SHIPPED] adjustment-strategy spec fixes (4 root causes)** — button IDs in initial HTML + all `updateAdjusterUI()` rebuilds, `isProcessing=false` before setTimeout, `game_over→0 stars` explicit branch, postMessage `duration_data`/`attempts` required fields; deployed 2026-03-21 (commit 4789948).
30. **[RESOLVED] Claude CLI auth reliability** — resolved by CLIProxyAPI migration (2026-03-21). Pipeline now routes all LLM calls through CLIProxyAPI; `claude -p` is no longer called directly. Auth issues on the raw CLI no longer affect builds. Lesson 61 in docs/lessons-learned.md.
31. **[SHIPPED] Fix smoke-regen context: CDN packages failed vs missing static div** — commit a4a7903; associations #392 CDN smoke triggered path; correct instructions now explain ScreenLayout.inject() failure root cause, not just "add a div".
32. **[R&D — DONE] First-attempt approval rate measurement** — measured rate: 1/3 = 33% first-attempt (builds #388, #390, #394). Phase-name normalization gap identified as top cause. Task #65 complete. Shipped phase-name normalization (game_over→gameover, game_complete→results, start_screen→start) and related gen prompt fixes.
33. **[SHIPPED] Page console error capture on 0/0 batch failures** — commit f6dc634; `capturePageConsoleErrors()` injected into fix prompt as `brokenPageErrorContext`; 550 tests pass.
34. **[SHIPPED] Global fix 0/0 regression guard** — commit 25ca9e6; rollback to pre-fix HTML when previously-passing batch regresses to 0/0 after global fix; 550 tests pass.
35. **[SHIPPED] CDN classification fix: ScreenLayout.inject() games** — commit 54dd4b7; `hasTransitionSlot` / `hasSlot` now checks for `ScreenLayout.inject` and CDN package URL in HTML — catches count-and-tap and similar CDN games that inject the transition slot at runtime.
60. **[SHIPPED] fixWaitForPackagesTimeout() deterministic pre-fix** — detects `10000` timeout literal in CDN game HTML and patches to `120000` directly, eliminating the static-fix LLM call entirely; saves 1 LLM call per CDN build on old timeout; zero regressions.
61. **[SHIPPED] CSS stripping from fix/review prompts** — strips `<style>` blocks from HTML before passing to fix/review LLMs; ~24% token reduction; CSS is irrelevant to JS logic fixes and was consuming prompt budget unnecessarily.
62. **[SHIPPED] Diff-based fix prompts at iteration 2+** — sends only changed lines (diff from prior iteration HTML) instead of full HTML for fix calls at iter 2+; ~91-99% token reduction on large CDN game files; fixes remain targeted.
63. **[SHIPPED] Test case completeness rules M13/GF6/EC4/EC5** — M13: wrong-answer path ≥1 test; GF6: lives-exhaustion gameover path; EC4: timer-expiry path for timed games; EC5: option-visibility after CDN slot transition. Closes gap between test-pass and review-approve.
64. **[SHIPPED] EC-FIX-1/2/3 edge-case fix rules** — EC-FIX-1: selector uniqueness guard; EC-FIX-2: lives-deduction ≥1 guard; EC-FIX-3: timer-expiry endGame guard. Injected into edge-case fix prompts to prevent next-iteration regression.
65. **[SHIPPED] Mobile viewport gen rule RULE-27 + T1 warnings** — RULE-27: body must not overflow:hidden when content scrolls; viewport meta must not block user-scalable; T1 warns on violations; prevents mobile test failures on scroll-heavy games.
66. **[SHIPPED] Review rejection rules: handleGameOver/signalPayload/calcStars** — RULE-REV-1: endGame must call handleGameOver (bypass = rejected); RULE-REV-2: signalPayload spread loses nested metrics (spread = rejected); RULE-REV-3: calcStars must match exact spec thresholds (accuracy = rejected). Adds 3 previously-uncodified review rejections as rejectable rules in review prompt.
67. **[SHIPPED] claude-opus-4-6 for gen + fix models** — switched GEN_MODEL and FIX_MODEL from gemini-3.1-pro-preview to claude-opus-4-6; Opus-class model expected to reduce smoke-regen rate + iter-1 failures vs Gemini baseline; measuring across next 10 builds.
68. **[SHIPPED] PostCompact + SessionStart hooks** — .claude/settings.json PostCompact hook recreates all 5 crons after context compaction; SessionStart hook runs full continuity checklist; prevents orphaned background tasks on session restart.
69. **[SHIPPED] Automated queue disabled** — build queue is now manual-only; Queue Strategist cron is advisory-only and no longer auto-enqueues; prevents accidental build storms during R&D measurement periods.
36. **[SHIPPED] FeedbackManager.init() purge from 20 specs** — commit c2b6a36; removed blocking audio popup code from all 20 CDN game specs; eliminates smoke-check failures caused by FeedbackManager blocking ScreenLayout.inject().
37. **[SUPERSEDED] Debug-window rule contradiction fix** — commits cc8eb56 + 4e0bf96 went the wrong direction (said NEVER assign to window). See item 42 for the correct final fix.
38. **[SUPERSEDED] PART-012 gen prompt exception** — superseded by commit d827777 which removed the exception and corrected the rule to MUST assign to window. See item 42.
39. **[SHIPPED] BullMQ lock 90min → 5h + stalledInterval 5min** — commit 17b384d; eliminates double-execution ("Missing key for job N") from premature stall detection on long multi-iteration builds; lockRenewTime 20min→60min; LockManager timer fires every 30min keeping 4h+ remaining at all times.
40. **[SHIPPED] Sentry.Integrations.CaptureConsole fix** — commit 14b15d1; initSentry() template updated to remove deprecated `new Sentry.Integrations.CaptureConsole(...)` usage; T1 static check added. Eliminates face-memory and similar CDN game Step 1d failures. P6 item marked done.
52. **[SHIPPED] BullMQ "Missing key" lock expiry fix (R&D #52)** — commit 4af2fd2; root cause: two successive SIGKILL cycles (systemctl restart while builds running) corrupted BullMQ stall-set `stc` field, causing `moveToFinished` to find job hash missing after 49-min pipeline completion. Fix: (1) `maxStalledCount: 0` — prevents stall checker from ever accumulating `stc` field regardless of restart cycles; (2) "Missing key" error now logged as WARN not ERROR, skipped for Sentry — pure BullMQ bookkeeping noise after SIGKILL; (3) SIGTERM handler logs active build IDs explicitly so restarts are visibly warned. Deploy pending (worker.js staged at /tmp/worker.js — deploy after current builds 473+475 complete).
41. **[SHIPPED] Smoke-regen #gameContent recovery improvement (R&D #41)** — commit e0e302a; buildSmokeRegenFixPrompt() now includes CRITICAL CDN GAME INIT FIXES section: explicit WRONG vs CORRECT ScreenLayout.inject() showing missing `slots:` wrapper, full CDN `<script src>` tag template with exact URLs for helpers/components/feedback-manager, and initSentry() ordering rule (must be AFTER waitForPackages resolves). Previous prompt described the symptom but did not show the LLM how to identify the bug in the failing HTML. Targets 38.5% repeat-failure baseline. Next data points from visual-memory #476 and keep-track #477 will measure impact. Success criterion: repeat-failure rate drops below 20% over next 10 smoke-regen events.
42. **[SHIPPED] Debug-window rule final correction** — commit d827777; T1 debugWindow error check removed (was wrong — approved games have window.debugGame, review does NOT reject it); CDN_CONSTRAINTS_BLOCK flipped to MUST assign to window; PART-012 gen prompt corrected. Supersedes cc8eb56 + 4e0bf96 which went wrong direction.
43. **[SHIPPED] 0/0 approval gate fix + CSS comma-ID selector escaping** — commit e843db2; game-flow approval requires ≥1 test pass AND ≥2 other categories passing (closes loophole where 0 game-flow tests ran but build auto-approved); CSS comma-ID selectors (e.g. `#q1,#q2`) auto-escaped in post-processing to prevent Playwright selector syntax errors.
44. **[SHIPPED] CSS selector safety rule + audio URL CDN constraint** — commit a32341d; CSS selector safety rule added to gen prompt (never use comma-separated ID selectors in CSS); audio URL must use cdn.mathai.ai domain added to CDN_CONSTRAINTS_BLOCK in fix prompts; prevents audio asset 404s during testing.
45. **[SHIPPED] fixCdnDomainsInFile audio preload URL corruption fix** — commit 7adcb0f; audio preload `<link>` tags with cdn.homeworkapp.ai were not being rewritten (only script tags); also fixed wrong-path script tag handling where path replacement produced malformed URLs; 550 tests pass.
46. **[SHIPPED] Phase name injection from DOM snapshot** — commit 7796007; post-processing reads dom-snapshot.json gameStateShape.phase after test-gen; replaces waitForPhase(page, 'playing') with actual runtime phase when non-standard. Lesson 75. 558 tests pass; deployed.
11. **Human-run Playwright traces** — record `--trace` from a correct human test run; use as ground truth for test generation, eliminating LLM selector hallucinations
12. **E4 warehouse-aware context** — deterministic Stage 1: spec → capability matrix → dependency graph → assembled prompt (skipped per user request)
47. **[SHIPPED] hasTwoPhases contract test fix + global fix loop deleted-spec bug** — commit 4f4164c; (1) hasTwoPhases feature flag now warns: skipToEnd('victory') → recall phase, NOT results — contract tests must use skipToEnd('game_over') (Lesson 111); (2) When triage deletes all spec files in a batch, batchFailed reset to 0 and batch added to deletedSpecBatches; global fix loop trigger excludes deletedSpecBatches so no spurious fix iterations run on deleted specs (Lesson 112). Affects associations, light-up, face-memory, rapid-challenge (hasTwoPhases games). 577 tests pass.
49. **[SHIPPED] T1 check for missing CDN script tags** — commit debe44a; When `waitForPackages()` is defined but no `<script src="https://storage.googleapis.com/...">` tags exist, new T1 error 5c2 fires: "MISSING: CDN `<script src>` tag." Diagnosed from disappearing-numbers #464 where fix LLM dropped all CDN script tags → blank page, "Packages failed to load within 10s." (Lesson 114). 577 tests pass.
48. **[SHIPPED] Global fix loop 0/0 treated as inconclusive** — commit 2e0f890; When Playwright returns 0/0 in the global fix loop (no tests ran — resource exhaustion, Playwright startup failure, or spec parse error), no longer treated as "page broken" and triggering LLM HTML fix. Now treated as inconclusive — keep per-batch score, log warning. Mirrors `detectCrossBatchRegression` guard on line 313 (Lesson 113). Prevents unnecessary LLM fix calls on infra failures. 577 tests pass.
50. **[SHIPPED] FeedbackManager.playDynamicFeedback namespace rule** — commit 054df32; gen prompt rule: never call FeedbackManager.sound.playDynamicFeedback (method doesn't exist on .sound sub-object — only on top-level FeedbackManager). Rule added to CDN_CONSTRAINTS_BLOCK (fix prompts) AND gen prompt FeedbackManager section. Diagnosed from count-and-tap #471 local run: TypeError throws synchronously in handleAnswer() leaving isProcessing=true permanently, deadlocking round lifecycle. (Lesson 115). 577 tests pass.
51. **[SHIPPED] Smoke-regen #gameContent recovery improvement (R&D #41)** — commit e0e302a; buildSmokeRegenFixPrompt() now includes CRITICAL CDN GAME INIT FIXES section with three explicit WRONG/CORRECT patterns for the 3 root causes of missing #gameContent in CDN games: (1) ScreenLayout.inject() missing `slots:` wrapper key, (2) missing CDN `<script src>` tags, (3) initSentry() called before waitForPackages(). Targets 38.5% repeat-failure baseline. Next data points from visual-memory and keep-track builds will measure impact.
53. **[SHIPPED] DOM snapshot CDN cold-start timeout (R&D #53)** — commit ce79a04 (Lesson 116); captureGameDomSnapshot() now detects CDN games via HTML script src patterns and uses 120s poll deadline + 60s final waitFor vs 65s + 5s for non-CDN. Evidence: disappearing-numbers #475 snapshot timed out at 5s final check after 65s poll. Eliminates static-fallback on CDN games, preserving window.gameState shape. Deployed without restart (pipeline-utils.js staged at /tmp — deploy agent deploying after builds 473+475 complete).
55. **[SHIPPED] T1 check for waitForPackages timeout <120s (R&D #55)** — commit 50b5a4e (Lesson 119); T1 validator check 5e1: if `waitForPackages()` exists AND timeout value < 120000 AND HTML has CDN script tags, ERROR flag. Forces static-fix LLM to upgrade to 120000ms. Makes Lesson 117 self-enforcing — no CDN game reaches smoke check with a 10s timeout. 577 tests pass; deployed to server.
56. **[SHIPPED] Error-to-lesson pattern matcher for fix prompts (R&D #56)** — commit f915865; 18-pattern LESSON_PATTERNS array in `lib/pipeline-fix-loop.js`; `getMatchingLessons()` injects up to 3 matching lesson hints into every per-batch fix prompt as "KNOWN PATTERNS" section. 587 tests pass; deployed to server 2026-03-21. Measure impact across next 10 CDN builds.
57. **[SHIPPED] Approval gate: skip triage-deleted categories (R&D #57)** — commit fad0139; `deletedSpecBatches` Set returned from `runFixLoop()`; approval gate marks SKIPPED (not FAILED) when category spec fully deleted by triage. Validated: disappearing-numbers #480 approved with level-progression triage-deleted (hasTwoPhases wrong test) + edge-cases page crash — reviewer accepted 6/8. 592 tests pass; deployed 2026-03-21.
58. **[SHIPPED] Final re-test 0/0 page crash root cause + fix (R&D #58)** — Root cause confirmed: Playwright startup failure (2-3s turnaround = process crash, not timeout). Fix: `workers: 1` in playwright config + `webServer.timeout` 15s→30s + stderr logging on all 4 catch blocks. Commit 4c942e4. Deployed 2026-03-21. Secondary finding from keep-track #482: GCP CDN cold-start is ~150s, not 120s. CDN poll loop increased 120s→160s, test timeout 180s→240s (commit e4e149b, Lesson 106). Keep-track #483 queued with fix.
59. **[COMPLETE] Scale run: 29→52 approved games (R&D #59)** — 52/52 COMPLETE — all 52 warehouse games approved (2026-03-22). memory-flip #505 ✅, two-player-race #506 ✅, position-maximizer #507 ✅ all approved after CDN URL path fix + test-gen quality rules. keep-track #503 ✅ approved 2026-03-22 — global fix 2 (248s LLM call) fixed mechanics 0/3→3/3 AND level-progression 0/1→1/1 simultaneously; 8/9 final (1 edge-case regression), review approved at attempt 2/3 after targeted fix. Additional pipeline bugs found and fixed this session: (3) Lesson 123 — 0/0 no-snapshot early-exit wasted 2 fix LLM calls per category (commit 15f9c70); (4) CDN URL path normalization — unpkg.com/@mathai/ + cdn.homeworkapp.ai/cdn/components/web/ 403/404s fixed (commit d06ad43). R&D combined rules first data point: position-maximizer 0/5 → 8/9 (+60%) with M10-M12+EC1 rules.
54. **[SHIPPED] CDN game waitForPackages timeout fix (R&D #54)** — commit c32e39f (Lesson 117); increased waitForPackages() timeout from 10s → 120s in gen prompt (template, CDN constraints block, smoke-regen prompt, inline rules). Root cause: fresh Playwright test browsers have no CDN cache; CDN takes 30-120s cold on GCP VMs. 10s timeout guaranteed to fail in every test run. Evidence: disappearing-numbers #475 — iter 1 AND iter 2 game-flow 0/2 with identical triage fix_html "game fails to initialize in transition slot". 577 tests pass. Deployed without restart (prompts.js).
70. **[SHIPPED] Lesson 133 rules — 5 test-gen quality rules** — commit f0861ea (2026-03-22); (1) M13 syncDOMState race guard: getLives/getScore/getRound must use expect.poll() to wait for syncDOMState 500ms cycle instead of reading synchronously after answer(); (2) CT4 strengthened: 4-step postMessage sequence mandatory (waitForPhase('results') → waitForTimeout(200) → getLastPostMessage() → null-guard); (3) GF6 sub-phase waitForPhase guard: wait for game-screen to be visible before asserting sub-phase-specific elements; (4) GF3/EC1/EC4 gameover timeout raised 15000ms → 20000ms (lives reaching 0 triggers 800ms animation before phase changes); (5) RULE-DUP: data-testid attributes must be globally unique — no two elements share the same data-testid across the game. Addresses remaining iteration-1 failure patterns from builds 508-512 measurement window.

---

_Last updated: 2026-03-23 — Analytics routing: GEN-ANALYTICS-001 (unknown bucket 33 occurrences) → R&D backlog; TE-ANALYTICS-001 (adjustment-strategy toBeVisible 8 failures) → P6 backlog; CODE-001 (resolveFailurePattern 0% resolve rate) → Code Review log + R&D backlog._
