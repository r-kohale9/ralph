# Build Log — which-ratio

*Build history pending — see DB on server.*

Query: `SELECT id, status, iterations, created_at FROM builds WHERE game_id='which-ratio' ORDER BY id DESC`

SSH: `ssh -i ~/.ssh/google_compute_engine the-hw-app@34.93.153.206`
