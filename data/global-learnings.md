- When a game uses a "transition" phase between screens, ensure it automatically advances to "playing" within a reasonable timeout; tests expect `data-phase="playing"` but the phase gets stuck on "transition" when the transition-end logic fails to fire or has no fallback timer.
- A `data-round="NaN"` value indicates the round counter was not properly initialized (likely parsed from an undefined variable); always initialize round tracking to a valid number (e.g., `0` or `1`) before the first phase change.
- Transition screens driven solely by CSS animation events (e.g., `animationend`, `transitionend`) must include a `setTimeout` fallback to guarantee the phase advances, since Playwright's headless browser may not reliably fire these events.
- Guard CDN custom-element registrations (e.g., `lottie-player`) with a `if (!customElements.get('lottie-player'))` check before importing, to prevent "name has already been used" errors when scripts load more than once.
- When using Playwright's `dragTo` on draggable elements, ensure the drop target is visible, stable, and scrolled into view first—prefer explicit `locator.hover()` → `page.mouse.down()` → `targetLocator.hover()` → `page.mouse.up()` sequences over `dragTo`, which frequently times out.
- After simulating game-state changes (e.g., losing lives), wait for the resulting UI transition (e.g., game-over screen) with `locator.waitFor({ state: 'visible' })` before asserting on state flags, otherwise the predicate times out because the expected DOM change hasn't propagated yet.
- Playwright locators do not have an `isIntersectingViewport` method; use `locator.boundingBox()` and compare against `page.viewportSize()` to assert that elements are within the visible viewport.
- Guard `customElements.define()` calls with `if (!customElements.get('lottie-player'))` to prevent duplicate registration errors when the Lottie player script is loaded multiple times or across test navigations.
- Ensure data structures expected to be iterable (e.g., arrays for round/level chains) are initialized as empty arrays (`[]`) rather than `null` or `undefined`, so destructuring and `for...of` loops don't throw `TypeError: X is not iterable`.
- Guard CDN custom-element registration (e.g., `lottie-player`) with a `customElements.get()` check before defining, to prevent "name has already been used" registry errors on re-import or HMR reloads.
- Ensure wrong-answer handling actually decrements `data-lives` and transitions `data-phase` to `"gameover"` when lives reach zero; a `data-lives` stuck at its initial value means the incorrect-answer code path is not executing or not updating state.
- When `data-round` shows `NaN`, the round counter is not being properly initialized or parsed as a number—always initialize round tracking with an explicit numeric value (e.g., `0` or `1`) and use `parseInt`/`Number` on any attribute reads before arithmetic.
- Simulated wrong answers in tests must match the exact interaction contract the game expects (e.g., clicking a specific `[data-testid="choice"]` element vs. submitting a form); if the click target doesn't trigger the wrong-answer handler, lives never decrement and phase never transitions.
- When a phase-change depends on an animation or timeout, the duration must be shorter than the Playwright assertion timeout (or tests must explicitly wait/trigger the advance), otherwise polling will exhaust retries while the UI stays stuck in the intermediate state.
- Truncated error logs (e.g., `isIntersec…`) indicate a missing or misnamed Playwright method — always verify the full API method name (`isIntersectingViewport` requires the experimental flag or should be replaced with a bounding-box visibility check) to avoid silent test-file parse/runtime failures.
- Loading a CDN script (e.g., Lottie) multiple times or without a `loaded` guard causes `CustomElementRegistry` duplicate-definition errors and stack overflows; gate all CDN component registrations with a single-load check or move the script tag to load exactly once.
- Playwright selectors timing out on `data-index` attributes (e.g., `.left-cell[data-left-index="0"]`) indicates the game grid is not rendered or populated before the test interacts; ensure the game transitions fully to the play state and cells are present in the DOM before any click assertions.
- Guard custom element registration with `if (!customElements.get('lottie-player'))` before calling `customElements.define()` to prevent "already been used with this registry" errors when the Lottie player script is loaded multiple times.
- When a test asserts that a game-state value (e.g., sequence length, score, level) increases over rounds, ensure the underlying game logic actually mutates that value on progression—returning a default or zero means the state-update code path is never reached.

## audio_patrn_mcq (FAILED) — 2026-04-08
- When building games that rely on audio generation or pattern matching, implement robust error handling and retry logic for API calls to gracefully handle transient 500 server errors rather than letting them crash the build.
- Design build pipelines to include automatic retry with exponential backoff for external API dependencies, since intermittent server errors (500s) are not caused by code issues and are often resolved on retry.
- Avoid coupling the entire build success to a single API call without fallback behavior; if an API call fails, the build should either retry, use cached/default assets, or fail with a clear diagnostic message rather than an opaque error.
- When encountering 500 Internal Server Errors during builds, log the full request context (endpoint, payload size, parameters) so that future debugging can distinguish between payload-related failures and genuine server outages.
